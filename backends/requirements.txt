# Gateway + all 5 role backends. Railpack (root dir = /backends) installs this.
# Versions pinned to match the local/dev environment.

# Web framework + ASGI server
fastapi==0.136.1
uvicorn[standard]==0.46.0
httpx==0.28.1

# Config + validation (backends import shared.config -> pydantic-settings)
pydantic==2.13.3
pydantic-settings==2.14.0
email-validator==2.3.0

# Databases
psycopg2-binary>=2.9.9
pymongo==4.16.0
redis==7.4.0

# Auth / security / MFA
PyJWT==2.10.1
bcrypt==5.0.0
passlib==1.7.4
pyotp>=2.9.0
qrcode>=7.4

# Forms / file uploads / .env loading
python-multipart==0.0.26
python-dotenv==1.0.0

# ORM (used by some routers)
SQLAlchemy==2.0.25
