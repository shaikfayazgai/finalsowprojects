---
phase: 03-womens-portal-university-portal
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - apps/women-portal/src/app/(app)/skills/page.tsx
  - apps/women-portal/src/app/(app)/earnings/page.tsx
  - apps/women-portal/src/app/(app)/credentials/page.tsx
  - apps/women-portal/src/app/(app)/messages/page.tsx
  - apps/women-portal/src/app/(app)/settings/page.tsx
  - apps/women-portal/src/app/(app)/settings/privacy/page.tsx
  - apps/women-portal/src/app/(app)/settings/notifications/page.tsx
  - apps/women-portal/src/app/(app)/settings/devices/page.tsx
  - apps/women-portal/src/components/skills/skill-genome-page.tsx
  - apps/women-portal/src/components/skills/index.ts
  - apps/women-portal/src/components/earnings/earnings-dashboard.tsx
  - apps/women-portal/src/components/earnings/index.ts
  - apps/women-portal/src/components/credentials/podl-credentials-list.tsx
  - apps/women-portal/src/components/credentials/index.ts
  - apps/women-portal/src/components/messages/messages-page.tsx
  - apps/women-portal/src/components/messages/index.ts
  - apps/women-portal/src/components/settings/profile-settings-form.tsx
  - apps/women-portal/src/components/settings/privacy-settings-form.tsx
  - apps/women-portal/src/components/settings/notification-prefs-form.tsx
  - apps/women-portal/src/components/settings/device-info-form.tsx
  - apps/women-portal/src/components/settings/index.ts
  - apps/women-portal/src/lib/msw/handlers/skills.ts
  - apps/women-portal/src/lib/msw/handlers/earnings.ts
  - apps/women-portal/src/lib/msw/handlers/podl.ts
  - apps/women-portal/src/lib/msw/handlers/messages.ts
  - apps/women-portal/src/lib/msw/handlers/profile.ts
  - apps/women-portal/src/lib/msw/handlers/index.ts
  - apps/women-portal/src/lib/msw/factories/skills.ts
  - apps/women-portal/src/lib/msw/factories/earnings.ts
  - apps/women-portal/src/lib/msw/factories/podl.ts
  - apps/women-portal/src/lib/msw/factories/messages.ts
  - apps/women-portal/src/messages/en.json
autonomous: true

must_haves:
  truths:
    - "Skill Genome page renders private skill profile with SkillGenomePanel -- NO leaderboard, NO peer comparison, NO ranking visible ANYWHERE"
    - "Skill Genome shows how completed tasks contributed to skill growth (task-to-skill mapping visible)"
    - "Earnings dashboard shows pending/released/total amounts with withdrawal history in a table"
    - "PoDL credentials page lists credential cards with chain-verified indicators and download affordance"
    - "Messages page shows async conversation threads with Community Support Lead (NOT live chat)"
    - "Profile settings form allows editing display name, city, timezone"
    - "Privacy settings allow toggling visibility preferences via switches"
    - "Notification preferences allow toggling in-app/email/WhatsApp channels per category"
    - "Device info form allows updating primary device, OS, internet speed, webcam, hours"
  artifacts:
    - path: "apps/women-portal/src/app/(app)/skills/page.tsx"
      provides: "Private Skill Genome page (WP-14, WP-15)"
    - path: "apps/women-portal/src/app/(app)/earnings/page.tsx"
      provides: "Earnings dashboard (WP-16)"
    - path: "apps/women-portal/src/app/(app)/credentials/page.tsx"
      provides: "PoDL credentials list (WP-17)"
    - path: "apps/women-portal/src/app/(app)/messages/page.tsx"
      provides: "Community Support messages (WP-18)"
    - path: "apps/women-portal/src/app/(app)/settings/page.tsx"
      provides: "Profile settings (WP-19)"
    - path: "apps/women-portal/src/app/(app)/settings/privacy/page.tsx"
      provides: "Privacy settings (WP-20)"
    - path: "apps/women-portal/src/app/(app)/settings/notifications/page.tsx"
      provides: "Notification preferences (WP-21)"
    - path: "apps/women-portal/src/app/(app)/settings/devices/page.tsx"
      provides: "Device/connectivity info update (WP-22)"
  key_links:
    - from: "apps/women-portal/src/components/skills/skill-genome-page.tsx"
      to: "@glimmora/ui SkillGenomePanel"
      via: "Uses SkillGenomePanel with privacy notice"
      pattern: "import.*SkillGenomePanel.*from.*@glimmora/ui"
    - from: "apps/women-portal/src/components/credentials/podl-credentials-list.tsx"
      to: "@glimmora/ui PoDLCard"
      via: "Renders PoDL credential cards"
      pattern: "import.*PoDLCard.*from.*@glimmora/ui"
---

<objective>
Build the remaining Women's Portal authenticated pages: Skill Genome (private, no comparison), earnings dashboard, PoDL credentials list, async messages with Community Support Lead, and all 4 settings pages (profile, privacy, notifications, devices).

Purpose: These complete the Women's Portal feature set. The Skill Genome and credentials pages demonstrate Fatima's private growth and achievement. The earnings page shows payment transparency. Messages provide her only communication channel. Settings give her control over her experience.

Output: 8 pages (skills, earnings, credentials, messages, 4 settings pages) + 5 new MSW handler files + 4 factory files. All Women's Portal requirements (WP-01 through WP-22) fully addressed.
</objective>

<execution_context>
@/Users/kavi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kavi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-womens-portal-university-portal/03-RESEARCH.md
@.planning/phases/03-womens-portal-university-portal/03-01-SUMMARY.md
@.planning/phases/03-womens-portal-university-portal/03-02-SUMMARY.md

Key codebase references:
@packages/ui/src/components/skill-genome-panel/skill-genome-panel.tsx
@packages/ui/src/components/podl-card/podl-card.tsx
@packages/ui/src/components/kpi-stat-card/kpi-stat-card.tsx
@packages/types/src/earnings.ts
@packages/types/src/message.ts
@packages/types/src/notification.ts
@packages/types/src/device.ts
@packages/types/src/skill-genome.ts
@packages/types/src/podl.ts
@apps/women-portal/src/store/auth-store.ts
@apps/women-portal/src/lib/msw/handlers/index.ts
@apps/women-portal/src/lib/msw/factories/common.ts
@apps/women-portal/src/app/(app)/layout.tsx

CRITICAL privacy constraints:
- Skill Genome: PRIVATE capability profile. NO public comparison, NO leaderboard, NO peer ranking.
- Messages: senderRole only (NOT sender name) -- privacy-preserving
- Privacy settings: user controls what is shared, defaults to maximum privacy
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Skill Genome, Earnings, Credentials pages + MSW factories and handlers</name>
  <files>
    apps/women-portal/src/app/(app)/skills/page.tsx
    apps/women-portal/src/app/(app)/earnings/page.tsx
    apps/women-portal/src/app/(app)/credentials/page.tsx
    apps/women-portal/src/components/skills/skill-genome-page.tsx
    apps/women-portal/src/components/skills/index.ts
    apps/women-portal/src/components/earnings/earnings-dashboard.tsx
    apps/women-portal/src/components/earnings/index.ts
    apps/women-portal/src/components/credentials/podl-credentials-list.tsx
    apps/women-portal/src/components/credentials/index.ts
    apps/women-portal/src/lib/msw/factories/skills.ts
    apps/women-portal/src/lib/msw/factories/earnings.ts
    apps/women-portal/src/lib/msw/factories/podl.ts
    apps/women-portal/src/lib/msw/handlers/skills.ts
    apps/women-portal/src/lib/msw/handlers/earnings.ts
    apps/women-portal/src/lib/msw/handlers/podl.ts
    apps/women-portal/src/lib/msw/handlers/index.ts
    apps/women-portal/src/messages/en.json
  </files>
  <action>
**Step 1: Create mock data factories.**

**apps/women-portal/src/lib/msw/factories/skills.ts:**
```typescript
import type { SkillGenome, SkillNode } from '@glimmora/types'

export function createMockSkillGenome(): SkillGenome {
  return {
    contributorId: 'user-001',
    skills: [
      { id: 's1', name: 'React', category: 'Frontend', level: 'advanced', evidenceCount: 12, lastDemonstrated: '2026-02-20', growthPercentage: 85, verifiedByMentor: true },
      { id: 's2', name: 'TypeScript', category: 'Languages', level: 'advanced', evidenceCount: 10, lastDemonstrated: '2026-02-18', growthPercentage: 78, verifiedByMentor: true },
      { id: 's3', name: 'Tailwind CSS', category: 'Frontend', level: 'intermediate', evidenceCount: 7, lastDemonstrated: '2026-02-15', growthPercentage: 60, verifiedByMentor: true },
      { id: 's4', name: 'Node.js', category: 'Backend', level: 'intermediate', evidenceCount: 5, lastDemonstrated: '2026-02-10', growthPercentage: 45, verifiedByMentor: false },
      { id: 's5', name: 'SQL', category: 'Data', level: 'beginner', evidenceCount: 2, lastDemonstrated: '2026-01-28', growthPercentage: 20, verifiedByMentor: false },
      { id: 's6', name: 'Testing', category: 'Quality', level: 'intermediate', evidenceCount: 4, lastDemonstrated: '2026-02-12', growthPercentage: 55, verifiedByMentor: true },
    ],
    lastUpdated: '2026-02-20T10:00:00Z',
    totalPoDLs: 6,
    overallGrowthPercentage: 58,
  }
}
```

**apps/women-portal/src/lib/msw/factories/earnings.ts:**
```typescript
import type { Earning, EarningsSummary } from '@glimmora/types'
import { randomId } from './common'

export function createMockEarningsSummary(): EarningsSummary {
  return {
    pending: 240,
    released: 1680,
    total: 1920,
    currency: 'USD',
    withdrawalHistory: [
      { id: randomId('earn'), taskId: 'task-008', projectId: 'proj-001', amount: 120, currency: 'USD', status: 'released', earnedAt: '2026-02-15T10:00:00Z', releasedAt: '2026-02-18T10:00:00Z' },
      { id: randomId('earn'), taskId: 'task-006', projectId: 'proj-001', amount: 200, currency: 'USD', status: 'released', earnedAt: '2026-02-10T10:00:00Z', releasedAt: '2026-02-14T10:00:00Z' },
      { id: randomId('earn'), taskId: 'task-004', projectId: 'proj-001', amount: 160, currency: 'USD', status: 'withdrawn', earnedAt: '2026-02-05T10:00:00Z', releasedAt: '2026-02-08T10:00:00Z', withdrawnAt: '2026-02-09T10:00:00Z' },
      { id: randomId('earn'), taskId: 'task-002', projectId: 'proj-001', amount: 240, currency: 'USD', status: 'pending', earnedAt: '2026-02-20T10:00:00Z' },
      { id: randomId('earn'), taskId: 'task-001', projectId: 'proj-001', amount: 180, currency: 'USD', status: 'released', earnedAt: '2026-01-28T10:00:00Z', releasedAt: '2026-02-01T10:00:00Z' },
    ],
  }
}
```

**apps/women-portal/src/lib/msw/factories/podl.ts:**
```typescript
import type { PoDLCredential } from '@glimmora/types'
import { randomId } from './common'

export function createMockPoDLCredentials(): PoDLCredential[] {
  return [
    {
      id: randomId('podl'), contributorId: 'user-001', taskId: 'task-008', projectId: 'proj-001',
      issuedAt: '2026-02-18T10:00:00Z', title: 'Dark Mode Toggle Implementation',
      description: 'Successfully implemented dark mode toggle with theme persistence',
      skillsDemonstrated: ['CSS', 'React', 'Tailwind'], evidenceHash: '0x7a3f...b2c1',
      verifiedByMentorId: 'mentor-001', organizationName: 'TechCorp Inc.',
      isRevoked: false, exportUrl: '/mock/podl-001.pdf',
    },
    {
      id: randomId('podl'), contributorId: 'user-001', taskId: 'task-006', projectId: 'proj-001',
      issuedAt: '2026-02-14T10:00:00Z', title: 'Image Loading Pipeline Optimization',
      description: 'Optimized image loading with lazy loading and CDN integration',
      skillsDemonstrated: ['Performance', 'React', 'CDN'], evidenceHash: '0x9b2e...a4d3',
      verifiedByMentorId: 'mentor-002', organizationName: 'TechCorp Inc.',
      isRevoked: false,
    },
    {
      id: randomId('podl'), contributorId: 'user-001', taskId: 'task-003', projectId: 'proj-002',
      issuedAt: '2026-01-25T10:00:00Z', title: 'Responsive Navigation Component',
      description: 'Built accessible mobile navigation with keyboard support',
      skillsDemonstrated: ['React', 'Accessibility', 'CSS'], evidenceHash: '0x4c1d...e7f8',
      verifiedByMentorId: 'mentor-001', organizationName: 'DesignHub Ltd.',
      isRevoked: false, exportUrl: '/mock/podl-003.pdf',
    },
  ]
}
```

**Step 2: Create MSW handlers.**

**apps/women-portal/src/lib/msw/handlers/skills.ts:**
```typescript
import { http, HttpResponse } from 'msw'
import { createMockSkillGenome } from '../factories/skills'

export const skillsHandlers = [
  http.get('/api/skill-genome', () => {
    return HttpResponse.json({ data: createMockSkillGenome() })
  }),
]
```

**apps/women-portal/src/lib/msw/handlers/earnings.ts:**
```typescript
import { http, HttpResponse } from 'msw'
import { createMockEarningsSummary } from '../factories/earnings'

export const earningsHandlers = [
  http.get('/api/earnings', () => {
    return HttpResponse.json({ data: createMockEarningsSummary() })
  }),
]
```

**apps/women-portal/src/lib/msw/handlers/podl.ts:**
```typescript
import { http, HttpResponse } from 'msw'
import { createMockPoDLCredentials } from '../factories/podl'

export const podlHandlers = [
  http.get('/api/credentials', () => {
    return HttpResponse.json({ data: createMockPoDLCredentials() })
  }),
]
```

**Update handlers/index.ts** to add skills, earnings, podl handlers (add imports + spread into handlers array).

**Step 3: Build Skill Genome page component.**

**apps/women-portal/src/components/skills/skill-genome-page.tsx:**
```typescript
'use client'
import { SkillGenomePanel } from '@glimmora/ui'
import { useQuery } from '@tanstack/react-query'
import { useTranslations } from 'next-intl'
import type { SkillGenome } from '@glimmora/types'

export function SkillGenomePage() {
  const t = useTranslations()
  const { data } = useQuery<{ data: SkillGenome }>({
    queryKey: ['skill-genome'],
    queryFn: () => fetch('/api/skill-genome').then(r => r.json()),
  })

  const genome = data?.data
  if (!genome) return null

  // Map SkillNode to SkillGenomePanel's expected format
  const skills = genome.skills.map(s => ({
    name: s.name,
    tier: s.level === 'beginner' ? 'emerging' as const : s.level === 'intermediate' ? 'developing' as const : s.level === 'advanced' ? 'proficient' as const : 'expert' as const,
    evidenceCount: s.evidenceCount,
    progress: s.growthPercentage,
    verified: s.verifiedByMentor,
  }))

  return (
    <div className="space-y-6 max-w-2xl">
      {/* Overall stats */}
      <div className="grid grid-cols-2 gap-4">
        <div className="bg-bg-card rounded-card shadow-card p-4">
          <p className="text-xs font-body text-text-caption uppercase tracking-wider">Total PoDLs</p>
          <p className="text-2xl font-display font-semibold text-text-heading mt-1">{genome.totalPoDLs}</p>
        </div>
        <div className="bg-bg-card rounded-card shadow-card p-4">
          <p className="text-xs font-body text-text-caption uppercase tracking-wider">Overall Growth</p>
          <p className="text-2xl font-display font-semibold text-text-heading mt-1">{genome.overallGrowthPercentage}%</p>
        </div>
      </div>

      {/* Skill Genome Panel -- PRIVATE, no comparison */}
      <SkillGenomePanel skills={skills} />

      {/* Task-to-skill contribution explanation */}
      <div className="bg-bg-card rounded-card shadow-card p-6">
        <h3 className="font-display text-base font-semibold text-text-heading mb-3">How Tasks Build Your Skills</h3>
        <p className="text-sm font-body text-text-body mb-4">
          Every task you complete contributes to your Skill Genome. When a mentor verifies your evidence,
          the associated skills are strengthened and your progress toward the next tier increases.
        </p>
        <div className="space-y-2">
          {genome.skills.filter(s => s.verifiedByMentor).slice(0, 3).map(s => (
            <div key={s.id} className="flex items-center justify-between text-sm font-body">
              <span className="text-text-body">{s.name}</span>
              <span className="text-text-caption">{s.evidenceCount} deliveries verified</span>
            </div>
          ))}
        </div>
      </div>
    </div>
  )
}
```

NOTE: Adapt SkillGenomePanel props to match its actual interface. The component expects `skills` array with `name`, `tier`, `evidenceCount`, `progress`, `verified` fields. Map from SkillNode `level` to the panel's `tier` enum ('emerging'/'developing'/'proficient'/'expert').

**Step 4: Build Earnings Dashboard component.**

**apps/women-portal/src/components/earnings/earnings-dashboard.tsx:**
```typescript
'use client'
import { Badge } from '@glimmora/ui'
import { useQuery } from '@tanstack/react-query'
import { useTranslations } from 'next-intl'
import type { EarningsSummary } from '@glimmora/types'

export function EarningsDashboard() {
  const t = useTranslations()
  const { data } = useQuery<{ data: EarningsSummary }>({
    queryKey: ['earnings'],
    queryFn: () => fetch('/api/earnings').then(r => r.json()),
  })

  const summary = data?.data
  if (!summary) return null

  return (
    <div className="space-y-6">
      {/* KPI cards */}
      <div className="grid grid-cols-1 md:grid-cols-3 gap-4">
        <div className="bg-bg-card rounded-card shadow-card p-6">
          <p className="text-xs font-body text-text-caption uppercase tracking-wider">Pending</p>
          <p className="text-2xl font-display font-semibold text-status-warning mt-1">${summary.pending}</p>
        </div>
        <div className="bg-bg-card rounded-card shadow-card p-6">
          <p className="text-xs font-body text-text-caption uppercase tracking-wider">Released</p>
          <p className="text-2xl font-display font-semibold text-status-success mt-1">${summary.released}</p>
        </div>
        <div className="bg-bg-card rounded-card shadow-card p-6">
          <p className="text-xs font-body text-text-caption uppercase tracking-wider">Total Earned</p>
          <p className="text-2xl font-display font-semibold text-text-heading mt-1">${summary.total}</p>
        </div>
      </div>

      {/* Withdrawal history table */}
      <div className="bg-bg-card rounded-card shadow-card overflow-hidden">
        <div className="p-4 border-b border-border">
          <h2 className="font-display text-lg font-semibold text-text-heading">Payment History</h2>
        </div>
        <div className="overflow-x-auto">
          <table className="w-full text-sm font-body">
            <thead>
              <tr className="border-b border-border bg-bg-dashboard">
                <th className="text-start px-4 py-3 text-xs font-medium text-text-caption uppercase tracking-wider">Date</th>
                <th className="text-start px-4 py-3 text-xs font-medium text-text-caption uppercase tracking-wider">Amount</th>
                <th className="text-start px-4 py-3 text-xs font-medium text-text-caption uppercase tracking-wider">Status</th>
              </tr>
            </thead>
            <tbody>
              {summary.withdrawalHistory.map((earning) => (
                <tr key={earning.id} className="border-b border-border last:border-0">
                  <td className="px-4 py-3 text-text-body">{new Date(earning.earnedAt).toLocaleDateString()}</td>
                  <td className="px-4 py-3 text-text-heading font-medium">${earning.amount}</td>
                  <td className="px-4 py-3">
                    <Badge status={earning.status === 'released' ? 'done' : earning.status === 'pending' ? 'atrisk' : 'normal'}>
                      {earning.status}
                    </Badge>
                  </td>
                </tr>
              ))}
            </tbody>
          </table>
        </div>
      </div>
    </div>
  )
}
```

**Step 5: Build PoDL Credentials List.**

**apps/women-portal/src/components/credentials/podl-credentials-list.tsx:**
```typescript
'use client'
import { PoDLCard } from '@glimmora/ui'
import { useQuery } from '@tanstack/react-query'
import type { PoDLCredential } from '@glimmora/types'

export function PoDLCredentialsList() {
  const { data } = useQuery<{ data: PoDLCredential[] }>({
    queryKey: ['credentials'],
    queryFn: () => fetch('/api/credentials').then(r => r.json()),
  })

  const credentials = data?.data || []

  return (
    <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
      {credentials.map((cred) => (
        <PoDLCard
          key={cred.id}
          title={cred.title}
          projectName={cred.organizationName}
          completedDate={new Date(cred.issuedAt).toLocaleDateString()}
          skills={cred.skillsDemonstrated}
          verificationHash={cred.evidenceHash}
          chainVerified={!cred.isRevoked}
          onExport={cred.exportUrl ? () => window.open(cred.exportUrl) : undefined}
        />
      ))}
      {credentials.length === 0 && (
        <p className="text-center text-sm font-body text-text-caption py-8 col-span-2">No credentials yet. Complete tasks to earn your first PoDL.</p>
      )}
    </div>
  )
}
```

Create index.ts barrel files for skills/, earnings/, credentials/ directories.

Create page files that import the components:
- `apps/women-portal/src/app/(app)/skills/page.tsx` renders SkillGenomePage
- `apps/women-portal/src/app/(app)/earnings/page.tsx` renders EarningsDashboard
- `apps/women-portal/src/app/(app)/credentials/page.tsx` renders PoDLCredentialsList

**Step 6: Add translation keys for skills, earnings, credentials, messages, settings to en.json.**

Merge into existing en.json:
```json
{
  "skills": {
    "title": "Skill Genome",
    "totalPodls": "Total PoDLs",
    "overallGrowth": "Overall Growth",
    "howTasksBuild": "How Tasks Build Your Skills"
  },
  "earnings": {
    "title": "Earnings",
    "pending": "Pending",
    "released": "Released",
    "totalEarned": "Total Earned",
    "paymentHistory": "Payment History"
  },
  "credentials": {
    "title": "PoDL Credentials",
    "empty": "No credentials yet. Complete tasks to earn your first PoDL."
  },
  "messages": {
    "title": "Messages",
    "newMessage": "New Message",
    "placeholder": "Type your message...",
    "send": "Send",
    "empty": "No messages yet"
  },
  "settings": {
    "title": "Settings",
    "profile": "Profile",
    "privacy": "Privacy",
    "notifications": "Notifications",
    "devices": "Devices & Connectivity",
    "save": "Save Changes"
  }
}
```
  </action>
  <verify>
Run `pnpm turbo build --filter=women-portal` -- builds without errors.
Verify `ls apps/women-portal/src/app/\(app\)/{skills,earnings,credentials}/page.tsx` -- all 3 exist.
Verify `ls apps/women-portal/src/lib/msw/handlers/{skills,earnings,podl}.ts` -- all 3 exist.
**Privacy verification:** `grep -i "rank\|leaderboard\|top.*percent\|comparison\|versus\|compared" apps/women-portal/src/components/skills/skill-genome-page.tsx` -- must return ZERO matches.
  </verify>
  <done>Skill Genome page with private capability profile (NO comparison/ranking), earnings dashboard with KPI cards and payment history table, PoDL credentials grid with chain-verified indicators. All data from MSW handlers returning realistic mock data.</done>
</task>

<task type="auto">
  <name>Task 2: Build Messages page + all 4 Settings pages + remaining MSW handlers</name>
  <files>
    apps/women-portal/src/app/(app)/messages/page.tsx
    apps/women-portal/src/app/(app)/settings/page.tsx
    apps/women-portal/src/app/(app)/settings/privacy/page.tsx
    apps/women-portal/src/app/(app)/settings/notifications/page.tsx
    apps/women-portal/src/app/(app)/settings/devices/page.tsx
    apps/women-portal/src/components/messages/messages-page.tsx
    apps/women-portal/src/components/messages/index.ts
    apps/women-portal/src/components/settings/profile-settings-form.tsx
    apps/women-portal/src/components/settings/privacy-settings-form.tsx
    apps/women-portal/src/components/settings/notification-prefs-form.tsx
    apps/women-portal/src/components/settings/device-info-form.tsx
    apps/women-portal/src/components/settings/index.ts
    apps/women-portal/src/lib/msw/handlers/messages.ts
    apps/women-portal/src/lib/msw/handlers/profile.ts
    apps/women-portal/src/lib/msw/handlers/index.ts
    apps/women-portal/src/lib/msw/factories/messages.ts
  </files>
  <action>
**Step 1: Create messages factory and handler.**

**apps/women-portal/src/lib/msw/factories/messages.ts:**
```typescript
import type { Message, MessageThread } from '@glimmora/types'
import { randomId } from './common'

export function createMockMessageThreads(): MessageThread[] {
  return [
    {
      id: randomId('thread'),
      contributorId: 'user-001',
      supportLeadId: 'support-001',
      subject: 'Getting started with my first task',
      lastMessageAt: '2026-02-25T14:00:00Z',
      unreadCount: 1,
      messages: [
        { id: randomId('msg'), threadId: 'thread-1', senderRole: 'contributor', content: 'Hi, I just received my first task assignment. Can you help me understand the evidence submission process?', sentAt: '2026-02-25T10:00:00Z', readAt: '2026-02-25T10:30:00Z' },
        { id: randomId('msg'), threadId: 'thread-1', senderRole: 'support-lead', content: 'Welcome! Great to see you starting. For evidence submission, you can upload files, share links, paste code, or write text descriptions. Each task brief will guide you on what evidence is expected.', sentAt: '2026-02-25T11:00:00Z', readAt: '2026-02-25T12:00:00Z' },
        { id: randomId('msg'), threadId: 'thread-1', senderRole: 'contributor', content: 'Thank you! That makes sense. One more question - how long does the review process typically take?', sentAt: '2026-02-25T13:00:00Z', readAt: '2026-02-25T13:30:00Z' },
        { id: randomId('msg'), threadId: 'thread-1', senderRole: 'support-lead', content: 'Reviews typically take 24-48 hours. You can track the status in your Submissions page. If rework is needed, you will see specific feedback from the mentor.', sentAt: '2026-02-25T14:00:00Z' },
      ],
    },
    {
      id: randomId('thread'),
      contributorId: 'user-001',
      supportLeadId: 'support-001',
      subject: 'Payment question',
      lastMessageAt: '2026-02-20T09:00:00Z',
      unreadCount: 0,
      messages: [
        { id: randomId('msg'), threadId: 'thread-2', senderRole: 'contributor', content: 'When will my payment for the completed task be released?', sentAt: '2026-02-20T08:00:00Z', readAt: '2026-02-20T08:30:00Z' },
        { id: randomId('msg'), threadId: 'thread-2', senderRole: 'support-lead', content: 'Payments are released once the enterprise requester approves the evidence pack. Your latest task payment should be processed within 3-5 business days.', sentAt: '2026-02-20T09:00:00Z', readAt: '2026-02-20T09:30:00Z' },
      ],
    },
  ]
}
```

**apps/women-portal/src/lib/msw/handlers/messages.ts:**
```typescript
import { http, HttpResponse } from 'msw'
import { createMockMessageThreads } from '../factories/messages'

const threads = createMockMessageThreads()

export const messageHandlers = [
  http.get('/api/messages', () => {
    return HttpResponse.json({ data: threads })
  }),

  http.post('/api/messages', async ({ request }) => {
    const body = await request.json() as { threadId: string; content: string }
    const thread = threads.find(t => t.id === body.threadId)
    if (thread) {
      thread.messages.push({
        id: `msg-${Date.now()}`,
        threadId: body.threadId,
        senderRole: 'contributor',
        content: body.content,
        sentAt: new Date().toISOString(),
      })
      thread.lastMessageAt = new Date().toISOString()
    }
    return HttpResponse.json({ data: thread })
  }),
]
```

**apps/women-portal/src/lib/msw/handlers/profile.ts:**
```typescript
import { http, HttpResponse } from 'msw'
import type { NotificationPreference, DeviceInfo } from '@glimmora/types'

const mockPrivacy = {
  profileVisibleToTeam: false,
  skillsVisibleToMentor: true,
  earningsVisible: false,
}

const mockNotifications: NotificationPreference[] = [
  { channel: 'in-app', category: 'task-updates', enabled: true },
  { channel: 'email', category: 'task-updates', enabled: true },
  { channel: 'whatsapp', category: 'task-updates', enabled: false },
  { channel: 'in-app', category: 'payment', enabled: true },
  { channel: 'email', category: 'payment', enabled: true },
  { channel: 'whatsapp', category: 'payment', enabled: false },
  { channel: 'in-app', category: 'community', enabled: true },
  { channel: 'email', category: 'community', enabled: false },
  { channel: 'whatsapp', category: 'community', enabled: false },
  { channel: 'in-app', category: 'system', enabled: true },
  { channel: 'email', category: 'system', enabled: true },
  { channel: 'whatsapp', category: 'system', enabled: false },
]

const mockDevices: DeviceInfo = {
  primaryDevice: 'laptop',
  operatingSystem: 'windows',
  internetSpeed: 'moderate',
  hasWebcam: true,
  availableHoursPerWeek: 15,
}

export const profileHandlers = [
  http.get('/api/settings/privacy', () => HttpResponse.json({ data: mockPrivacy })),
  http.put('/api/settings/privacy', async ({ request }) => {
    const body = await request.json()
    Object.assign(mockPrivacy, body)
    return HttpResponse.json({ data: mockPrivacy })
  }),
  http.get('/api/settings/notifications', () => HttpResponse.json({ data: mockNotifications })),
  http.put('/api/settings/notifications', async ({ request }) => {
    const body = await request.json() as NotificationPreference[]
    // Mock update
    return HttpResponse.json({ data: body })
  }),
  http.get('/api/profile/devices', () => HttpResponse.json({ data: mockDevices })),
  http.put('/api/profile/devices', async ({ request }) => {
    const body = await request.json()
    Object.assign(mockDevices, body)
    return HttpResponse.json({ data: mockDevices })
  }),
  http.get('/api/profile', () => {
    return HttpResponse.json({ data: { displayName: 'Fatima', email: 'fatima@example.com', city: 'Karachi', timezone: 'Asia/Karachi' } })
  }),
  http.put('/api/profile', async ({ request }) => {
    const body = await request.json()
    return HttpResponse.json({ data: body })
  }),
]
```

**Update handlers/index.ts** to add messageHandlers, profileHandlers, skillsHandlers, earningsHandlers, podlHandlers.

**Step 2: Build Messages page.**

**apps/women-portal/src/components/messages/messages-page.tsx:**
A chat-style async messages interface with thread list on left (or top on mobile) and message view on right. Uses `useQuery` to fetch `/api/messages`, displays threads as cards, and shows message bubbles when a thread is selected. Support lead messages align left, contributor messages align right. Send message via POST. This is async messaging (NOT live chat) -- no typing indicators, no read receipts visible.

Implementation should include:
- Thread list showing subject, last message preview, unread badge
- Selected thread showing messages in chronological order
- Chat-bubble styling: contributor messages `bg-brand-primary/10 ms-auto rounded-card rounded-br-none`, support messages `bg-bg-card border border-border rounded-card rounded-bl-none`
- Sender role shown as "You" or "Support Lead" (NOT names)
- Message input at bottom with send button
- Responsive: stack on mobile, side-by-side on desktop

**Step 3: Build Settings pages.**

**Profile Settings (apps/women-portal/src/components/settings/profile-settings-form.tsx):**
Form with TextInput fields for displayName, city, timezone (Select). Fetches from GET /api/profile, saves via PUT /api/profile. Uses existing @glimmora/ui form components.

**Privacy Settings (apps/women-portal/src/components/settings/privacy-settings-form.tsx):**
Toggle switches for: profileVisibleToTeam, skillsVisibleToMentor, earningsVisible. All default to maximum privacy (off). Uses Switch component from @glimmora/ui. Fetches/saves via /api/settings/privacy.

**Notification Preferences (apps/women-portal/src/components/settings/notification-prefs-form.tsx):**
Grid of toggles: rows = categories (task-updates, payment, community, system), columns = channels (in-app, email, whatsapp). Uses Switch component. Fetches/saves via /api/settings/notifications.

**Device Info (apps/women-portal/src/components/settings/device-info-form.tsx):**
Form matching the onboarding devices step but for updating info. Select for device, OS, internet speed. Checkbox for webcam. Select for hours. Fetches/saves via /api/profile/devices.

**Settings root page** (apps/women-portal/src/app/(app)/settings/page.tsx):
Profile settings form with sidebar links to Privacy, Notifications, Devices sub-pages. Use Tabs or a simple nav list linking to sub-routes.

Create all page files that import components.

Create index.ts barrel files.
  </action>
  <verify>
Run `pnpm turbo build --filter=women-portal` -- builds without errors.
Verify all pages exist: `ls apps/women-portal/src/app/\(app\)/{messages,settings,settings/privacy,settings/notifications,settings/devices}/page.tsx` -- all 5 exist.
Verify handlers: `grep -c "Handler" apps/women-portal/src/lib/msw/handlers/index.ts` -- should show all handler arrays imported.
  </verify>
  <done>Messages page with async thread-based conversation UI. 4 settings pages (profile, privacy, notifications, devices) all with form components and MSW handlers. All Women's Portal requirements WP-01 through WP-22 are now fully addressed with corresponding pages, components, MSW handlers, and factories.</done>
</task>

</tasks>

<verification>
1. `pnpm turbo build --filter=women-portal` builds without errors
2. All 8 new pages exist and render
3. Skill Genome page uses SkillGenomePanel with ZERO comparison/ranking/leaderboard text
4. Earnings shows pending/released/total with history table
5. Credentials shows PoDL cards with chain-verified indicators
6. Messages shows thread list + chat bubbles (senderRole only, not names)
7. Profile settings form edits display name, city, timezone
8. Privacy settings uses Switch toggles defaulting to maximum privacy
9. Notification preferences shows channel x category grid
10. Device info matches onboarding step 2 fields
11. All MSW handlers respond: /api/skill-genome, /api/earnings, /api/credentials, /api/messages, /api/profile, /api/settings/privacy, /api/settings/notifications, /api/profile/devices
</verification>

<success_criteria>
- Skill Genome is purely private -- ZERO leaderboard, ranking, or comparison elements
- Earnings shows clear pending vs released amounts
- PoDL credentials display chain-verified status visually
- Messages use async thread pattern (not live chat)
- Privacy defaults are maximum restriction (all toggles off by default)
- All WP-01 through WP-22 requirements addressed with corresponding pages
- Complete Women's Portal: 15 pages total across pre-auth and authenticated sections
</success_criteria>

<output>
After completion, create `.planning/phases/03-womens-portal-university-portal/03-03-SUMMARY.md`
</output>
