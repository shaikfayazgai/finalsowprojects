---
phase: 03-womens-portal-university-portal
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - apps/women-portal/src/app/(app)/layout.tsx
  - apps/women-portal/src/app/(app)/dashboard/page.tsx
  - apps/women-portal/src/app/(app)/tasks/page.tsx
  - apps/women-portal/src/app/(app)/tasks/[taskId]/page.tsx
  - apps/women-portal/src/app/(app)/tasks/[taskId]/submit/page.tsx
  - apps/women-portal/src/app/(app)/tasks/[taskId]/rework/page.tsx
  - apps/women-portal/src/app/(app)/submissions/page.tsx
  - apps/women-portal/src/components/dashboard/kpi-row.tsx
  - apps/women-portal/src/components/dashboard/active-tasks-summary.tsx
  - apps/women-portal/src/components/dashboard/apg-activity-widget.tsx
  - apps/women-portal/src/components/dashboard/index.ts
  - apps/women-portal/src/components/tasks/task-list-with-tabs.tsx
  - apps/women-portal/src/components/tasks/task-detail-view.tsx
  - apps/women-portal/src/components/tasks/evidence-submission-form.tsx
  - apps/women-portal/src/components/tasks/submission-status-tracker.tsx
  - apps/women-portal/src/components/tasks/rework-request-view.tsx
  - apps/women-portal/src/components/tasks/index.ts
  - apps/women-portal/src/lib/msw/handlers/tasks.ts
  - apps/women-portal/src/lib/msw/handlers/evidence.ts
  - apps/women-portal/src/lib/msw/handlers/apg.ts
  - apps/women-portal/src/lib/msw/handlers/index.ts
  - apps/women-portal/src/lib/msw/factories/task.ts
  - apps/women-portal/src/lib/msw/factories/evidence.ts
  - apps/women-portal/src/lib/msw/factories/apg.ts
  - apps/women-portal/src/messages/en.json
autonomous: true

must_haves:
  truths:
    - "Women's Portal (app) layout uses AppShell with Sidebar showing navigation: Dashboard, Tasks, Submissions, Skills, Earnings, Credentials, Messages, Settings"
    - "Dashboard renders 3 gradient KPI cards (Active Tasks, Earnings This Month, Skills Growing) + APG Activity Feed + Active Tasks Summary"
    - "Task list page shows status-filtered tabs (All, Open, In Progress, Under Review, Completed) with task cards showing title, status badge, skill tags, due date"
    - "Task detail page shows task brief with APG guidance, skill requirements as tags, and evidence submission CTA"
    - "Evidence submission form supports 5 types (File Upload, URL, Code Snippet, Video URL, Text) via tabbed interface using @glimmora/ui FileUpload, Tabs, TextInput, Textarea"
    - "Submission status tracker shows timeline visualization of evidence review stages"
    - "Rework request view shows mentor feedback and enables resubmission"
    - "All task/evidence data driven by MSW mock handlers returning realistic data"
  artifacts:
    - path: "apps/women-portal/src/app/(app)/layout.tsx"
      provides: "AppShell layout with Sidebar for all authenticated pages"
    - path: "apps/women-portal/src/app/(app)/dashboard/page.tsx"
      provides: "Dashboard page (WP-06, WP-07, WP-08)"
    - path: "apps/women-portal/src/app/(app)/tasks/[taskId]/submit/page.tsx"
      provides: "Evidence submission page (WP-11)"
    - path: "apps/women-portal/src/app/(app)/submissions/page.tsx"
      provides: "Submission tracking page (WP-12)"
    - path: "apps/women-portal/src/app/(app)/tasks/[taskId]/rework/page.tsx"
      provides: "Rework request view (WP-13)"
  key_links:
    - from: "apps/women-portal/src/app/(app)/layout.tsx"
      to: "@glimmora/ui AppShell"
      via: "Uses AppShell component with portal-specific sidebar config"
      pattern: "import.*AppShell.*from.*@glimmora/ui"
    - from: "apps/women-portal/src/components/dashboard/kpi-row.tsx"
      to: "@glimmora/ui GradientCard"
      via: "Gradient KPI cards on dashboard"
      pattern: "import.*GradientCard.*from.*@glimmora/ui"
    - from: "apps/women-portal/src/components/dashboard/kpi-row.tsx"
      to: "@glimmora/ui KPIStatCard"
      via: "KPI stat cards on dashboard"
      pattern: "import.*KPIStatCard.*from.*@glimmora/ui"
    - from: "apps/women-portal/src/components/tasks/evidence-submission-form.tsx"
      to: "@glimmora/ui FileUpload, Tabs"
      via: "Multi-type evidence submission"
      pattern: "import.*FileUpload.*Tabs.*from.*@glimmora/ui"
---

<objective>
Build the Women's Portal authenticated experience: AppShell layout with sidebar navigation, dashboard with gradient KPI cards and APG feed, task list with status filtering, task detail with APG guidance, multi-type evidence submission, submission tracking, and rework/resubmission flows.

Purpose: This is the core work delivery loop -- the journey from "I see my tasks" through "I submit evidence" to "I track my submissions and handle rework." This is where Fatima spends most of her time and where the platform delivers its primary value.

Output: 7 authenticated pages (dashboard, tasks list, task detail, evidence submit, submissions tracker, rework view) + AppShell layout + MSW handlers for tasks, evidence, and APG activities.
</objective>

<execution_context>
@/Users/kavi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kavi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-womens-portal-university-portal/03-RESEARCH.md
@.planning/phases/03-womens-portal-university-portal/03-01-SUMMARY.md

Key codebase references:
@packages/ui/src/components/app-shell/app-shell.tsx
@packages/ui/src/components/gradient-card/gradient-card.tsx
@packages/ui/src/components/kpi-stat-card/kpi-stat-card.tsx
@packages/ui/src/components/apg-feed/apg-feed.tsx
@packages/ui/src/components/tabs/tabs.tsx
@packages/ui/src/components/badge/badge.tsx
@packages/ui/src/components/tag/tag.tsx
@packages/ui/src/components/file-upload/file-upload.tsx
@packages/ui/src/components/timeline-bar/timeline-bar.tsx
@packages/ui/src/components/evidence-viewer/evidence-viewer.tsx
@packages/ui/src/components/data-table/data-table.tsx
@packages/types/src/task.ts
@packages/types/src/evidence.ts
@packages/types/src/api.ts
@apps/women-portal/src/store/auth-store.ts
@apps/women-portal/src/lib/msw/handlers/index.ts
@apps/women-portal/src/lib/msw/factories/user.ts

Patterns from 03-01:
- Route groups: (pre-auth) for unauthenticated, (app) for authenticated
- MSW handler pattern: domain-specific files aggregated in handlers/index.ts
- Factory pattern: createMock* functions with Partial<T> overrides
- Translation: useTranslations('namespace') for all user-visible strings
- Store: useAuthStore for authentication state
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AppShell layout + MSW factories/handlers for tasks, evidence, APG</name>
  <files>
    apps/women-portal/src/app/(app)/layout.tsx
    apps/women-portal/src/lib/msw/factories/task.ts
    apps/women-portal/src/lib/msw/factories/evidence.ts
    apps/women-portal/src/lib/msw/factories/apg.ts
    apps/women-portal/src/lib/msw/handlers/tasks.ts
    apps/women-portal/src/lib/msw/handlers/evidence.ts
    apps/women-portal/src/lib/msw/handlers/apg.ts
    apps/women-portal/src/lib/msw/handlers/index.ts
    apps/women-portal/src/messages/en.json
  </files>
  <action>
**Step 1: Create the (app) route group layout with AppShell.**

**apps/women-portal/src/app/(app)/layout.tsx:**
```typescript
'use client'
import { AppShell } from '@glimmora/ui'
import { usePathname } from 'next/navigation'
import { useTranslations } from 'next-intl'
import {
  LayoutDashboard,
  ListTodo,
  FileCheck,
  Brain,
  Wallet,
  Award,
  MessageSquare,
  Settings,
} from 'lucide-react'

export default function AppLayout({ children }: { children: React.ReactNode }) {
  const pathname = usePathname()
  const t = useTranslations()

  const navItems = [
    { label: 'Dashboard', href: '/dashboard', icon: LayoutDashboard },
    { label: 'Tasks', href: '/tasks', icon: ListTodo },
    { label: 'Submissions', href: '/submissions', icon: FileCheck },
    { label: 'Skills', href: '/skills', icon: Brain },
    { label: 'Earnings', href: '/earnings', icon: Wallet },
    { label: 'Credentials', href: '/credentials', icon: Award },
    { label: 'Messages', href: '/messages', icon: MessageSquare },
    { label: 'Settings', href: '/settings', icon: Settings },
  ]

  return (
    <AppShell
      portalName="Women's Portal"
      navItems={navItems}
      currentPath={pathname}
    >
      <div className="p-6">
        {children}
      </div>
    </AppShell>
  )
}
```

NOTE: Adapt the AppShell props to match the actual AppShell component API from @glimmora/ui. Read the app-shell.tsx file and use its exact prop interface. The navItems may need to be structured differently (e.g., `items` prop, `navigation` prop, etc.). The key requirement is: Sidebar with 8 navigation links, portal branding, and children rendered in the content area.

**Step 2: Create MSW mock data factories.**

**apps/women-portal/src/lib/msw/factories/task.ts:**
```typescript
import type { Task } from '@glimmora/types'
import { randomId, isoNow } from './common'

let taskCounter = 1

export function createMockTask(overrides: Partial<Task> = {}): Task {
  const id = `task-${String(taskCounter++).padStart(3, '0')}`
  return {
    id,
    projectId: 'proj-001',
    title: `UI Component Implementation: ${id}`,
    description: 'Implement the specified UI component following the design system guidelines and accessibility requirements.',
    type: 'development',
    status: 'open',
    priority: 'medium',
    skillRequirements: ['React', 'TypeScript', 'Tailwind CSS'],
    estimatedHours: 8,
    dueDate: '2026-04-01T00:00:00Z',
    apgGuidance: 'Focus on accessibility and responsive behavior. Use existing design system components where possible.',
    createdAt: isoNow(),
    updatedAt: isoNow(),
    ...overrides,
  }
}

export function createMockTaskList(): Task[] {
  return [
    createMockTask({ title: 'Build User Profile Card Component', status: 'in-progress', priority: 'high', skillRequirements: ['React', 'TypeScript', 'CSS'] }),
    createMockTask({ title: 'Implement Search Filters API', status: 'evidence-submitted', priority: 'medium', skillRequirements: ['Node.js', 'TypeScript', 'SQL'] }),
    createMockTask({ title: 'Create Mobile Navigation Menu', status: 'open', priority: 'medium', skillRequirements: ['React', 'CSS', 'Accessibility'] }),
    createMockTask({ title: 'Write Unit Tests for Auth Module', status: 'under-review', priority: 'high', skillRequirements: ['Testing', 'TypeScript', 'Jest'] }),
    createMockTask({ title: 'Design Data Export Feature', status: 'rework-required', priority: 'low', skillRequirements: ['TypeScript', 'CSV', 'React'] }),
    createMockTask({ title: 'Optimize Image Loading Pipeline', status: 'approved', priority: 'medium', skillRequirements: ['Performance', 'React', 'CDN'] }),
    createMockTask({ title: 'Build Notification Center Widget', status: 'open', priority: 'high', skillRequirements: ['React', 'WebSocket', 'TypeScript'] }),
    createMockTask({ title: 'Implement Dark Mode Toggle', status: 'completed', priority: 'low', skillRequirements: ['CSS', 'React', 'Tailwind'] }),
  ]
}
```

**apps/women-portal/src/lib/msw/factories/evidence.ts:**
```typescript
import type { Evidence, EvidencePack } from '@glimmora/types'
import { randomId, isoNow } from './common'

export function createMockEvidence(overrides: Partial<Evidence> = {}): Evidence {
  return {
    id: randomId('ev'),
    taskId: 'task-001',
    contributorId: 'user-001',
    type: 'file',
    title: 'Implementation Evidence',
    description: 'Code implementation with tests passing',
    content: '',
    fileName: 'component.tsx',
    fileSize: 24576,
    fileUrl: '/mock/files/component.tsx',
    status: 'submitted',
    submittedAt: isoNow(),
    ...overrides,
  }
}

export function createMockEvidencePack(taskId: string, status: Evidence['status'] = 'submitted'): EvidencePack {
  return {
    id: randomId('ep'),
    taskId,
    projectId: 'proj-001',
    evidenceItems: [
      createMockEvidence({ taskId, type: 'code', title: 'Source Code', content: 'export function Component() {\n  return <div>Hello</div>\n}', status }),
      createMockEvidence({ taskId, type: 'file', title: 'Screenshot', fileName: 'screenshot.png', fileSize: 150000, status }),
      createMockEvidence({ taskId, type: 'text', title: 'Implementation Notes', content: 'Implemented following the design system guidelines. All accessibility requirements met.', status }),
    ],
    submittedAt: isoNow(),
    status,
  }
}
```

**apps/women-portal/src/lib/msw/factories/apg.ts:**
```typescript
import type { APGActivity } from '@glimmora/types'
import { randomId } from './common'

export function createMockAPGActivities(): APGActivity[] {
  const now = Date.now()
  return [
    {
      id: randomId('apg'),
      type: 'task-assigned',
      message: 'APG assigned "Build User Profile Card" based on your React + TypeScript skills',
      entityId: 'task-001',
      entityType: 'task',
      timestamp: new Date(now - 2 * 60 * 60 * 1000).toISOString(),
    },
    {
      id: randomId('apg'),
      type: 'evidence-reviewed',
      message: 'Evidence for "Search Filters API" moved to mentor review queue',
      entityId: 'task-002',
      entityType: 'evidence',
      timestamp: new Date(now - 5 * 60 * 60 * 1000).toISOString(),
    },
    {
      id: randomId('apg'),
      type: 'milestone-updated',
      message: 'Project Alpha milestone "Phase 2 Components" is 75% complete',
      entityId: 'proj-001',
      entityType: 'project',
      timestamp: new Date(now - 12 * 60 * 60 * 1000).toISOString(),
    },
    {
      id: randomId('apg'),
      type: 'guidance-issued',
      message: 'Consider adding error boundary handling to the notification widget',
      entityId: 'task-007',
      entityType: 'task',
      timestamp: new Date(now - 24 * 60 * 60 * 1000).toISOString(),
    },
    {
      id: randomId('apg'),
      type: 'payment-triggered',
      message: 'Payment of $120 released for "Implement Dark Mode Toggle"',
      entityId: 'task-008',
      entityType: 'payment',
      timestamp: new Date(now - 48 * 60 * 60 * 1000).toISOString(),
    },
  ]
}
```

**Step 3: Create MSW handlers.**

**apps/women-portal/src/lib/msw/handlers/tasks.ts:**
```typescript
import { http, HttpResponse } from 'msw'
import { createMockTask, createMockTaskList } from '../factories/task'

const mockTasks = createMockTaskList()

export const taskHandlers = [
  http.get('/api/tasks', ({ request }) => {
    const url = new URL(request.url)
    const status = url.searchParams.get('status')
    const filtered = status && status !== 'all'
      ? mockTasks.filter(t => t.status === status)
      : mockTasks
    return HttpResponse.json({ data: filtered, meta: { page: 1, pageSize: 20, total: filtered.length, totalPages: 1 } })
  }),

  http.get('/api/tasks/:taskId', ({ params }) => {
    const task = mockTasks.find(t => t.id === params.taskId) || createMockTask({ id: params.taskId as string })
    return HttpResponse.json({ data: task })
  }),
]
```

**apps/women-portal/src/lib/msw/handlers/evidence.ts:**
```typescript
import { http, HttpResponse } from 'msw'
import { createMockEvidencePack } from '../factories/evidence'

const evidencePacks = new Map<string, ReturnType<typeof createMockEvidencePack>>()

export const evidenceHandlers = [
  http.post('/api/tasks/:taskId/evidence', async ({ params }) => {
    const pack = createMockEvidencePack(params.taskId as string)
    evidencePacks.set(params.taskId as string, pack)
    return HttpResponse.json({ data: pack }, { status: 201 })
  }),

  http.get('/api/tasks/:taskId/evidence', ({ params }) => {
    const taskId = params.taskId as string
    const pack = evidencePacks.get(taskId) || createMockEvidencePack(taskId, 'under-review')
    return HttpResponse.json({ data: pack })
  }),
]
```

**apps/women-portal/src/lib/msw/handlers/apg.ts:**
```typescript
import { http, HttpResponse } from 'msw'
import { createMockAPGActivities } from '../factories/apg'

export const apgHandlers = [
  http.get('/api/apg/activities', () => {
    return HttpResponse.json({ data: createMockAPGActivities() })
  }),
]
```

**Update apps/women-portal/src/lib/msw/handlers/index.ts** to include all handlers:
```typescript
import { authHandlers } from './auth'
import { onboardingHandlers } from './onboarding'
import { taskHandlers } from './tasks'
import { evidenceHandlers } from './evidence'
import { apgHandlers } from './apg'

export const handlers = [
  ...authHandlers,
  ...onboardingHandlers,
  ...taskHandlers,
  ...evidenceHandlers,
  ...apgHandlers,
]
```

**Step 4: Add dashboard/task translations to en.json.**

Merge these keys into the existing `apps/women-portal/src/messages/en.json`:
```json
{
  "dashboard": {
    "title": "Dashboard",
    "activeTasks": "Active Tasks",
    "earningsThisMonth": "Earnings This Month",
    "skillsGrowing": "Skills Growing",
    "apgActivity": "APG Activity",
    "currentTasks": "Current Tasks",
    "viewAllTasks": "View All Tasks"
  },
  "tasks": {
    "title": "My Tasks",
    "all": "All",
    "open": "Open",
    "inProgress": "In Progress",
    "underReview": "Under Review",
    "completed": "Completed",
    "dueDate": "Due",
    "viewTask": "View Task",
    "noTasks": "No tasks in this category"
  },
  "taskDetail": {
    "brief": "Task Brief",
    "apgGuidance": "APG Guidance",
    "skillsRequired": "Skills Required",
    "estimatedHours": "Estimated Hours",
    "dueDate": "Due Date",
    "submitEvidence": "Submit Evidence",
    "status": "Status"
  },
  "evidence": {
    "title": "Submit Evidence",
    "fileUpload": "File Upload",
    "url": "URL Link",
    "code": "Code Snippet",
    "videoUrl": "Video URL",
    "text": "Text",
    "submit": "Submit Evidence",
    "titleLabel": "Evidence Title",
    "descriptionLabel": "Description",
    "urlPlaceholder": "https://...",
    "codePlaceholder": "Paste your code here...",
    "videoPlaceholder": "YouTube or Vimeo URL...",
    "textPlaceholder": "Describe your work..."
  },
  "submissions": {
    "title": "My Submissions",
    "noSubmissions": "No submissions yet"
  },
  "rework": {
    "title": "Rework Required",
    "feedback": "Mentor Feedback",
    "items": "Items to Address",
    "resubmit": "Resubmit Evidence"
  }
}
```
Merge these into the existing en.json (do not replace -- ADD to the existing keys from 03-01).
  </action>
  <verify>
Run `pnpm turbo build --filter=women-portal` -- builds without errors.
Verify `ls apps/women-portal/src/app/\(app\)/layout.tsx` -- exists.
Verify `ls apps/women-portal/src/lib/msw/handlers/{tasks,evidence,apg}.ts` -- all exist.
Verify `ls apps/women-portal/src/lib/msw/factories/{task,evidence,apg}.ts` -- all exist.
  </verify>
  <done>AppShell layout with 8-item sidebar navigation. MSW factories producing realistic mock tasks (8 varied statuses), evidence packs (3 items per pack), and APG activities (5 types). All handlers registered. Translation keys for dashboard/tasks/evidence/submissions/rework added.</done>
</task>

<task type="auto">
  <name>Task 2: Build Dashboard, Task List, Task Detail, Evidence Submission, Submissions Tracker, and Rework pages</name>
  <files>
    apps/women-portal/src/app/(app)/dashboard/page.tsx
    apps/women-portal/src/app/(app)/tasks/page.tsx
    apps/women-portal/src/app/(app)/tasks/[taskId]/page.tsx
    apps/women-portal/src/app/(app)/tasks/[taskId]/submit/page.tsx
    apps/women-portal/src/app/(app)/tasks/[taskId]/rework/page.tsx
    apps/women-portal/src/app/(app)/submissions/page.tsx
    apps/women-portal/src/components/dashboard/kpi-row.tsx
    apps/women-portal/src/components/dashboard/active-tasks-summary.tsx
    apps/women-portal/src/components/dashboard/apg-activity-widget.tsx
    apps/women-portal/src/components/dashboard/index.ts
    apps/women-portal/src/components/tasks/task-list-with-tabs.tsx
    apps/women-portal/src/components/tasks/task-detail-view.tsx
    apps/women-portal/src/components/tasks/evidence-submission-form.tsx
    apps/women-portal/src/components/tasks/submission-status-tracker.tsx
    apps/women-portal/src/components/tasks/rework-request-view.tsx
    apps/women-portal/src/components/tasks/index.ts
  </files>
  <action>
**Step 1: Build Dashboard components.**

**apps/women-portal/src/components/dashboard/kpi-row.tsx:**
```typescript
'use client'
import { GradientCard, KPIStatCard } from '@glimmora/ui'
import { useTranslations } from 'next-intl'

interface KPIRowProps {
  activeTasks: number
  earningsThisMonth: number
  skillsGrowing: number
}

export function KPIRow({ activeTasks, earningsThisMonth, skillsGrowing }: KPIRowProps) {
  const t = useTranslations('dashboard')
  return (
    <div className="grid grid-cols-1 md:grid-cols-3 gap-4">
      <GradientCard>
        <KPIStatCard label={t('activeTasks')} value={activeTasks} />
      </GradientCard>
      <GradientCard>
        <KPIStatCard label={t('earningsThisMonth')} value={`$${earningsThisMonth}`} />
      </GradientCard>
      <GradientCard>
        <KPIStatCard label={t('skillsGrowing')} value={skillsGrowing} />
      </GradientCard>
    </div>
  )
}
```

NOTE: Adapt GradientCard and KPIStatCard props to match their actual API from @glimmora/ui. Read the component files to determine exact prop names and nesting pattern. The key requirement is: 3 cards in a row with gradient backgrounds showing the KPI label and value.

**apps/women-portal/src/components/dashboard/apg-activity-widget.tsx:**
```typescript
'use client'
import { APGFeed } from '@glimmora/ui'
import { useQuery } from '@tanstack/react-query'
import { useTranslations } from 'next-intl'
import type { APGActivity } from '@glimmora/types'

export function APGActivityWidget() {
  const t = useTranslations('dashboard')
  const { data } = useQuery<{ data: APGActivity[] }>({
    queryKey: ['apg-activities'],
    queryFn: () => fetch('/api/apg/activities').then(r => r.json()),
  })

  const actions = (data?.data || []).map(a => ({
    id: a.id,
    type: a.type as 'task_assigned' | 'review_requested' | 'milestone_completed' | 'risk_detected' | 'team_formed' | 'payment_triggered',
    title: a.message,
    description: '',
    timestamp: a.timestamp,
  }))

  return (
    <div className="bg-bg-card rounded-card shadow-card p-6">
      <h2 className="font-display text-lg font-semibold text-text-heading mb-4">{t('apgActivity')}</h2>
      <APGFeed actions={actions} maxVisible={5} />
    </div>
  )
}
```

NOTE: Adapt the APGFeed action type mapping to match the actual APGFeed component prop interface. The existing APGActivity type from @glimmora/types uses hyphens ('task-assigned') but the APGFeed component may use underscores ('task_assigned'). Map between them as needed.

**apps/women-portal/src/components/dashboard/active-tasks-summary.tsx:**
```typescript
'use client'
import { Badge, Button } from '@glimmora/ui'
import { useQuery } from '@tanstack/react-query'
import { useTranslations } from 'next-intl'
import Link from 'next/link'
import type { Task, APIResponse } from '@glimmora/types'

export function ActiveTasksSummary() {
  const t = useTranslations('dashboard')
  const { data } = useQuery<APIResponse<Task[]>>({
    queryKey: ['tasks', 'active'],
    queryFn: () => fetch('/api/tasks?status=in-progress').then(r => r.json()),
  })

  const tasks = data?.data || []

  return (
    <div className="bg-bg-card rounded-card shadow-card p-6">
      <div className="flex items-center justify-between mb-4">
        <h2 className="font-display text-lg font-semibold text-text-heading">{t('currentTasks')}</h2>
        <Link href="/tasks">
          <Button variant="ghost" size="sm">{t('viewAllTasks')}</Button>
        </Link>
      </div>
      <div className="space-y-3">
        {tasks.slice(0, 3).map((task) => (
          <Link key={task.id} href={`/tasks/${task.id}`} className="flex items-center justify-between p-3 rounded-inner hover:bg-hover transition-colors">
            <div>
              <p className="text-sm font-body font-medium text-text-heading">{task.title}</p>
              <p className="text-xs font-body text-text-caption">Due: {new Date(task.dueDate).toLocaleDateString()}</p>
            </div>
            <Badge status="inprogress">In Progress</Badge>
          </Link>
        ))}
        {tasks.length === 0 && (
          <p className="text-sm font-body text-text-caption text-center py-4">No active tasks</p>
        )}
      </div>
    </div>
  )
}
```

Create index.ts barrel.

**Step 2: Build Dashboard page.**

**apps/women-portal/src/app/(app)/dashboard/page.tsx:**
```typescript
import { KPIRow, APGActivityWidget, ActiveTasksSummary } from '@/components/dashboard'

export default function DashboardPage() {
  return (
    <div className="space-y-6">
      <h1 className="font-display text-2xl font-semibold text-text-heading">Dashboard</h1>
      <KPIRow activeTasks={3} earningsThisMonth={480} skillsGrowing={5} />
      <div className="grid grid-cols-1 lg:grid-cols-2 gap-6">
        <ActiveTasksSummary />
        <APGActivityWidget />
      </div>
    </div>
  )
}
```

**Step 3: Build Task List with tabs.**

**apps/women-portal/src/components/tasks/task-list-with-tabs.tsx:**
```typescript
'use client'
import { useState } from 'react'
import { Tabs, TabsList, TabsTrigger, TabsContent, Badge, Tag } from '@glimmora/ui'
import { useQuery } from '@tanstack/react-query'
import { useTranslations } from 'next-intl'
import Link from 'next/link'
import type { Task, APIResponse } from '@glimmora/types'

const STATUS_BADGE_MAP: Record<string, 'urgent' | 'normal' | 'inprogress' | 'done' | 'atrisk'> = {
  'open': 'normal',
  'assigned': 'normal',
  'in-progress': 'inprogress',
  'evidence-submitted': 'inprogress',
  'under-review': 'atrisk',
  'rework-required': 'urgent',
  'approved': 'done',
  'completed': 'done',
  'rejected': 'urgent',
}

export function TaskListWithTabs() {
  const [tab, setTab] = useState('all')
  const t = useTranslations('tasks')

  const { data, isLoading } = useQuery<APIResponse<Task[]>>({
    queryKey: ['tasks', tab],
    queryFn: () => fetch(`/api/tasks${tab !== 'all' ? `?status=${tab}` : ''}`).then(r => r.json()),
  })

  const tasks = data?.data || []

  return (
    <Tabs value={tab} onValueChange={setTab}>
      <TabsList>
        <TabsTrigger value="all">{t('all')}</TabsTrigger>
        <TabsTrigger value="open">{t('open')}</TabsTrigger>
        <TabsTrigger value="in-progress">{t('inProgress')}</TabsTrigger>
        <TabsTrigger value="under-review">{t('underReview')}</TabsTrigger>
        <TabsTrigger value="completed">{t('completed')}</TabsTrigger>
      </TabsList>

      <TabsContent value={tab}>
        <div className="space-y-3 mt-4">
          {tasks.map((task) => (
            <Link
              key={task.id}
              href={`/tasks/${task.id}`}
              className="block bg-bg-card rounded-card shadow-card p-4 hover:shadow-card-hover transition-shadow"
            >
              <div className="flex items-start justify-between gap-4">
                <div className="flex-1 min-w-0">
                  <h3 className="text-sm font-body font-medium text-text-heading truncate">{task.title}</h3>
                  <p className="text-xs font-body text-text-caption mt-1 line-clamp-2">{task.description}</p>
                  <div className="flex flex-wrap gap-1.5 mt-2">
                    {task.skillRequirements.map((skill) => (
                      <Tag key={skill} variant="skill">{skill}</Tag>
                    ))}
                  </div>
                </div>
                <div className="flex flex-col items-end gap-2 shrink-0">
                  <Badge status={STATUS_BADGE_MAP[task.status] || 'normal'}>{task.status}</Badge>
                  <span className="text-xs font-body text-text-caption">{t('dueDate')}: {new Date(task.dueDate).toLocaleDateString()}</span>
                </div>
              </div>
            </Link>
          ))}
          {tasks.length === 0 && !isLoading && (
            <p className="text-center text-sm font-body text-text-caption py-8">{t('noTasks')}</p>
          )}
        </div>
      </TabsContent>
    </Tabs>
  )
}
```

**apps/women-portal/src/app/(app)/tasks/page.tsx:**
```typescript
import { TaskListWithTabs } from '@/components/tasks'

export default function TasksPage() {
  return (
    <div className="space-y-6">
      <h1 className="font-display text-2xl font-semibold text-text-heading">My Tasks</h1>
      <TaskListWithTabs />
    </div>
  )
}
```

**Step 4: Build Task Detail page.**

**apps/women-portal/src/components/tasks/task-detail-view.tsx:**
```typescript
'use client'
import { Badge, Tag, Button } from '@glimmora/ui'
import { useQuery } from '@tanstack/react-query'
import { useTranslations } from 'next-intl'
import Link from 'next/link'
import type { Task } from '@glimmora/types'

interface TaskDetailViewProps {
  taskId: string
}

export function TaskDetailView({ taskId }: TaskDetailViewProps) {
  const t = useTranslations('taskDetail')
  const { data } = useQuery<{ data: Task }>({
    queryKey: ['task', taskId],
    queryFn: () => fetch(`/api/tasks/${taskId}`).then(r => r.json()),
  })

  const task = data?.data
  if (!task) return null

  return (
    <div className="space-y-6">
      <div className="flex items-start justify-between gap-4">
        <div>
          <h1 className="font-display text-2xl font-semibold text-text-heading">{task.title}</h1>
          <div className="flex items-center gap-3 mt-2">
            <Badge status="inprogress">{task.status}</Badge>
            <span className="text-sm font-body text-text-caption">{task.type}</span>
          </div>
        </div>
        {(task.status === 'assigned' || task.status === 'in-progress' || task.status === 'rework-required') && (
          <Link href={`/tasks/${taskId}/submit`}>
            <Button variant="primary">{t('submitEvidence')}</Button>
          </Link>
        )}
      </div>

      {/* Task Brief */}
      <div className="bg-bg-card rounded-card shadow-card p-6">
        <h2 className="font-display text-lg font-semibold text-text-heading mb-3">{t('brief')}</h2>
        <p className="text-sm font-body text-text-body whitespace-pre-wrap">{task.description}</p>
      </div>

      {/* APG Guidance */}
      {task.apgGuidance && (
        <div className="bg-brand-teal/5 border border-brand-teal/20 rounded-card p-6">
          <h2 className="font-display text-lg font-semibold text-text-heading mb-3 flex items-center gap-2">
            <svg className="h-5 w-5 text-brand-teal" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2"><circle cx="12" cy="12" r="10"/><path d="M12 16v-4M12 8h.01"/></svg>
            {t('apgGuidance')}
          </h2>
          <p className="text-sm font-body text-text-body">{task.apgGuidance}</p>
        </div>
      )}

      {/* Meta info */}
      <div className="grid grid-cols-2 md:grid-cols-3 gap-4">
        <div className="bg-bg-card rounded-card p-4">
          <p className="text-xs font-body text-text-caption uppercase tracking-wider">{t('skillsRequired')}</p>
          <div className="flex flex-wrap gap-1.5 mt-2">
            {task.skillRequirements.map((skill) => (
              <Tag key={skill} variant="skill">{skill}</Tag>
            ))}
          </div>
        </div>
        <div className="bg-bg-card rounded-card p-4">
          <p className="text-xs font-body text-text-caption uppercase tracking-wider">{t('estimatedHours')}</p>
          <p className="text-lg font-display font-semibold text-text-heading mt-1">{task.estimatedHours}h</p>
        </div>
        <div className="bg-bg-card rounded-card p-4">
          <p className="text-xs font-body text-text-caption uppercase tracking-wider">{t('dueDate')}</p>
          <p className="text-lg font-display font-semibold text-text-heading mt-1">{new Date(task.dueDate).toLocaleDateString()}</p>
        </div>
      </div>
    </div>
  )
}
```

**apps/women-portal/src/app/(app)/tasks/[taskId]/page.tsx:**
```typescript
import { TaskDetailView } from '@/components/tasks'

export default function TaskDetailPage({ params }: { params: Promise<{ taskId: string }> }) {
  // Next.js 15: params is async
  return <TaskDetailPageInner params={params} />
}

async function TaskDetailPageInner({ params }: { params: Promise<{ taskId: string }> }) {
  const { taskId } = await params
  return <TaskDetailView taskId={taskId} />
}
```

NOTE: In Next.js 15, `params` is a Promise. Use `await params` to resolve it. If the codebase uses Next.js 14, adjust to destructure directly. Check the existing canary page pattern.

**Step 5: Build Evidence Submission Form.**

**apps/women-portal/src/components/tasks/evidence-submission-form.tsx:**
```typescript
'use client'
import { useState } from 'react'
import { useTranslations } from 'next-intl'
import { useRouter } from 'next/navigation'
import { Tabs, TabsList, TabsTrigger, TabsContent, FileUpload, TextInput, Textarea, Button } from '@glimmora/ui'

interface EvidenceSubmissionFormProps {
  taskId: string
}

export function EvidenceSubmissionForm({ taskId }: EvidenceSubmissionFormProps) {
  const t = useTranslations('evidence')
  const router = useRouter()
  const [activeType, setActiveType] = useState('file')
  const [title, setTitle] = useState('')
  const [description, setDescription] = useState('')
  const [url, setUrl] = useState('')
  const [code, setCode] = useState('')
  const [text, setText] = useState('')
  const [isSubmitting, setIsSubmitting] = useState(false)

  async function handleSubmit() {
    setIsSubmitting(true)
    try {
      await fetch(`/api/tasks/${taskId}/evidence`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ type: activeType, title, description, content: url || code || text }),
      })
      router.push(`/tasks/${taskId}`)
    } finally {
      setIsSubmitting(false)
    }
  }

  return (
    <div className="space-y-6">
      <TextInput label={t('titleLabel')} value={title} onChange={(e) => setTitle(e.target.value)} />
      <Textarea label={t('descriptionLabel')} value={description} onChange={(e) => setDescription(e.target.value)} />

      <Tabs value={activeType} onValueChange={setActiveType}>
        <TabsList>
          <TabsTrigger value="file">{t('fileUpload')}</TabsTrigger>
          <TabsTrigger value="url">{t('url')}</TabsTrigger>
          <TabsTrigger value="code">{t('code')}</TabsTrigger>
          <TabsTrigger value="video-url">{t('videoUrl')}</TabsTrigger>
          <TabsTrigger value="text">{t('text')}</TabsTrigger>
        </TabsList>

        <TabsContent value="file">
          <FileUpload accept=".pdf,.doc,.docx,.zip,.png,.jpg,.jpeg" maxFiles={5} maxSizeMB={10} />
        </TabsContent>
        <TabsContent value="url">
          <TextInput placeholder={t('urlPlaceholder')} value={url} onChange={(e) => setUrl(e.target.value)} />
        </TabsContent>
        <TabsContent value="code">
          <Textarea placeholder={t('codePlaceholder')} value={code} onChange={(e) => setCode(e.target.value)} className="font-mono min-h-[200px]" />
        </TabsContent>
        <TabsContent value="video-url">
          <TextInput placeholder={t('videoPlaceholder')} value={url} onChange={(e) => setUrl(e.target.value)} />
        </TabsContent>
        <TabsContent value="text">
          <Textarea placeholder={t('textPlaceholder')} value={text} onChange={(e) => setText(e.target.value)} className="min-h-[200px]" />
        </TabsContent>
      </Tabs>

      <Button variant="primary" className="w-full" onClick={handleSubmit} disabled={isSubmitting}>
        {isSubmitting ? '...' : t('submit')}
      </Button>
    </div>
  )
}
```

**apps/women-portal/src/app/(app)/tasks/[taskId]/submit/page.tsx:**
```typescript
import { EvidenceSubmissionForm } from '@/components/tasks'

export default async function SubmitEvidencePage({ params }: { params: Promise<{ taskId: string }> }) {
  const { taskId } = await params
  return (
    <div className="max-w-2xl">
      <h1 className="font-display text-2xl font-semibold text-text-heading mb-6">Submit Evidence</h1>
      <EvidenceSubmissionForm taskId={taskId} />
    </div>
  )
}
```

**Step 6: Build Submission Status Tracker.**

**apps/women-portal/src/components/tasks/submission-status-tracker.tsx:**
```typescript
'use client'
import { Badge } from '@glimmora/ui'
import { useQuery } from '@tanstack/react-query'
import { useTranslations } from 'next-intl'
import Link from 'next/link'
import type { Task, APIResponse } from '@glimmora/types'

const REVIEW_STATUSES = ['evidence-submitted', 'under-review', 'rework-required', 'approved', 'rejected']

export function SubmissionStatusTracker() {
  const t = useTranslations('submissions')
  const { data } = useQuery<APIResponse<Task[]>>({
    queryKey: ['tasks', 'all'],
    queryFn: () => fetch('/api/tasks').then(r => r.json()),
  })

  const submittedTasks = (data?.data || []).filter(t => REVIEW_STATUSES.includes(t.status))

  return (
    <div className="space-y-4">
      {submittedTasks.map((task) => (
        <Link
          key={task.id}
          href={task.status === 'rework-required' ? `/tasks/${task.id}/rework` : `/tasks/${task.id}`}
          className="block bg-bg-card rounded-card shadow-card p-4 hover:shadow-card-hover transition-shadow"
        >
          <div className="flex items-center justify-between">
            <div>
              <h3 className="text-sm font-body font-medium text-text-heading">{task.title}</h3>
              <p className="text-xs font-body text-text-caption mt-1">Submitted {new Date(task.updatedAt).toLocaleDateString()}</p>
            </div>
            <Badge status={task.status === 'approved' ? 'done' : task.status === 'rework-required' ? 'urgent' : 'inprogress'}>
              {task.status}
            </Badge>
          </div>
        </Link>
      ))}
      {submittedTasks.length === 0 && (
        <p className="text-center text-sm font-body text-text-caption py-8">{t('noSubmissions')}</p>
      )}
    </div>
  )
}
```

**apps/women-portal/src/app/(app)/submissions/page.tsx:**
```typescript
import { SubmissionStatusTracker } from '@/components/tasks'

export default function SubmissionsPage() {
  return (
    <div className="space-y-6">
      <h1 className="font-display text-2xl font-semibold text-text-heading">My Submissions</h1>
      <SubmissionStatusTracker />
    </div>
  )
}
```

**Step 7: Build Rework Request View.**

**apps/women-portal/src/components/tasks/rework-request-view.tsx:**
```typescript
'use client'
import { Badge, Button } from '@glimmora/ui'
import { useQuery } from '@tanstack/react-query'
import { useTranslations } from 'next-intl'
import Link from 'next/link'
import type { EvidencePack } from '@glimmora/types'

interface ReworkRequestViewProps {
  taskId: string
}

export function ReworkRequestView({ taskId }: ReworkRequestViewProps) {
  const t = useTranslations('rework')
  const { data } = useQuery<{ data: EvidencePack }>({
    queryKey: ['evidence', taskId],
    queryFn: () => fetch(`/api/tasks/${taskId}/evidence`).then(r => r.json()),
  })

  const pack = data?.data
  if (!pack) return null

  return (
    <div className="space-y-6 max-w-2xl">
      <div className="flex items-center gap-3">
        <Badge status="urgent">Rework Required</Badge>
      </div>

      {/* Feedback from reviewer */}
      <div className="bg-status-urgent/5 border border-status-urgent/20 rounded-card p-6">
        <h2 className="font-display text-lg font-semibold text-text-heading mb-3">{t('feedback')}</h2>
        {pack.evidenceItems.filter(e => e.reviewerFeedback).map((item) => (
          <div key={item.id} className="mb-4">
            <p className="text-sm font-body text-text-body whitespace-pre-wrap">{item.reviewerFeedback}</p>
            {item.reworkItems && item.reworkItems.length > 0 && (
              <div className="mt-3">
                <p className="text-xs font-body font-medium text-text-heading uppercase tracking-wider mb-2">{t('items')}</p>
                <ul className="space-y-1">
                  {item.reworkItems.map((ri, i) => (
                    <li key={i} className="text-sm font-body text-text-body flex items-start gap-2">
                      <span className="text-status-urgent mt-0.5">-</span>
                      {ri}
                    </li>
                  ))}
                </ul>
              </div>
            )}
          </div>
        ))}
      </div>

      <Link href={`/tasks/${taskId}/submit`}>
        <Button variant="primary" className="w-full">{t('resubmit')}</Button>
      </Link>
    </div>
  )
}
```

**apps/women-portal/src/app/(app)/tasks/[taskId]/rework/page.tsx:**
```typescript
import { ReworkRequestView } from '@/components/tasks'

export default async function ReworkPage({ params }: { params: Promise<{ taskId: string }> }) {
  const { taskId } = await params
  return (
    <div>
      <h1 className="font-display text-2xl font-semibold text-text-heading mb-6">Rework Required</h1>
      <ReworkRequestView taskId={taskId} />
    </div>
  )
}
```

Create tasks index.ts barrel:
```typescript
export { TaskListWithTabs } from './task-list-with-tabs'
export { TaskDetailView } from './task-detail-view'
export { EvidenceSubmissionForm } from './evidence-submission-form'
export { SubmissionStatusTracker } from './submission-status-tracker'
export { ReworkRequestView } from './rework-request-view'
```
  </action>
  <verify>
Run `pnpm turbo build --filter=women-portal` -- builds without errors.
Verify all 6 pages exist: `ls apps/women-portal/src/app/\(app\)/{dashboard/page.tsx,tasks/page.tsx,submissions/page.tsx}` and `ls apps/women-portal/src/app/\(app\)/tasks/\[taskId\]/{page.tsx,submit/page.tsx,rework/page.tsx}`.
Verify all component files: `ls apps/women-portal/src/components/{dashboard,tasks}/` -- both directories populated.
  </verify>
  <done>Dashboard with 3 gradient KPI cards, APG activity feed, and active tasks summary. Task list with 5 status-filtered tabs. Task detail showing brief, APG guidance, and skill tags. Evidence submission with 5-type tabbed interface. Submission status tracker with review stage indicators. Rework view showing feedback and resubmit CTA. All driven by MSW mock data via TanStack Query.</done>
</task>

</tasks>

<verification>
1. `pnpm turbo build --filter=women-portal` builds without errors
2. AppShell layout wraps all (app) pages with sidebar navigation
3. Dashboard shows gradient KPI cards with realistic numbers
4. Dashboard shows APG Activity Feed with 5 activity types
5. Task list renders 8 mock tasks with correct status badges and skill tags
6. Task detail shows task brief, APG guidance block, skill tags, estimated hours, due date
7. Evidence submission form has 5 tabs (File, URL, Code, Video, Text) with FileUpload component
8. Submissions tracker shows tasks filtered by review statuses
9. Rework view shows reviewer feedback with rework items list and resubmit button
10. All visible text uses useTranslations() (no hardcoded strings in components)
11. MSW handlers respond to GET /api/tasks, GET /api/tasks/:id, POST /api/tasks/:id/evidence, GET /api/tasks/:id/evidence, GET /api/apg/activities
</verification>

<success_criteria>
- Dashboard renders gradient KPI cards and APG feed with mock data
- Task list has working status tab filters
- Task detail shows APG guidance prominently
- Evidence submission supports all 5 evidence types
- Rework flow shows structured mentor feedback and enables resubmission
- All pages nested under AppShell with consistent sidebar navigation
- No hardcoded strings in components -- all text comes from translations
</success_criteria>

<output>
After completion, create `.planning/phases/03-womens-portal-university-portal/03-02-SUMMARY.md`
</output>
