---
phase: 03-womens-portal-university-portal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/types/src/earnings.ts
  - packages/types/src/message.ts
  - packages/types/src/onboarding.ts
  - packages/types/src/student.ts
  - packages/types/src/governor.ts
  - packages/types/src/notification.ts
  - packages/types/src/device.ts
  - packages/types/src/index.ts
  - apps/women-portal/package.json
  - apps/women-portal/next.config.ts
  - apps/women-portal/src/i18n/request.ts
  - apps/women-portal/src/messages/en.json
  - apps/women-portal/src/messages/ur.json
  - apps/women-portal/src/messages/ar.json
  - apps/women-portal/src/app/layout.tsx
  - apps/women-portal/src/app/globals.css
  - apps/women-portal/src/app/(pre-auth)/layout.tsx
  - apps/women-portal/src/app/(pre-auth)/page.tsx
  - apps/women-portal/src/app/(pre-auth)/welcome/page.tsx
  - apps/women-portal/src/app/(pre-auth)/register/page.tsx
  - apps/women-portal/src/app/(pre-auth)/onboarding/layout.tsx
  - apps/women-portal/src/app/(pre-auth)/onboarding/profile/page.tsx
  - apps/women-portal/src/app/(pre-auth)/onboarding/devices/page.tsx
  - apps/women-portal/src/app/(pre-auth)/onboarding/skills/page.tsx
  - apps/women-portal/src/app/(pre-auth)/onboarding/activation/page.tsx
  - apps/women-portal/src/components/language-selector/language-selector.tsx
  - apps/women-portal/src/components/language-selector/index.ts
  - apps/women-portal/src/components/welcome-screen/welcome-screen.tsx
  - apps/women-portal/src/components/welcome-screen/index.ts
  - apps/women-portal/src/components/registration-form/registration-form.tsx
  - apps/women-portal/src/components/registration-form/index.ts
  - apps/women-portal/src/components/onboarding/profile-step.tsx
  - apps/women-portal/src/components/onboarding/devices-step.tsx
  - apps/women-portal/src/components/onboarding/skills-step.tsx
  - apps/women-portal/src/components/onboarding/activation-step.tsx
  - apps/women-portal/src/components/onboarding/privacy-banner.tsx
  - apps/women-portal/src/components/onboarding/index.ts
  - apps/women-portal/src/store/auth-store.ts
  - apps/women-portal/src/store/language-store.ts
  - apps/women-portal/src/lib/msw/handlers.ts
  - apps/women-portal/src/lib/msw/handlers/auth.ts
  - apps/women-portal/src/lib/msw/handlers/onboarding.ts
  - apps/women-portal/src/lib/msw/handlers/index.ts
  - apps/women-portal/src/lib/msw/factories/user.ts
  - apps/women-portal/src/lib/msw/factories/common.ts
  - apps/women-portal/src/app/page.tsx
  - apps/university-portal/package.json
  - apps/university-portal/next.config.ts
  - apps/university-portal/src/i18n/request.ts
  - apps/university-portal/src/messages/en.json
  - pnpm-lock.yaml
autonomous: true

must_haves:
  truths:
    - "Language selection (English, Urdu, Arabic) is the FIRST interactive element on the Women's Portal -- shown at the root URL before any other content"
    - "Selecting Urdu or Arabic sets dir='rtl' on the <html> element and the page content flows right-to-left"
    - "WhatsApp-style welcome screen shows conversational chat-bubble UI in Glimmora brand colors (NOT WhatsApp green), appearing after language selection and before registration"
    - "Registration form accepts email + password (no phone required, no social login) and stores mock auth state"
    - "4-step onboarding wizard uses route-based steps (/onboarding/profile, /devices, /skills, /activation) with Stepper component showing progress"
    - "Privacy guarantee messaging is visible on every onboarding step via PrivacyBanner component"
    - "Activation confirmation (step 4) shows success state with 72-hour activation target messaging"
    - "@glimmora/types exports Earning, EarningsSummary, Message, MessageThread, OnboardingStep, OnboardingProgress, StudentProfile, AlumniProfile, GovernorMetrics, CohortTrend, TaskCategory, NotificationPreference, DeviceInfo types"
    - "next-intl is installed and configured in cookie-based mode (without-i18n-routing) in both Women's and University portals"
  artifacts:
    - path: "packages/types/src/earnings.ts"
      provides: "Earning and EarningsSummary types for WP-16, UP-02"
    - path: "packages/types/src/message.ts"
      provides: "Message and MessageThread types for WP-18"
    - path: "packages/types/src/onboarding.ts"
      provides: "OnboardingStep and OnboardingProgress types for WP-04, UP-01"
    - path: "packages/types/src/student.ts"
      provides: "StudentProfile and AlumniProfile types for UP-01, UP-08"
    - path: "packages/types/src/governor.ts"
      provides: "GovernorMetrics, CohortTrend, TaskCategory types for UP-10..12"
    - path: "packages/types/src/notification.ts"
      provides: "NotificationPreference type for WP-21"
    - path: "packages/types/src/device.ts"
      provides: "DeviceInfo type for WP-04 step 2, WP-22"
    - path: "apps/women-portal/src/app/(pre-auth)/page.tsx"
      provides: "Language selector as first interactive element (WP-01)"
    - path: "apps/women-portal/src/app/(pre-auth)/welcome/page.tsx"
      provides: "WhatsApp-style welcome screen (WP-02)"
    - path: "apps/women-portal/src/app/(pre-auth)/register/page.tsx"
      provides: "Email + password registration (WP-03)"
    - path: "apps/women-portal/src/app/(pre-auth)/onboarding/activation/page.tsx"
      provides: "Onboarding step 4 activation confirmation (WP-04)"
  key_links:
    - from: "apps/women-portal/src/i18n/request.ts"
      to: "apps/women-portal/src/messages/*.json"
      via: "next-intl cookie-based locale resolution"
      pattern: "getRequestConfig.*cookies.*NEXT_LOCALE"
    - from: "apps/women-portal/src/app/layout.tsx"
      to: "next-intl"
      via: "NextIntlClientProvider wrapping all children"
      pattern: "NextIntlClientProvider"
    - from: "apps/women-portal/src/components/language-selector/language-selector.tsx"
      to: "apps/women-portal/src/app/layout.tsx"
      via: "Sets NEXT_LOCALE cookie -> layout reads it for lang/dir attributes"
      pattern: "document.cookie.*NEXT_LOCALE"
---

<objective>
Establish the Phase 3 foundation: extend @glimmora/types with 14 new types needed by both portals, install and configure next-intl in cookie-based mode for i18n/RTL support, create MSW auth/onboarding handlers, and build the complete Women's Portal pre-auth flow from language selection through 4-step onboarding to activation.

Purpose: This is the foundation plan for Phase 3. Every subsequent plan depends on the types, i18n infrastructure, auth store, and route group pattern established here. The Women's Portal pre-auth flow is the most persona-critical UX in the platform -- Fatima's trust is built or broken in these first 7 screens.

Output: 14 new type files in @glimmora/types. next-intl configured in both portals. Women's Portal with working language selection -> welcome -> registration -> 4-step onboarding flow, all driven by MSW mock data.
</objective>

<execution_context>
@/Users/kavi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kavi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-womens-portal-university-portal/03-RESEARCH.md

Key codebase references:
@packages/types/src/index.ts
@packages/types/src/user.ts
@packages/types/src/evidence.ts
@packages/types/src/podl.ts
@packages/types/src/skill-genome.ts
@packages/types/src/api.ts
@apps/women-portal/src/app/layout.tsx
@apps/women-portal/src/app/page.tsx
@apps/women-portal/src/components/providers/Providers.tsx
@apps/women-portal/src/components/providers/MSWProvider.tsx
@apps/women-portal/src/lib/msw/handlers.ts
@apps/women-portal/src/lib/msw/browser.ts
@apps/women-portal/src/store/app-store.ts
@apps/university-portal/src/app/layout.tsx
@apps/university-portal/next.config.ts

CRITICAL privacy constraints:
- No public profiles, no leaderboards, no peer comparison -- EVER
- Language selection MUST be FIRST interactive element on Women's Portal
- Privacy guarantee messaging visible throughout onboarding
- 72-hour activation target is emotional messaging, not operational SLA
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend @glimmora/types with 14 new types + install next-intl in both portals</name>
  <files>
    packages/types/src/earnings.ts
    packages/types/src/message.ts
    packages/types/src/onboarding.ts
    packages/types/src/student.ts
    packages/types/src/governor.ts
    packages/types/src/notification.ts
    packages/types/src/device.ts
    packages/types/src/index.ts
    apps/women-portal/package.json
    apps/women-portal/next.config.ts
    apps/women-portal/src/i18n/request.ts
    apps/women-portal/src/messages/en.json
    apps/women-portal/src/messages/ur.json
    apps/women-portal/src/messages/ar.json
    apps/women-portal/src/app/layout.tsx
    apps/women-portal/src/app/globals.css
    apps/university-portal/package.json
    apps/university-portal/next.config.ts
    apps/university-portal/src/i18n/request.ts
    apps/university-portal/src/messages/en.json
    pnpm-lock.yaml
  </files>
  <action>
**Step 1: Create 7 new type files in @glimmora/types.**

**packages/types/src/earnings.ts:**
```typescript
export type EarningsStatus = 'pending' | 'released' | 'withdrawn'

export interface Earning {
  id: string
  taskId: string
  projectId: string
  amount: number
  currency: string
  status: EarningsStatus
  earnedAt: string
  releasedAt?: string
  withdrawnAt?: string
}

export interface EarningsSummary {
  pending: number
  released: number
  total: number
  currency: string
  withdrawalHistory: Earning[]
}
```

**packages/types/src/message.ts:**
```typescript
export type MessageRole = 'contributor' | 'support-lead'

export interface Message {
  id: string
  threadId: string
  senderRole: MessageRole
  content: string
  sentAt: string
  readAt?: string
}

export interface MessageThread {
  id: string
  contributorId: string
  supportLeadId: string
  subject: string
  lastMessageAt: string
  unreadCount: number
  messages: Message[]
}
```

**packages/types/src/onboarding.ts:**
```typescript
export type OnboardingStepStatus = 'pending' | 'in-progress' | 'completed' | 'skipped'

export interface OnboardingStep {
  id: string
  stepNumber: number
  label: string
  status: OnboardingStepStatus
  completedAt?: string
}

export interface OnboardingProgress {
  userId: string
  steps: OnboardingStep[]
  currentStep: number
  isComplete: boolean
}
```

**packages/types/src/student.ts:**
```typescript
import type { UserRole, ContributorProfile } from './user'

export interface StudentProfile extends ContributorProfile {
  role: Extract<UserRole, 'student-contributor'>
  universityEmail: string
  studentId: string
  universityName: string
  academicYear: string
  degreeProgram: string
  graduationYear?: string
  emailVerified: boolean
}

export interface AlumniProfile extends ContributorProfile {
  role: Extract<UserRole, 'alumni-contributor'>
  universityName: string
  degreeProgram: string
  graduationYear: string
  careerContext?: string
  reactivatedAt?: string
  previousPoDLCount: number
}
```

**packages/types/src/governor.ts:**
```typescript
export interface GovernorMetrics {
  institutionId: string
  activeContributors: number
  totalTasksCompleted: number
  averageCompletionRate: number
  totalPoDLsIssued: number
  averageSkillGrowth: number
  periodStart: string
  periodEnd: string
}

export interface CohortTrend {
  cohortId: string
  cohortLabel: string
  totalContributors: number
  activePercentage: number
  completionRate: number
  topSkillCategories: string[]
  averageEarnings: number
}

export interface TaskCategory {
  id: string
  name: string
  description: string
  isActive: boolean
  taskCount: number
  institutionId: string
}
```

**packages/types/src/notification.ts:**
```typescript
export interface NotificationPreference {
  channel: 'in-app' | 'email' | 'whatsapp'
  category: 'task-updates' | 'payment' | 'community' | 'system'
  enabled: boolean
}
```

**packages/types/src/device.ts:**
```typescript
export interface DeviceInfo {
  primaryDevice: 'desktop' | 'laptop' | 'tablet' | 'smartphone'
  operatingSystem: string
  internetSpeed: 'slow' | 'moderate' | 'fast'
  hasWebcam: boolean
  availableHoursPerWeek: number
}
```

**Update packages/types/src/index.ts** -- add exports for all 7 new files AFTER existing exports:
```typescript
export type { EarningsStatus, Earning, EarningsSummary } from './earnings'
export type { MessageRole, Message, MessageThread } from './message'
export type { OnboardingStepStatus, OnboardingStep, OnboardingProgress } from './onboarding'
export type { StudentProfile, AlumniProfile } from './student'
export type { GovernorMetrics, CohortTrend, TaskCategory } from './governor'
export type { NotificationPreference } from './notification'
export type { DeviceInfo } from './device'
```

**Step 2: Install next-intl in both portals.**

```bash
pnpm add next-intl --filter women-portal --filter university-portal
```

**Step 3: Configure next-intl for Women's Portal.**

**apps/women-portal/src/i18n/request.ts:**
```typescript
import { cookies } from 'next/headers'
import { getRequestConfig } from 'next-intl/server'

export default getRequestConfig(async () => {
  const store = await cookies()
  const locale = store.get('NEXT_LOCALE')?.value || 'en'

  return {
    locale,
    messages: (await import(`../../messages/${locale}.json`)).default,
  }
})
```

**Update apps/women-portal/next.config.ts** to wrap with next-intl plugin:
```typescript
import type { NextConfig } from 'next'
import createNextIntlPlugin from 'next-intl/plugin'

const nextConfig: NextConfig = {
  transpilePackages: ['@glimmora/ui', '@glimmora/types', '@glimmora/config'],
}

const withNextIntl = createNextIntlPlugin()
export default withNextIntl(nextConfig)
```

**apps/women-portal/src/messages/en.json** -- complete English translation file covering pre-auth + onboarding:
```json
{
  "language": {
    "title": "Choose your language",
    "subtitle": "You can change this anytime in settings"
  },
  "welcome": {
    "greeting": "Welcome to GlimmoraTeam",
    "message1": "We're glad you're here. This is a safe space for skilled women to contribute to real projects and earn on their own terms.",
    "message2": "Your identity is always protected. No public profiles, no leaderboards -- just your skills and your work.",
    "message3": "Ready to get started? It takes about 5 minutes.",
    "cta": "Get Started"
  },
  "register": {
    "title": "Create your account",
    "subtitle": "Your information is private and never shared publicly",
    "emailLabel": "Email address",
    "emailPlaceholder": "you@example.com",
    "passwordLabel": "Password",
    "passwordPlaceholder": "At least 8 characters",
    "confirmPasswordLabel": "Confirm password",
    "confirmPasswordPlaceholder": "Re-enter your password",
    "submitButton": "Create Account",
    "loginLink": "Already have an account? Sign in",
    "privacyNote": "We never share your email with anyone"
  },
  "onboarding": {
    "step1Label": "Profile",
    "step1Description": "Basic info",
    "step2Label": "Devices",
    "step2Description": "Your setup",
    "step3Label": "Skills",
    "step3Description": "What you know",
    "step4Label": "Activation",
    "step4Description": "Welcome!",
    "profile": {
      "title": "Tell us a bit about yourself",
      "displayNameLabel": "What should we call you?",
      "displayNamePlaceholder": "A nickname or first name",
      "displayNameHint": "This is private -- only you and your support lead will see it",
      "cityLabel": "City",
      "cityPlaceholder": "Where are you based?",
      "timezoneLabel": "Timezone",
      "nextButton": "Continue"
    },
    "devices": {
      "title": "Your setup",
      "subtitle": "This helps us match you with tasks suited to your equipment",
      "primaryDeviceLabel": "Primary device",
      "osLabel": "Operating system",
      "internetLabel": "Internet connection speed",
      "webcamLabel": "Do you have a webcam?",
      "hoursLabel": "Hours available per week",
      "nextButton": "Continue"
    },
    "skills": {
      "title": "What skills do you have?",
      "subtitle": "Select all that apply -- don't worry, you can update these later",
      "searchPlaceholder": "Search skills...",
      "selectedCount": "{count} skills selected",
      "nextButton": "Continue"
    },
    "activation": {
      "title": "You're all set!",
      "subtitle": "Your profile is being reviewed",
      "message": "We aim to activate your account within 72 hours. You'll receive an email when you're ready to start.",
      "privacyReminder": "Remember: your identity is always protected. No public profiles, no leaderboards.",
      "dashboardButton": "Go to Dashboard"
    }
  },
  "privacy": {
    "banner": "Your privacy is protected. No public profiles. No leaderboards. No peer comparison -- ever."
  },
  "common": {
    "back": "Back",
    "next": "Next",
    "skip": "Skip",
    "save": "Save",
    "cancel": "Cancel",
    "loading": "Loading..."
  }
}
```

**apps/women-portal/src/messages/ur.json** -- Urdu placeholder translations for representative screens (proves RTL works):
```json
{
  "language": {
    "title": "اپنی زبان منتخب کریں",
    "subtitle": "آپ اسے کسی بھی وقت سیٹنگز میں تبدیل کر سکتے ہیں"
  },
  "welcome": {
    "greeting": "گلیمورا ٹیم میں خوش آمدید",
    "message1": "ہمیں خوشی ہے کہ آپ یہاں ہیں۔ یہ ہنر مند خواتین کے لیے ایک محفوظ جگہ ہے۔",
    "message2": "آپ کی شناخت ہمیشہ محفوظ رہتی ہے۔ کوئی عوامی پروفائل نہیں، کوئی لیڈر بورڈ نہیں۔",
    "message3": "شروع کرنے کے لیے تیار ہیں؟ اس میں تقریباً 5 منٹ لگتے ہیں۔",
    "cta": "شروع کریں"
  },
  "register": {
    "title": "اپنا اکاؤنٹ بنائیں",
    "subtitle": "آپ کی معلومات نجی ہیں اور کبھی عوامی طور پر شیئر نہیں کی جاتیں",
    "emailLabel": "ای میل ایڈریس",
    "emailPlaceholder": "you@example.com",
    "passwordLabel": "پاس ورڈ",
    "passwordPlaceholder": "کم از کم 8 حروف",
    "confirmPasswordLabel": "پاس ورڈ کی تصدیق کریں",
    "confirmPasswordPlaceholder": "اپنا پاس ورڈ دوبارہ درج کریں",
    "submitButton": "اکاؤنٹ بنائیں",
    "loginLink": "پہلے سے اکاؤنٹ ہے؟ سائن ان کریں",
    "privacyNote": "ہم آپ کا ای میل کسی کے ساتھ شیئر نہیں کرتے"
  },
  "onboarding": {
    "step1Label": "پروفائل",
    "step1Description": "بنیادی معلومات",
    "step2Label": "آلات",
    "step2Description": "آپ کا سیٹ اپ",
    "step3Label": "مہارتیں",
    "step3Description": "آپ کیا جانتے ہیں",
    "step4Label": "ایکٹیویشن",
    "step4Description": "خوش آمدید!",
    "profile": {
      "title": "اپنے بارے میں تھوڑا بتائیں",
      "displayNameLabel": "ہم آپ کو کیا کہیں؟",
      "displayNamePlaceholder": "ایک عرفی نام یا پہلا نام",
      "displayNameHint": "یہ نجی ہے -- صرف آپ اور آپ کی سپورٹ لیڈ اسے دیکھیں گی",
      "cityLabel": "شہر",
      "cityPlaceholder": "آپ کہاں مقیم ہیں؟",
      "timezoneLabel": "ٹائم زون",
      "nextButton": "جاری رکھیں"
    },
    "devices": {
      "title": "آپ کا سیٹ اپ",
      "subtitle": "یہ ہمیں آپ کے آلات کے مطابق کام تفویض کرنے میں مدد کرتا ہے",
      "primaryDeviceLabel": "بنیادی آلہ",
      "osLabel": "آپریٹنگ سسٹم",
      "internetLabel": "انٹرنیٹ کنکشن کی رفتار",
      "webcamLabel": "کیا آپ کے پاس ویب کیم ہے؟",
      "hoursLabel": "فی ہفتہ دستیاب گھنٹے",
      "nextButton": "جاری رکھیں"
    },
    "skills": {
      "title": "آپ کے پاس کون سی مہارتیں ہیں؟",
      "subtitle": "تمام متعلقہ منتخب کریں -- فکر نہ کریں، آپ بعد میں ان کو اپ ڈیٹ کر سکتے ہیں",
      "searchPlaceholder": "مہارتیں تلاش کریں...",
      "selectedCount": "{count} مہارتیں منتخب",
      "nextButton": "جاری رکھیں"
    },
    "activation": {
      "title": "آپ تیار ہیں!",
      "subtitle": "آپ کے پروفائل کا جائزہ لیا جا رہا ہے",
      "message": "ہم 72 گھنٹوں کے اندر آپ کا اکاؤنٹ فعال کرنے کا ارادہ رکھتے ہیں۔ جب آپ شروع کرنے کے لیے تیار ہوں گے تو آپ کو ای میل ملے گی۔",
      "privacyReminder": "یاد رکھیں: آپ کی شناخت ہمیشہ محفوظ ہے۔ کوئی عوامی پروفائل نہیں، کوئی لیڈر بورڈ نہیں۔",
      "dashboardButton": "ڈیش بورڈ پر جائیں"
    }
  },
  "privacy": {
    "banner": "آپ کی رازداری محفوظ ہے۔ کوئی عوامی پروفائل نہیں۔ کوئی لیڈر بورڈ نہیں۔ کوئی موازنہ نہیں -- کبھی بھی۔"
  },
  "common": {
    "back": "واپس",
    "next": "اگلا",
    "skip": "چھوڑیں",
    "save": "محفوظ کریں",
    "cancel": "منسوخ کریں",
    "loading": "لوڈ ہو رہا ہے..."
  }
}
```

**apps/women-portal/src/messages/ar.json** -- Arabic placeholder with same structure:
```json
{
  "language": {
    "title": "اختر لغتك",
    "subtitle": "يمكنك تغيير هذا في أي وقت من الإعدادات"
  },
  "welcome": {
    "greeting": "مرحباً بك في غليمورا تيم",
    "message1": "نحن سعداء بوجودك هنا. هذا مكان آمن للنساء الماهرات للمساهمة في مشاريع حقيقية.",
    "message2": "هويتك محمية دائماً. لا ملفات شخصية عامة، لا لوحات متصدرين.",
    "message3": "مستعدة للبدء؟ يستغرق الأمر حوالي 5 دقائق.",
    "cta": "ابدأي"
  },
  "register": {
    "title": "أنشئي حسابك",
    "subtitle": "معلوماتك خاصة ولا تُشارك علنياً أبداً",
    "emailLabel": "البريد الإلكتروني",
    "emailPlaceholder": "you@example.com",
    "passwordLabel": "كلمة المرور",
    "passwordPlaceholder": "8 أحرف على الأقل",
    "confirmPasswordLabel": "تأكيد كلمة المرور",
    "confirmPasswordPlaceholder": "أعيدي إدخال كلمة المرور",
    "submitButton": "إنشاء الحساب",
    "loginLink": "لديك حساب بالفعل؟ سجلي الدخول",
    "privacyNote": "لا نشارك بريدك الإلكتروني مع أحد أبداً"
  },
  "onboarding": {
    "step1Label": "الملف الشخصي",
    "step1Description": "معلومات أساسية",
    "step2Label": "الأجهزة",
    "step2Description": "إعداداتك",
    "step3Label": "المهارات",
    "step3Description": "ماذا تعرفين",
    "step4Label": "التفعيل",
    "step4Description": "مرحباً!",
    "profile": {
      "title": "أخبرينا قليلاً عن نفسك",
      "displayNameLabel": "ماذا نناديك؟",
      "displayNamePlaceholder": "لقب أو اسم أول",
      "displayNameHint": "هذا خاص -- أنتِ وقائدة الدعم فقط ستريانه",
      "cityLabel": "المدينة",
      "cityPlaceholder": "أين تقيمين؟",
      "timezoneLabel": "المنطقة الزمنية",
      "nextButton": "متابعة"
    },
    "devices": {
      "title": "إعداداتك",
      "subtitle": "هذا يساعدنا في مطابقتك مع المهام المناسبة لأجهزتك",
      "primaryDeviceLabel": "الجهاز الأساسي",
      "osLabel": "نظام التشغيل",
      "internetLabel": "سرعة الإنترنت",
      "webcamLabel": "هل لديك كاميرا ويب؟",
      "hoursLabel": "الساعات المتاحة أسبوعياً",
      "nextButton": "متابعة"
    },
    "skills": {
      "title": "ما المهارات التي لديك؟",
      "subtitle": "اختاري كل ما ينطبق -- لا تقلقي، يمكنك تحديثها لاحقاً",
      "searchPlaceholder": "ابحثي عن المهارات...",
      "selectedCount": "{count} مهارات مختارة",
      "nextButton": "متابعة"
    },
    "activation": {
      "title": "أنتِ جاهزة!",
      "subtitle": "ملفك الشخصي قيد المراجعة",
      "message": "نهدف إلى تفعيل حسابك خلال 72 ساعة. ستتلقين بريداً إلكترونياً عندما تكونين مستعدة للبدء.",
      "privacyReminder": "تذكري: هويتك محمية دائماً. لا ملفات شخصية عامة، لا لوحات متصدرين.",
      "dashboardButton": "الذهاب إلى لوحة التحكم"
    }
  },
  "privacy": {
    "banner": "خصوصيتك محمية. لا ملفات شخصية عامة. لا لوحات متصدرين. لا مقارنة -- أبداً."
  },
  "common": {
    "back": "رجوع",
    "next": "التالي",
    "skip": "تخطي",
    "save": "حفظ",
    "cancel": "إلغاء",
    "loading": "جاري التحميل..."
  }
}
```

**Step 4: Update Women's Portal root layout for i18n + RTL.**

Replace the existing `apps/women-portal/src/app/layout.tsx` with:
```typescript
import type { Metadata } from 'next'
import { NextIntlClientProvider } from 'next-intl'
import { getLocale, getMessages } from 'next-intl/server'
import { Providers } from '@/components/providers/Providers'
import './globals.css'

const RTL_LOCALES = ['ur', 'ar']

export const metadata: Metadata = {
  title: "Women's Portal | GlimmoraTeam",
  description: 'GlimmoraTeam contributor portal for women',
}

export default async function RootLayout({ children }: { children: React.ReactNode }) {
  const locale = await getLocale()
  const messages = await getMessages()
  const dir = RTL_LOCALES.includes(locale) ? 'rtl' : 'ltr'

  return (
    <html lang={locale} dir={dir}>
      <body className="font-body bg-bg-app text-text-body">
        <NextIntlClientProvider messages={messages}>
          <Providers>{children}</Providers>
        </NextIntlClientProvider>
      </body>
    </html>
  )
}
```

**Step 5: Add Noto Sans Arabic font import to globals.css.**

Append to the existing `apps/women-portal/src/app/globals.css`:
```css
/* Noto Sans Arabic for RTL (Urdu/Arabic) support */
@import url('https://fonts.googleapis.com/css2?family=Noto+Sans+Arabic:wght@300;400;500;700&display=swap');

:root[dir="rtl"] {
  --font-body: 'Noto Sans Arabic', system-ui, sans-serif;
}
```

**Step 6: Configure next-intl for University Portal (minimal -- no RTL needed for now).**

**apps/university-portal/src/i18n/request.ts:**
```typescript
import { getRequestConfig } from 'next-intl/server'

export default getRequestConfig(async () => {
  return {
    locale: 'en',
    messages: (await import('../../messages/en.json')).default,
  }
})
```

**apps/university-portal/src/messages/en.json:**
```json
{
  "common": {
    "back": "Back",
    "next": "Next",
    "save": "Save",
    "cancel": "Cancel",
    "loading": "Loading..."
  }
}
```

Update `apps/university-portal/next.config.ts` with same next-intl plugin pattern as Women's Portal.

Update `apps/university-portal/src/app/layout.tsx` to add `NextIntlClientProvider` wrapping (same pattern as Women's Portal but hardcoded to `lang="en" dir="ltr"`).

**Step 7: Verify types build and next-intl installs.**

```bash
pnpm --filter @glimmora/types type-check
pnpm turbo build --filter=women-portal --filter=university-portal
```
  </action>
  <verify>
Run `pnpm --filter @glimmora/types type-check` -- zero errors.
Run `pnpm turbo build --filter=women-portal` -- builds without errors.
Run `pnpm turbo build --filter=university-portal` -- builds without errors.
Run `ls packages/types/src/{earnings,message,onboarding,student,governor,notification,device}.ts | wc -l` -- must be 7.
Verify `grep "next-intl" apps/women-portal/package.json` -- must match.
Verify `grep "next-intl" apps/university-portal/package.json` -- must match.
  </verify>
  <done>7 new type files (14 types) added to @glimmora/types. next-intl installed and configured in cookie-based mode for Women's Portal (with RTL support) and University Portal (English only). Translation files for en/ur/ar created. Root layout updated with NextIntlClientProvider and dynamic lang/dir attributes. Noto Sans Arabic font loaded for RTL.</done>
</task>

<task type="auto">
  <name>Task 2: Build Women's Portal pre-auth flow (language selector, welcome, registration) + stores + MSW auth handlers</name>
  <files>
    apps/women-portal/src/app/page.tsx
    apps/women-portal/src/app/(pre-auth)/layout.tsx
    apps/women-portal/src/app/(pre-auth)/page.tsx
    apps/women-portal/src/app/(pre-auth)/welcome/page.tsx
    apps/women-portal/src/app/(pre-auth)/register/page.tsx
    apps/women-portal/src/components/language-selector/language-selector.tsx
    apps/women-portal/src/components/language-selector/index.ts
    apps/women-portal/src/components/welcome-screen/welcome-screen.tsx
    apps/women-portal/src/components/welcome-screen/index.ts
    apps/women-portal/src/components/registration-form/registration-form.tsx
    apps/women-portal/src/components/registration-form/index.ts
    apps/women-portal/src/store/auth-store.ts
    apps/women-portal/src/store/language-store.ts
    apps/women-portal/src/lib/msw/handlers/auth.ts
    apps/women-portal/src/lib/msw/handlers/index.ts
    apps/women-portal/src/lib/msw/handlers.ts
    apps/women-portal/src/lib/msw/factories/user.ts
    apps/women-portal/src/lib/msw/factories/common.ts
  </files>
  <action>
**Step 1: Update the root page.tsx to redirect to (pre-auth).**

The existing `apps/women-portal/src/app/page.tsx` is a canary page. Replace it with a redirect to the pre-auth language selector:
```typescript
import { redirect } from 'next/navigation'

export default function RootPage() {
  // Root URL redirects to the language selector (first interactive element)
  redirect('/')
  // The (pre-auth)/page.tsx handles the actual language selector
  // This file exists only because Next.js requires a root page.tsx
}
```

Actually, since (pre-auth)/page.tsx IS at the root URL (route groups don't add segments), the root page.tsx and (pre-auth)/page.tsx would conflict. Instead:
- Delete the existing `apps/women-portal/src/app/page.tsx` (or rename it)
- The `(pre-auth)/page.tsx` will serve at `/` (the root URL)

So: Remove `apps/women-portal/src/app/page.tsx` and the `canary/` directory. The `(pre-auth)/page.tsx` will be the root.

**Step 2: Create pre-auth layout.**

**apps/women-portal/src/app/(pre-auth)/layout.tsx:**
```typescript
export default function PreAuthLayout({ children }: { children: React.ReactNode }) {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center bg-bg-app px-4">
      <div className="w-full max-w-md">
        {children}
      </div>
    </main>
  )
}
```

**Step 3: Create LanguageSelector component.**

**apps/women-portal/src/components/language-selector/language-selector.tsx:**
```typescript
'use client'
import { useRouter } from 'next/navigation'

interface Language {
  code: string
  nativeLabel: string
  englishLabel: string
  dir: 'ltr' | 'rtl'
}

const SUPPORTED_LANGUAGES: Language[] = [
  { code: 'en', nativeLabel: 'English', englishLabel: 'English', dir: 'ltr' },
  { code: 'ur', nativeLabel: 'اردو', englishLabel: 'Urdu', dir: 'rtl' },
  { code: 'ar', nativeLabel: 'العربية', englishLabel: 'Arabic', dir: 'rtl' },
]

export function LanguageSelector() {
  const router = useRouter()

  function handleSelect(code: string) {
    document.cookie = `NEXT_LOCALE=${code};path=/;max-age=${365 * 24 * 60 * 60};SameSite=Lax`
    router.refresh()
    router.push('/welcome')
  }

  return (
    <div className="flex flex-col items-center gap-6">
      <div className="text-center mb-4">
        <h1 className="font-display text-3xl font-semibold text-text-heading mb-2">
          GlimmoraTeam
        </h1>
        {/* Show "Choose your language" in ALL three languages simultaneously */}
        <p className="text-sm text-text-caption font-body">Choose your language</p>
        <p className="text-sm text-text-caption font-body" dir="rtl">اپنی زبان منتخب کریں</p>
        <p className="text-sm text-text-caption font-body" dir="rtl">اختر لغتك</p>
      </div>

      <div className="flex flex-col gap-3 w-full">
        {SUPPORTED_LANGUAGES.map((lang) => (
          <button
            key={lang.code}
            onClick={() => handleSelect(lang.code)}
            dir={lang.dir}
            className="w-full rounded-card border border-border bg-bg-card p-4 text-center shadow-card hover:shadow-card-hover hover:border-brand-primary/50 transition-all duration-200"
          >
            <span className="block text-2xl font-display text-text-heading">
              {lang.nativeLabel}
            </span>
            {lang.code !== 'en' && (
              <span className="block text-sm font-body text-text-caption mt-1">
                {lang.englishLabel}
              </span>
            )}
          </button>
        ))}
      </div>
    </div>
  )
}
```

Create `apps/women-portal/src/components/language-selector/index.ts`:
```typescript
export { LanguageSelector } from './language-selector'
```

**Step 4: Create Language Selector page.**

**apps/women-portal/src/app/(pre-auth)/page.tsx:**
```typescript
import { LanguageSelector } from '@/components/language-selector'

export default function LanguageSelectPage() {
  return <LanguageSelector />
}
```

**Step 5: Create WhatsApp-style Welcome Screen.**

**apps/women-portal/src/components/welcome-screen/welcome-screen.tsx:**
```typescript
'use client'
import { useTranslations } from 'next-intl'
import { useRouter } from 'next/navigation'
import { Button } from '@glimmora/ui'

export function WelcomeScreen() {
  const t = useTranslations('welcome')
  const router = useRouter()

  const messages = [
    { text: t('message1'), delay: 0 },
    { text: t('message2'), delay: 1 },
    { text: t('message3'), delay: 2 },
  ]

  return (
    <div className="flex flex-col gap-6">
      {/* GlimmoraTeam branding */}
      <div className="text-center mb-2">
        <div className="mx-auto mb-4 flex h-16 w-16 items-center justify-center rounded-full bg-brand-primary/10">
          <span className="text-2xl font-display font-bold text-brand-primary">G</span>
        </div>
        <h1 className="font-display text-2xl font-semibold text-text-heading">
          {t('greeting')}
        </h1>
      </div>

      {/* Chat-bubble style messages -- conversational, NOT a WhatsApp clone */}
      <div className="flex flex-col gap-3">
        {messages.map((msg, i) => (
          <div
            key={i}
            className="rounded-card rounded-tl-none bg-bg-card border border-border p-4 shadow-card max-w-[90%]"
          >
            <p className="text-sm font-body text-text-body leading-relaxed">
              {msg.text}
            </p>
          </div>
        ))}
      </div>

      {/* CTA */}
      <Button
        variant="primary"
        size="lg"
        className="w-full mt-4"
        onClick={() => router.push('/register')}
      >
        {t('cta')}
      </Button>
    </div>
  )
}
```

Create index.ts re-export.

**Step 6: Create Welcome page.**

**apps/women-portal/src/app/(pre-auth)/welcome/page.tsx:**
```typescript
import { WelcomeScreen } from '@/components/welcome-screen'

export default function WelcomePage() {
  return <WelcomeScreen />
}
```

**Step 7: Create Registration Form + Page.**

**apps/women-portal/src/components/registration-form/registration-form.tsx:**
```typescript
'use client'
import { useState } from 'react'
import { useTranslations } from 'next-intl'
import { useRouter } from 'next/navigation'
import { Button, TextInput } from '@glimmora/ui'
import { useAuthStore } from '@/store/auth-store'
import Link from 'next/link'

export function RegistrationForm() {
  const t = useTranslations('register')
  const tPrivacy = useTranslations('privacy')
  const router = useRouter()
  const setUser = useAuthStore((s) => s.setUser)

  const [email, setEmail] = useState('')
  const [password, setPassword] = useState('')
  const [confirmPassword, setConfirmPassword] = useState('')
  const [error, setError] = useState<string | null>(null)
  const [isLoading, setIsLoading] = useState(false)

  async function handleSubmit(e: React.FormEvent) {
    e.preventDefault()
    setError(null)

    if (password !== confirmPassword) {
      setError('Passwords do not match')
      return
    }
    if (password.length < 8) {
      setError('Password must be at least 8 characters')
      return
    }

    setIsLoading(true)
    try {
      const res = await fetch('/api/auth/register', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ email, password }),
      })
      const data = await res.json()
      if (!res.ok) throw new Error(data.error?.message || 'Registration failed')

      setUser(data.data)
      router.push('/onboarding/profile')
    } catch (err) {
      setError(err instanceof Error ? err.message : 'Something went wrong')
    } finally {
      setIsLoading(false)
    }
  }

  return (
    <form onSubmit={handleSubmit} className="flex flex-col gap-5">
      <div className="text-center mb-2">
        <h1 className="font-display text-2xl font-semibold text-text-heading">
          {t('title')}
        </h1>
        <p className="text-sm font-body text-text-caption mt-1">{t('subtitle')}</p>
      </div>

      {/* Privacy banner */}
      <div className="flex items-start gap-2 rounded-inner bg-brand-primary/5 border border-brand-primary/20 p-3">
        <svg className="h-4 w-4 text-brand-primary mt-0.5 shrink-0" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/></svg>
        <p className="text-xs font-body text-text-caption">{t('privacyNote')}</p>
      </div>

      <TextInput
        label={t('emailLabel')}
        type="email"
        placeholder={t('emailPlaceholder')}
        value={email}
        onChange={(e) => setEmail(e.target.value)}
        required
      />

      <TextInput
        label={t('passwordLabel')}
        type="password"
        placeholder={t('passwordPlaceholder')}
        value={password}
        onChange={(e) => setPassword(e.target.value)}
        required
      />

      <TextInput
        label={t('confirmPasswordLabel')}
        type="password"
        placeholder={t('confirmPasswordPlaceholder')}
        value={confirmPassword}
        onChange={(e) => setConfirmPassword(e.target.value)}
        required
      />

      {error && (
        <p className="text-sm text-status-urgent font-body">{error}</p>
      )}

      <Button type="submit" variant="primary" size="lg" className="w-full" disabled={isLoading}>
        {isLoading ? '...' : t('submitButton')}
      </Button>

      <p className="text-center text-sm font-body text-text-caption">
        <Link href="/" className="text-brand-primary hover:underline">{t('loginLink')}</Link>
      </p>
    </form>
  )
}
```

Create index.ts re-export.

**apps/women-portal/src/app/(pre-auth)/register/page.tsx:**
```typescript
import { RegistrationForm } from '@/components/registration-form'

export default function RegisterPage() {
  return <RegistrationForm />
}
```

**Step 8: Create Zustand stores.**

**apps/women-portal/src/store/auth-store.ts:**
```typescript
import { create } from 'zustand'
import { persist, createJSONStorage } from 'zustand/middleware'
import type { ContributorProfile } from '@glimmora/types'

interface AuthState {
  user: ContributorProfile | null
  isAuthenticated: boolean
  onboardingStep: number
  onboardingCompleted: boolean

  setUser: (user: ContributorProfile) => void
  setOnboardingStep: (step: number) => void
  completeOnboarding: () => void
  logout: () => void
}

export const useAuthStore = create<AuthState>()(
  persist(
    (set) => ({
      user: null,
      isAuthenticated: false,
      onboardingStep: 0,
      onboardingCompleted: false,

      setUser: (user) => set({ user, isAuthenticated: true }),
      setOnboardingStep: (step) => set({ onboardingStep: step }),
      completeOnboarding: () => set({ onboardingCompleted: true, onboardingStep: 4 }),
      logout: () => set({ user: null, isAuthenticated: false, onboardingStep: 0, onboardingCompleted: false }),
    }),
    {
      name: 'glimmora-women-auth',
      storage: createJSONStorage(() => localStorage),
    }
  )
)
```

**apps/women-portal/src/store/language-store.ts:**
```typescript
import { create } from 'zustand'

interface LanguageState {
  locale: string
  setLocale: (locale: string) => void
}

export const useLanguageStore = create<LanguageState>()((set) => ({
  locale: 'en',
  setLocale: (locale) => set({ locale }),
}))
```

**Step 9: Create MSW factories and auth handlers.**

**apps/women-portal/src/lib/msw/factories/common.ts:**
```typescript
export function randomId(prefix: string): string {
  return `${prefix}-${Math.random().toString(36).substring(2, 9)}`
}

export function isoNow(): string {
  return new Date().toISOString()
}
```

**apps/women-portal/src/lib/msw/factories/user.ts:**
```typescript
import type { ContributorProfile } from '@glimmora/types'
import { randomId, isoNow } from './common'

let userCounter = 1

export function createMockUser(overrides: Partial<ContributorProfile> = {}): ContributorProfile {
  const id = `user-${String(userCounter++).padStart(3, '0')}`
  return {
    id,
    role: 'woman-contributor',
    displayName: 'Fatima',
    email: 'fatima@example.com',
    isActive: true,
    tier: 'emerging',
    anonymousId: randomId('anon'),
    languagePreference: 'en',
    onboardingCompleted: false,
    createdAt: isoNow(),
    updatedAt: isoNow(),
    ...overrides,
  }
}
```

**apps/women-portal/src/lib/msw/handlers/auth.ts:**
```typescript
import { http, HttpResponse } from 'msw'
import { createMockUser } from '../factories/user'

export const authHandlers = [
  http.post('/api/auth/register', async ({ request }) => {
    const body = await request.json() as { email: string; password: string }
    const user = createMockUser({
      email: body.email,
      onboardingCompleted: false,
    })
    return HttpResponse.json({ data: user })
  }),

  http.post('/api/auth/login', async ({ request }) => {
    const body = await request.json() as { email: string; password: string }
    const user = createMockUser({
      email: body.email,
      onboardingCompleted: true,
    })
    return HttpResponse.json({ data: user })
  }),
]
```

**apps/women-portal/src/lib/msw/handlers/onboarding.ts:**
```typescript
import { http, HttpResponse } from 'msw'
import type { OnboardingProgress } from '@glimmora/types'

const mockProgress: OnboardingProgress = {
  userId: 'user-001',
  steps: [
    { id: 'step-1', stepNumber: 1, label: 'Profile', status: 'pending' },
    { id: 'step-2', stepNumber: 2, label: 'Devices', status: 'pending' },
    { id: 'step-3', stepNumber: 3, label: 'Skills', status: 'pending' },
    { id: 'step-4', stepNumber: 4, label: 'Activation', status: 'pending' },
  ],
  currentStep: 0,
  isComplete: false,
}

export const onboardingHandlers = [
  http.get('/api/onboarding/progress', () => {
    return HttpResponse.json({ data: mockProgress })
  }),

  http.put('/api/onboarding/progress', async ({ request }) => {
    const body = await request.json() as Partial<OnboardingProgress>
    Object.assign(mockProgress, body)
    return HttpResponse.json({ data: mockProgress })
  }),
]
```

**apps/women-portal/src/lib/msw/handlers/index.ts:**
```typescript
import { authHandlers } from './auth'
import { onboardingHandlers } from './onboarding'

export const handlers = [
  ...authHandlers,
  ...onboardingHandlers,
]
```

**Update apps/women-portal/src/lib/msw/handlers.ts** to re-export from the new handlers/index.ts:
```typescript
export { handlers } from './handlers/index'
```
  </action>
  <verify>
Run `pnpm turbo build --filter=women-portal` -- builds without errors.
Navigate to http://localhost:3001 and confirm language selector renders as the first screen.
Verify `ls apps/women-portal/src/components/{language-selector,welcome-screen,registration-form}/` -- all 3 exist with 2 files each.
Verify `ls apps/women-portal/src/store/{auth-store,language-store}.ts` -- both exist.
Verify `ls apps/women-portal/src/lib/msw/handlers/{auth,onboarding,index}.ts` -- all exist.
**WP-03 no phone/social login:** `grep -i "phone\|tel\|oauth\|google\|facebook\|social" apps/women-portal/src/components/registration-form/registration-form.tsx` -- must return ZERO matches.
**WP-01 root page conflict:** Confirm `apps/women-portal/src/app/page.tsx` either does not exist OR redirects to the language selector (not old canary content). The `(pre-auth)/page.tsx` must serve the root URL.
  </verify>
  <done>Language selector at root URL, WhatsApp-style welcome screen, registration form with MSW mock auth, Zustand auth/language stores, MSW handler architecture with factories -- all wired together. Language selection sets cookie -> layout reads it -> RTL/LTR switches correctly.</done>
</task>

<task type="auto">
  <name>Task 3: Build 4-step onboarding wizard with privacy banners</name>
  <files>
    apps/women-portal/src/app/(pre-auth)/onboarding/layout.tsx
    apps/women-portal/src/app/(pre-auth)/onboarding/profile/page.tsx
    apps/women-portal/src/app/(pre-auth)/onboarding/devices/page.tsx
    apps/women-portal/src/app/(pre-auth)/onboarding/skills/page.tsx
    apps/women-portal/src/app/(pre-auth)/onboarding/activation/page.tsx
    apps/women-portal/src/components/onboarding/profile-step.tsx
    apps/women-portal/src/components/onboarding/devices-step.tsx
    apps/women-portal/src/components/onboarding/skills-step.tsx
    apps/women-portal/src/components/onboarding/activation-step.tsx
    apps/women-portal/src/components/onboarding/privacy-banner.tsx
    apps/women-portal/src/components/onboarding/index.ts
  </files>
  <action>
**Step 1: Create onboarding layout with Stepper.**

**apps/women-portal/src/app/(pre-auth)/onboarding/layout.tsx:**
```typescript
'use client'
import { usePathname } from 'next/navigation'
import { useTranslations } from 'next-intl'
import { Stepper } from '@glimmora/ui'

const STEP_PATHS = ['/onboarding/profile', '/onboarding/devices', '/onboarding/skills', '/onboarding/activation']

export default function OnboardingLayout({ children }: { children: React.ReactNode }) {
  const pathname = usePathname()
  const t = useTranslations('onboarding')

  const steps = [
    { label: t('step1Label'), description: t('step1Description') },
    { label: t('step2Label'), description: t('step2Description') },
    { label: t('step3Label'), description: t('step3Description') },
    { label: t('step4Label'), description: t('step4Description') },
  ]

  const currentStep = STEP_PATHS.findIndex(p => pathname.includes(p))

  return (
    <div className="w-full max-w-lg mx-auto">
      <Stepper steps={steps} currentStep={Math.max(0, currentStep)} className="mb-8" />
      {children}
    </div>
  )
}
```

**Step 2: Create PrivacyBanner component.**

**apps/women-portal/src/components/onboarding/privacy-banner.tsx:**
```typescript
'use client'
import { useTranslations } from 'next-intl'

export function PrivacyBanner() {
  const t = useTranslations('privacy')

  return (
    <div className="flex items-start gap-2 rounded-inner bg-brand-forest/5 border border-brand-forest/20 p-3 mb-6">
      <svg className="h-4 w-4 text-brand-forest mt-0.5 shrink-0" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2">
        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/>
      </svg>
      <p className="text-xs font-body text-text-caption leading-relaxed">
        {t('banner')}
      </p>
    </div>
  )
}
```

**Step 3: Create ProfileStep component.**

**apps/women-portal/src/components/onboarding/profile-step.tsx:**
```typescript
'use client'
import { useState } from 'react'
import { useTranslations } from 'next-intl'
import { useRouter } from 'next/navigation'
import { Button, TextInput, Select } from '@glimmora/ui'
import { PrivacyBanner } from './privacy-banner'
import { useAuthStore } from '@/store/auth-store'

const TIMEZONES = [
  { value: 'Asia/Karachi', label: 'PKT (UTC+5)' },
  { value: 'Asia/Kolkata', label: 'IST (UTC+5:30)' },
  { value: 'Asia/Dubai', label: 'GST (UTC+4)' },
  { value: 'Asia/Riyadh', label: 'AST (UTC+3)' },
  { value: 'UTC', label: 'UTC' },
]

export function ProfileStep() {
  const t = useTranslations('onboarding.profile')
  const router = useRouter()
  const setOnboardingStep = useAuthStore((s) => s.setOnboardingStep)

  const [displayName, setDisplayName] = useState('')
  const [city, setCity] = useState('')
  const [timezone, setTimezone] = useState('Asia/Karachi')

  function handleNext() {
    setOnboardingStep(1)
    router.push('/onboarding/devices')
  }

  return (
    <div className="flex flex-col gap-5">
      <PrivacyBanner />

      <h2 className="font-display text-xl font-semibold text-text-heading">{t('title')}</h2>

      <div>
        <TextInput
          label={t('displayNameLabel')}
          placeholder={t('displayNamePlaceholder')}
          value={displayName}
          onChange={(e) => setDisplayName(e.target.value)}
        />
        <p className="text-xs font-body text-text-caption mt-1">{t('displayNameHint')}</p>
      </div>

      <TextInput
        label={t('cityLabel')}
        placeholder={t('cityPlaceholder')}
        value={city}
        onChange={(e) => setCity(e.target.value)}
      />

      <Select
        label={t('timezoneLabel')}
        value={timezone}
        onValueChange={setTimezone}
        options={TIMEZONES}
      />

      <Button variant="primary" className="w-full mt-2" onClick={handleNext}>
        {t('nextButton')}
      </Button>
    </div>
  )
}
```

**Step 4: Create DevicesStep component.**

**apps/women-portal/src/components/onboarding/devices-step.tsx:**
```typescript
'use client'
import { useState } from 'react'
import { useTranslations } from 'next-intl'
import { useRouter } from 'next/navigation'
import { Button, Select, RadioGroup, Checkbox } from '@glimmora/ui'
import { PrivacyBanner } from './privacy-banner'
import { useAuthStore } from '@/store/auth-store'

const DEVICES = [
  { value: 'desktop', label: 'Desktop Computer' },
  { value: 'laptop', label: 'Laptop' },
  { value: 'tablet', label: 'Tablet' },
  { value: 'smartphone', label: 'Smartphone' },
]

const OS_OPTIONS = [
  { value: 'windows', label: 'Windows' },
  { value: 'macos', label: 'macOS' },
  { value: 'linux', label: 'Linux' },
  { value: 'android', label: 'Android' },
  { value: 'ios', label: 'iOS' },
]

const SPEED_OPTIONS = [
  { value: 'slow', label: 'Slow (< 5 Mbps)' },
  { value: 'moderate', label: 'Moderate (5-25 Mbps)' },
  { value: 'fast', label: 'Fast (25+ Mbps)' },
]

const HOURS_OPTIONS = [
  { value: '5', label: '1-5 hours' },
  { value: '10', label: '5-10 hours' },
  { value: '20', label: '10-20 hours' },
  { value: '30', label: '20-30 hours' },
  { value: '40', label: '30+ hours' },
]

export function DevicesStep() {
  const t = useTranslations('onboarding.devices')
  const router = useRouter()
  const setOnboardingStep = useAuthStore((s) => s.setOnboardingStep)

  const [device, setDevice] = useState('laptop')
  const [os, setOs] = useState('windows')
  const [speed, setSpeed] = useState('moderate')
  const [hasWebcam, setHasWebcam] = useState(false)
  const [hours, setHours] = useState('10')

  function handleNext() {
    setOnboardingStep(2)
    router.push('/onboarding/skills')
  }

  return (
    <div className="flex flex-col gap-5">
      <PrivacyBanner />

      <div>
        <h2 className="font-display text-xl font-semibold text-text-heading">{t('title')}</h2>
        <p className="text-sm font-body text-text-caption mt-1">{t('subtitle')}</p>
      </div>

      <Select label={t('primaryDeviceLabel')} value={device} onValueChange={setDevice} options={DEVICES} />
      <Select label={t('osLabel')} value={os} onValueChange={setOs} options={OS_OPTIONS} />
      <Select label={t('internetLabel')} value={speed} onValueChange={setSpeed} options={SPEED_OPTIONS} />

      <Checkbox
        label={t('webcamLabel')}
        checked={hasWebcam}
        onCheckedChange={(checked) => setHasWebcam(checked === true)}
      />

      <Select label={t('hoursLabel')} value={hours} onValueChange={setHours} options={HOURS_OPTIONS} />

      <Button variant="primary" className="w-full mt-2" onClick={handleNext}>
        {t('nextButton')}
      </Button>
    </div>
  )
}
```

**Step 5: Create SkillsStep component.**

**apps/women-portal/src/components/onboarding/skills-step.tsx:**
```typescript
'use client'
import { useState } from 'react'
import { useTranslations } from 'next-intl'
import { useRouter } from 'next/navigation'
import { Button, TextInput, Tag } from '@glimmora/ui'
import { PrivacyBanner } from './privacy-banner'
import { useAuthStore } from '@/store/auth-store'

const AVAILABLE_SKILLS = [
  'React', 'TypeScript', 'JavaScript', 'Python', 'HTML/CSS',
  'Node.js', 'SQL', 'MongoDB', 'Docker', 'AWS',
  'UI/UX Design', 'Figma', 'Technical Writing', 'Data Analysis',
  'Testing/QA', 'Git/GitHub', 'REST APIs', 'GraphQL',
  'Mobile Development', 'WordPress',
]

export function SkillsStep() {
  const t = useTranslations('onboarding.skills')
  const router = useRouter()
  const setOnboardingStep = useAuthStore((s) => s.setOnboardingStep)

  const [search, setSearch] = useState('')
  const [selectedSkills, setSelectedSkills] = useState<string[]>([])

  const filteredSkills = AVAILABLE_SKILLS.filter(
    (skill) => skill.toLowerCase().includes(search.toLowerCase()) && !selectedSkills.includes(skill)
  )

  function toggleSkill(skill: string) {
    setSelectedSkills((prev) =>
      prev.includes(skill) ? prev.filter((s) => s !== skill) : [...prev, skill]
    )
  }

  function handleNext() {
    setOnboardingStep(3)
    router.push('/onboarding/activation')
  }

  return (
    <div className="flex flex-col gap-5">
      <PrivacyBanner />

      <div>
        <h2 className="font-display text-xl font-semibold text-text-heading">{t('title')}</h2>
        <p className="text-sm font-body text-text-caption mt-1">{t('subtitle')}</p>
      </div>

      <TextInput
        placeholder={t('searchPlaceholder')}
        value={search}
        onChange={(e) => setSearch(e.target.value)}
      />

      {/* Selected skills */}
      {selectedSkills.length > 0 && (
        <div>
          <p className="text-sm font-body text-text-caption mb-2">
            {t('selectedCount', { count: selectedSkills.length })}
          </p>
          <div className="flex flex-wrap gap-2">
            {selectedSkills.map((skill) => (
              <Tag
                key={skill}
                variant="skill"
                dismissible
                onDismiss={() => toggleSkill(skill)}
              >
                {skill}
              </Tag>
            ))}
          </div>
        </div>
      )}

      {/* Available skills grid */}
      <div className="flex flex-wrap gap-2">
        {filteredSkills.map((skill) => (
          <button
            key={skill}
            onClick={() => toggleSkill(skill)}
            className="rounded-badge px-3 py-1.5 text-xs font-body font-medium bg-hover text-text-body hover:bg-brand-primary/10 hover:text-brand-primary transition-colors"
          >
            {skill}
          </button>
        ))}
      </div>

      <Button
        variant="primary"
        className="w-full mt-2"
        onClick={handleNext}
        disabled={selectedSkills.length === 0}
      >
        {t('nextButton')}
      </Button>
    </div>
  )
}
```

**Step 6: Create ActivationStep component.**

**apps/women-portal/src/components/onboarding/activation-step.tsx:**
```typescript
'use client'
import { useTranslations } from 'next-intl'
import { useRouter } from 'next/navigation'
import { Button } from '@glimmora/ui'
import { PrivacyBanner } from './privacy-banner'
import { useAuthStore } from '@/store/auth-store'

export function ActivationStep() {
  const t = useTranslations('onboarding.activation')
  const router = useRouter()
  const completeOnboarding = useAuthStore((s) => s.completeOnboarding)

  function handleComplete() {
    completeOnboarding()
    router.push('/dashboard')
  }

  return (
    <div className="flex flex-col items-center gap-6 text-center">
      <PrivacyBanner />

      {/* Success indicator */}
      <div className="flex h-20 w-20 items-center justify-center rounded-full bg-status-success/10">
        <svg className="h-10 w-10 text-status-success" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2">
          <path d="M20 6L9 17l-5-5"/>
        </svg>
      </div>

      <div>
        <h2 className="font-display text-2xl font-semibold text-text-heading">{t('title')}</h2>
        <p className="text-sm font-body text-text-caption mt-1">{t('subtitle')}</p>
      </div>

      <p className="text-sm font-body text-text-body leading-relaxed max-w-sm">
        {t('message')}
      </p>

      <div className="rounded-inner bg-brand-primary/5 border border-brand-primary/20 p-4 max-w-sm">
        <p className="text-xs font-body text-text-caption leading-relaxed">
          {t('privacyReminder')}
        </p>
      </div>

      <Button variant="primary" size="lg" className="w-full max-w-xs" onClick={handleComplete}>
        {t('dashboardButton')}
      </Button>
    </div>
  )
}
```

**Step 7: Create onboarding index.ts barrel export.**

**apps/women-portal/src/components/onboarding/index.ts:**
```typescript
export { ProfileStep } from './profile-step'
export { DevicesStep } from './devices-step'
export { SkillsStep } from './skills-step'
export { ActivationStep } from './activation-step'
export { PrivacyBanner } from './privacy-banner'
```

**Step 8: Create onboarding page files.**

Each page imports its step component:

**apps/women-portal/src/app/(pre-auth)/onboarding/profile/page.tsx:**
```typescript
import { ProfileStep } from '@/components/onboarding'
export default function ProfilePage() { return <ProfileStep /> }
```

**apps/women-portal/src/app/(pre-auth)/onboarding/devices/page.tsx:**
```typescript
import { DevicesStep } from '@/components/onboarding'
export default function DevicesPage() { return <DevicesStep /> }
```

**apps/women-portal/src/app/(pre-auth)/onboarding/skills/page.tsx:**
```typescript
import { SkillsStep } from '@/components/onboarding'
export default function SkillsPage() { return <SkillsStep /> }
```

**apps/women-portal/src/app/(pre-auth)/onboarding/activation/page.tsx:**
```typescript
import { ActivationStep } from '@/components/onboarding'
export default function ActivationPage() { return <ActivationStep /> }
```

**IMPORTANT: Remove the existing canary page and route.**

Delete `apps/women-portal/src/app/page.tsx` (the canary redirect) and `apps/women-portal/src/app/canary/` directory entirely. The (pre-auth)/page.tsx now serves the root URL.
  </action>
  <verify>
Run `pnpm turbo build --filter=women-portal` -- builds without errors.
Verify `ls apps/women-portal/src/app/\(pre-auth\)/onboarding/{profile,devices,skills,activation}/page.tsx` -- all 4 exist.
Verify `ls apps/women-portal/src/components/onboarding/{profile-step,devices-step,skills-step,activation-step,privacy-banner}.tsx` -- all 5 exist.
Verify PrivacyBanner appears in every onboarding step: `grep -l "PrivacyBanner" apps/women-portal/src/components/onboarding/*.tsx` -- should match profile-step, devices-step, skills-step, activation-step.
**WP-05 PrivacyBanner in ALL 4 steps (strict count):** `grep -l "PrivacyBanner" apps/women-portal/src/components/onboarding/profile-step.tsx apps/women-portal/src/components/onboarding/devices-step.tsx apps/women-portal/src/components/onboarding/skills-step.tsx apps/women-portal/src/components/onboarding/activation-step.tsx | wc -l` -- must output `4`.
  </verify>
  <done>4-step onboarding wizard with route-based steps, Stepper component in shared layout, privacy banner on every step, skill selection with search/dismiss, and activation confirmation with 72-hour messaging. Canary page removed. Full pre-auth flow: / (language) -> /welcome -> /register -> /onboarding/profile -> /devices -> /skills -> /activation -> /dashboard.</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @glimmora/types type-check` passes with zero errors
2. `pnpm turbo build --filter=women-portal` builds without errors
3. `pnpm turbo build --filter=university-portal` builds without errors
4. All 7 new type files exist in packages/types/src/ with 14 exported types
5. next-intl installed in both portal package.json files
6. Women's Portal root URL (/) renders language selector with English, Urdu, Arabic options
7. Selecting Urdu/Arabic changes html dir="rtl" and content renders RTL
8. /welcome shows chat-bubble style welcome messages in Glimmora brand colors (NOT WhatsApp green)
9. /register shows email + password form with privacy notice (no phone, no social login)
10. /onboarding/profile through /onboarding/activation all render with Stepper and PrivacyBanner
11. Privacy banner text visible on EVERY onboarding step
12. Activation step shows 72-hour messaging
13. MSW auth handlers respond to POST /api/auth/register and /api/auth/login
14. No leaderboard, no public profile, no peer comparison text appears anywhere
</verification>

<success_criteria>
- Language selection is literally the FIRST thing a user sees at the Women's Portal root URL
- Urdu and Arabic render RTL with Noto Sans Arabic font
- WhatsApp-style welcome uses conversational chat bubbles in brand colors
- Registration is email + password only -- no phone, no social login
- All 4 onboarding steps are individual routes with browser back-button support
- Privacy guarantee banner is visible on every single onboarding step
- Activation step communicates 72-hour target and privacy commitment
- 14 new types available in @glimmora/types for use by all subsequent plans
- Both portals have next-intl configured and building successfully
</success_criteria>

<output>
After completion, create `.planning/phases/03-womens-portal-university-portal/03-01-SUMMARY.md`
</output>
