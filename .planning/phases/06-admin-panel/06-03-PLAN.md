---
phase: 06-admin-panel
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - apps/admin-panel/src/app/(app)/projects/page.tsx
  - apps/admin-panel/src/app/(app)/projects/[projectId]/page.tsx
  - apps/admin-panel/src/components/projects/project-list-table.tsx
  - apps/admin-panel/src/components/projects/project-admin-tabs.tsx
  - apps/admin-panel/src/components/projects/project-overview-tab.tsx
  - apps/admin-panel/src/components/projects/project-timeline-tab.tsx
  - apps/admin-panel/src/components/projects/project-evidence-tab.tsx
  - apps/admin-panel/src/components/projects/project-rework-tab.tsx
  - apps/admin-panel/src/components/projects/project-escalation-tab.tsx
  - apps/admin-panel/src/components/projects/project-payment-tab.tsx
  - apps/admin-panel/src/components/projects/project-team-tab.tsx
  - apps/admin-panel/src/components/projects/apg-activity-tab.tsx
  - apps/admin-panel/src/components/projects/admin-intervention-log.tsx
  - apps/admin-panel/src/components/projects/freeze-tab.tsx
  - apps/admin-panel/src/components/projects/freeze-dialog.tsx
  - apps/admin-panel/src/components/projects/index.ts
  - apps/admin-panel/src/lib/msw/factories/project.ts
  - apps/admin-panel/src/lib/msw/handlers/projects.ts
  - apps/admin-panel/src/lib/msw/handlers/index.ts
autonomous: true

must_haves:
  truths:
    - "Project list shows DataTable of all projects with status, health indicator, completion percentage, and assigned admin filter"
    - "Project detail has 10-tab admin view: Overview, Timeline, Evidence Packs, Rework Requests, Escalation Centre, Payment Release, Team Summary, APG Activity Log, Admin Interventions, Freeze/Unfreeze"
    - "Tab state reflected in URL hash for bookmarkability (#overview, #timeline, etc.)"
    - "APG Activity Log tab uses APGFeed component from @glimmora/ui showing all APG actions for the project"
    - "Admin Interventions tab shows immutable DataTable of past interventions -- NO edit/delete buttons"
    - "Freeze/Unfreeze tab has a freeze button requiring reason via confirmation dialog, plus freeze history"
    - "Freeze action calls POST /api/admin/projects/:id/freeze with reason, returns confirmation"
    - "Admin can record immutable interventions on a project via 'Record Intervention' button"
  artifacts:
    - path: "apps/admin-panel/src/app/(app)/projects/page.tsx"
      provides: "Project list for admin view"
    - path: "apps/admin-panel/src/app/(app)/projects/[projectId]/page.tsx"
      provides: "10-tab project admin detail (AP-08)"
    - path: "apps/admin-panel/src/components/projects/project-admin-tabs.tsx"
      provides: "10-tab component for project admin view"
      contains: "ProjectAdminTabs"
    - path: "apps/admin-panel/src/components/projects/admin-intervention-log.tsx"
      provides: "Immutable intervention log (AP-10)"
      contains: "AdminInterventionLog"
    - path: "apps/admin-panel/src/components/projects/freeze-tab.tsx"
      provides: "Freeze/unfreeze with reason (AP-09)"
      contains: "FreezeTab"
  key_links:
    - from: "apps/admin-panel/src/components/projects/apg-activity-tab.tsx"
      to: "@glimmora/ui APGFeed"
      via: "APG activity feed for project"
      pattern: "import.*APGFeed.*from.*@glimmora/ui"
    - from: "apps/admin-panel/src/components/projects/admin-intervention-log.tsx"
      to: "@glimmora/ui DataTable"
      via: "Immutable DataTable -- read-only, no actions column"
      pattern: "import.*DataTable.*from.*@glimmora/ui"
    - from: "apps/admin-panel/src/components/projects/project-team-tab.tsx"
      to: "@glimmora/ui AnonymizedTeamCard"
      via: "Team display with anonymized contributors"
      pattern: "import.*AnonymizedTeamCard.*from.*@glimmora/ui"
    - from: "apps/admin-panel/src/components/projects/freeze-dialog.tsx"
      to: "@glimmora/ui Dialog"
      via: "Confirmation dialog with mandatory reason for freeze action"
      pattern: "import.*Dialog.*from.*@glimmora/ui"
---

<objective>
Build the complete project management section: project list with filtering, 10-tab admin project detail (extending enterprise's 7-tab pattern with APG Activity Log, Admin Interventions, and Freeze/Unfreeze tabs), freeze/unfreeze with mandatory reason, and immutable intervention logging -- fulfilling AP-08, AP-09, and AP-10.

Purpose: The project admin view is the control surface for overseeing all platform delivery. It extends the enterprise's 7-tab project view with 3 admin-only tabs that provide governance capabilities: viewing all APG actions, recording immutable interventions, and freezing/unfreezing projects. The first 7 tabs follow the same patterns as enterprise-portal but adapted for the admin's broader view.

Output: Project list page with DataTable, project detail page with 10 client-side tabs (all functional), freeze dialog with mandatory reason, immutable intervention log, and MSW handlers for all project admin endpoints.
</objective>

<execution_context>
@/Users/kavi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kavi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-admin-panel/06-RESEARCH.md
@.planning/phases/06-admin-panel/06-01-SUMMARY.md

Key codebase references:
@packages/types/src/admin.ts (AdminIntervention, InterventionType)
@packages/types/src/project.ts (Project, ProjectStatus, ProjectHealthStatus, ProjectMilestone)
@packages/types/src/api.ts (APGActivity)
@packages/ui/src/components/data-table/data-table.tsx
@packages/ui/src/components/tabs/tabs.tsx
@packages/ui/src/components/apg-feed/apg-feed.tsx
@packages/ui/src/components/anonymized-team-card/anonymized-team-card.tsx
@packages/ui/src/components/evidence-viewer/evidence-viewer.tsx
@packages/ui/src/components/timeline-bar/timeline-bar.tsx
@apps/enterprise-portal/src/components/projects/project-detail-tabs.tsx (7-tab pattern to extend)
@apps/enterprise-portal/src/components/projects/project-overview.tsx (overview pattern)
@apps/enterprise-portal/src/components/projects/team-summary-grid.tsx (team pattern)
@apps/admin-panel/src/components/users/user-action-dialog.tsx (reusable confirmation dialog)

CRITICAL constraints:
- 10-tab view uses Tabs from @glimmora/ui with hash-based state (same pattern as enterprise 7-tab)
- Admin Interventions DataTable is READ-ONLY -- no action column, no edit/delete, isImmutable: true
- Freeze requires confirmation dialog with mandatory reason
- Team Summary uses AnonymizedTeamCard -- admin sees contributor seeds, NOT real names
- APG Activity Log uses APGFeed component from @glimmora/ui
- DataTable filtering is done ABOVE the table with useMemo, not built into DataTable
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build project list page, 10-tab project detail container, and 3 admin-only tabs (APG Activity, Interventions, Freeze)</name>
  <files>
    apps/admin-panel/src/app/(app)/projects/page.tsx
    apps/admin-panel/src/app/(app)/projects/[projectId]/page.tsx
    apps/admin-panel/src/components/projects/project-list-table.tsx
    apps/admin-panel/src/components/projects/project-admin-tabs.tsx
    apps/admin-panel/src/components/projects/apg-activity-tab.tsx
    apps/admin-panel/src/components/projects/admin-intervention-log.tsx
    apps/admin-panel/src/components/projects/freeze-tab.tsx
    apps/admin-panel/src/components/projects/freeze-dialog.tsx
    apps/admin-panel/src/components/projects/index.ts
    apps/admin-panel/src/lib/msw/factories/project.ts
    apps/admin-panel/src/lib/msw/handlers/projects.ts
    apps/admin-panel/src/lib/msw/handlers/index.ts
  </files>
  <action>
**Step 1: Create project list page.**

`apps/admin-panel/src/app/(app)/projects/page.tsx`:
'use client'

Fetches GET /api/admin/projects via TanStack useQuery. Renders:
1. PageHeader "Project Management"
2. Filter bar: TextInput search + Select for status (All / Active / Paused / Completed / Frozen) + Select for health (All / On Track / At Risk / Delayed / Critical)
3. ProjectListTable with filtered data (useMemo pattern)

`apps/admin-panel/src/components/projects/project-list-table.tsx`:
'use client'

DataTable with columns:
- Project Name (clickable -> /projects/:id)
- Enterprise (organization name)
- Status (Badge: active=inprogress, paused=normal, completed=done, frozen=urgent)
- Health (colored dot + text: on-track=green, at-risk=amber, delayed=atrisk, critical=urgent)
- Completion (Progress bar + percentage)
- Tasks (completed/total)
- Created (formatted date)
- Actions (DropdownMenu: View Details, Freeze Project)

Props: `{ projects: Project[] }`

**Step 2: Create project detail page.**

`apps/admin-panel/src/app/(app)/projects/[projectId]/page.tsx`:
'use client'

Reads projectId from URL params. Fetches GET /api/admin/projects/:projectId via TanStack useQuery. Renders:
1. PageHeader with project name + status Badge + health indicator + freeze indicator (if frozen)
2. ProjectAdminTabs component

**Step 3: Create ProjectAdminTabs with 10 tabs.**

`apps/admin-panel/src/components/projects/project-admin-tabs.tsx`:
'use client'

Uses Tabs from @glimmora/ui with 10 tabs:
```typescript
const TABS = [
  { value: 'overview', label: 'Overview' },
  { value: 'timeline', label: 'Timeline' },
  { value: 'evidence', label: 'Evidence Packs' },
  { value: 'rework', label: 'Rework Requests' },
  { value: 'escalation', label: 'Escalation Centre' },
  { value: 'payments', label: 'Payment Release' },
  { value: 'team', label: 'Team Summary' },
  { value: 'apg-activity', label: 'APG Activity Log' },
  { value: 'interventions', label: 'Admin Interventions' },
  { value: 'freeze', label: 'Freeze/Unfreeze' },
]
```

Hash-based tab state: read from window.location.hash, default to 'overview'.

Tab contents:
- overview -> ProjectOverviewTab (Step 4 of Task 2)
- timeline -> ProjectTimelineTab (Step 4 of Task 2)
- evidence -> ProjectEvidenceTab (Step 4 of Task 2)
- rework -> ProjectReworkTab (Step 4 of Task 2)
- escalation -> ProjectEscalationTab (Step 4 of Task 2)
- payments -> ProjectPaymentTab (Step 4 of Task 2)
- team -> ProjectTeamTab (Step 4 of Task 2)
- apg-activity -> APGActivityTab (this task)
- interventions -> AdminInterventionLog (this task)
- freeze -> FreezeTab (this task)

Props: `{ projectId: string; project: Project }`

**Step 4: Create APG Activity Log tab.**

`apps/admin-panel/src/components/projects/apg-activity-tab.tsx`:
'use client'

Fetches GET /api/admin/projects/:projectId/apg-activity via TanStack useQuery. Renders:
- APGFeed from @glimmora/ui with all APG actions for this project
- Shows comprehensive activity: task assignments, auto-approvals, escalation triggers, team formation events
- Empty state if no APG activity

Props: `{ projectId: string }`

**Step 5: Create Admin Intervention Log.**

`apps/admin-panel/src/components/projects/admin-intervention-log.tsx`:
'use client'

Fetches GET /api/admin/projects/:projectId/interventions via TanStack useQuery.

Two sections:
1. "Record Intervention" button at top -> opens a Dialog with:
   - Select for interventionType (project_freeze, project_unfreeze, contributor_reassignment, milestone_override, payment_hold, payment_release, escalation_created)
   - Textarea for reason (mandatory)
   - Textarea for details (optional)
   - Submit calls POST /api/admin/projects/:projectId/interventions

2. DataTable of past interventions -- READ-ONLY:
   - Columns: Timestamp, Type (Badge), Reason, Details, Performed By
   - NO action column -- interventions are immutable (isImmutable: true)
   - NO edit/delete buttons
   - Sorted by timestamp descending

Props: `{ projectId: string }`

**Step 6: Create Freeze/Unfreeze tab.**

`apps/admin-panel/src/components/projects/freeze-tab.tsx`:
'use client'

Fetches GET /api/admin/projects/:projectId/freeze-history via TanStack useQuery. Shows:

1. Current freeze status: if project is frozen, shows warning banner with "Project is FROZEN since {date}" + "All activity paused, contributors notified" + Unfreeze button
2. If not frozen: "Freeze Project" button (destructive variant)
3. Both buttons open FreezeDialog

4. Freeze History: DataTable of past freeze/unfreeze events
   - Columns: Action (Freeze/Unfreeze Badge), Reason, Performed By, Date
   - Read-only

Props: `{ projectId: string; isFrozen: boolean }`

`apps/admin-panel/src/components/projects/freeze-dialog.tsx`:
'use client'

Dialog with:
- Title: "Freeze Project" or "Unfreeze Project"
- Warning text: "Freezing will pause all project activity and notify all contributors"
- Textarea for reason (mandatory)
- Cancel (variant="secondary") + Confirm (variant="destructive" for freeze, variant="primary" for unfreeze)
- Calls POST /api/admin/projects/:id/freeze or POST /api/admin/projects/:id/unfreeze

Props: `{ projectId: string; action: 'freeze' | 'unfreeze'; onSuccess: () => void }`

**Step 7: Create MSW project factories and handlers.**

`apps/admin-panel/src/lib/msw/factories/project.ts`:
- `createMockAdminProjectList()` -- returns 8-10 projects with mixed statuses (active, paused, completed, frozen) and health indicators
- `createMockAdminProjectDetail(id)` -- returns full project detail with milestones, team data
- `createMockAPGActivity(projectId)` -- returns 8-10 APGActivity items (auto-approvals, assignments, escalations, team formations)
- `createMockInterventions(projectId)` -- returns 3-5 AdminIntervention objects with mixed types
- `createMockFreezeHistory(projectId)` -- returns 2-3 freeze/unfreeze entries
- `createMockProjectTimeline(projectId)` -- returns 5 milestones with mixed statuses
- `createMockProjectTeam()` -- returns 6-8 anonymized team members
- `createMockProjectEvidence(projectId)` -- returns 5-6 evidence packs with mixed statuses
- `createMockProjectReworks(projectId)` -- returns 3-4 rework requests
- `createMockProjectEscalations(projectId)` -- returns 2-3 escalations
- `createMockProjectPayments(projectId)` -- returns 5-8 payment records

`apps/admin-panel/src/lib/msw/handlers/projects.ts`:
- GET /api/admin/projects -> returns createMockAdminProjectList()
- GET /api/admin/projects/:id -> returns createMockAdminProjectDetail(id)
- GET /api/admin/projects/:id/apg-activity -> returns createMockAPGActivity(id)
- GET /api/admin/projects/:id/interventions -> returns createMockInterventions(id)
- POST /api/admin/projects/:id/interventions -> accepts { interventionType, reason, details }, returns 201
- GET /api/admin/projects/:id/freeze-history -> returns createMockFreezeHistory(id)
- POST /api/admin/projects/:id/freeze -> accepts { reason }, returns 200
- POST /api/admin/projects/:id/unfreeze -> accepts { reason }, returns 200
- GET /api/admin/projects/:id/timeline -> returns createMockProjectTimeline(id)
- GET /api/admin/projects/:id/team -> returns createMockProjectTeam()
- GET /api/admin/projects/:id/evidence -> returns createMockProjectEvidence(id)
- GET /api/admin/projects/:id/reworks -> returns createMockProjectReworks(id)
- GET /api/admin/projects/:id/escalations -> returns createMockProjectEscalations(id)
- GET /api/admin/projects/:id/payments -> returns createMockProjectPayments(id)

Update handlers/index.ts to spread projectHandlers.

`apps/admin-panel/src/components/projects/index.ts`:
Barrel export all project components.
  </action>
  <verify>
Run `pnpm turbo build --filter=@glimmora/admin-panel` -- builds without errors.
Verify project list: `ls apps/admin-panel/src/app/\(app\)/projects/page.tsx` -- exists.
Verify project detail: `ls apps/admin-panel/src/app/\(app\)/projects/\[projectId\]/page.tsx` -- exists.
Verify 10 tabs: `grep "overview.*timeline.*evidence.*rework.*escalation.*payments.*team.*apg-activity.*interventions.*freeze" apps/admin-panel/src/components/projects/project-admin-tabs.tsx` -- must match all 10 tab values.
Verify APGFeed: `grep "APGFeed" apps/admin-panel/src/components/projects/apg-activity-tab.tsx` -- must match.
Verify immutable interventions: `grep "DataTable" apps/admin-panel/src/components/projects/admin-intervention-log.tsx` -- must match. `grep -L "delete\|edit" apps/admin-panel/src/components/projects/admin-intervention-log.tsx` -- must match (confirming NO delete/edit).
Verify freeze dialog: `grep "reason" apps/admin-panel/src/components/projects/freeze-dialog.tsx` -- must match mandatory reason.
  </verify>
  <done>Project list with status/health filtering. 10-tab admin project detail with all tabs. APG Activity Log using APGFeed. Immutable AdminInterventionLog with read-only DataTable. Freeze/Unfreeze tab with mandatory reason dialog and freeze history.</done>
</task>

<task type="auto">
  <name>Task 2: Build the 7 standard project tabs (Overview, Timeline, Evidence, Rework, Escalation, Payment, Team)</name>
  <files>
    apps/admin-panel/src/components/projects/project-overview-tab.tsx
    apps/admin-panel/src/components/projects/project-timeline-tab.tsx
    apps/admin-panel/src/components/projects/project-evidence-tab.tsx
    apps/admin-panel/src/components/projects/project-rework-tab.tsx
    apps/admin-panel/src/components/projects/project-escalation-tab.tsx
    apps/admin-panel/src/components/projects/project-payment-tab.tsx
    apps/admin-panel/src/components/projects/project-team-tab.tsx
  </files>
  <action>
**These 7 tabs follow the same patterns as enterprise-portal but adapted for admin context. Build them as new files (not imports from enterprise -- pnpm strict mode, different apps).**

**Step 1: ProjectOverviewTab.**

`apps/admin-panel/src/components/projects/project-overview-tab.tsx`:
'use client'

Fetches project summary data. Shows:
- Summary stats: total tasks (completed/total), budget used vs total, days remaining, health status
- Milestone progress: list of milestones with status Badge and Progress bar
- Enterprise info: organization name, requester name, project created date
- Quick actions: "View Timeline" (switches tab), "View Evidence" (switches tab)

Props: `{ projectId: string; project: Project }`

**Step 2: ProjectTimelineTab.**

`apps/admin-panel/src/components/projects/project-timeline-tab.tsx`:
'use client'

Fetches GET /api/admin/projects/:id/timeline. Shows:
- List view of milestones with TimelineBar from @glimmora/ui
- Each milestone: name, target date, status Badge (on-track/at-risk/delayed), completion percentage
- Simple list layout (not full Gantt -- admin needs overview, not editing)

Props: `{ projectId: string }`

**Step 3: ProjectEvidenceTab.**

`apps/admin-panel/src/components/projects/project-evidence-tab.tsx`:
'use client'

Fetches GET /api/admin/projects/:id/evidence. Shows:
- DataTable of evidence packs: Task Name, Submitted Date, Status (Badge: pending/under_review/approved/rejected), Reviewer, Decision Date
- Click row to expand and show EvidenceViewer from @glimmora/ui (contributor identity hidden)
- Admin can see status but evidence review itself happens in mentor/enterprise portals

Props: `{ projectId: string }`

**Step 4: ProjectReworkTab.**

`apps/admin-panel/src/components/projects/project-rework-tab.tsx`:
'use client'

Fetches GET /api/admin/projects/:id/reworks. Shows:
- DataTable of rework requests: Task Name, Requested By (role), Reason, Status (Badge), Created Date
- Expandable rows showing rework details and feedback items

Props: `{ projectId: string }`

**Step 5: ProjectEscalationTab.**

`apps/admin-panel/src/components/projects/project-escalation-tab.tsx`:
'use client'

Fetches GET /api/admin/projects/:id/escalations. Shows:
- DataTable of escalations: Task Name, Escalated By, Type (Badge), Status, Created Date
- Admin can view but escalation resolution happens in dispute section
- Link to dispute if escalation created one

Props: `{ projectId: string }`

**Step 6: ProjectPaymentTab.**

`apps/admin-panel/src/components/projects/project-payment-tab.tsx`:
'use client'

Fetches GET /api/admin/projects/:id/payments. Shows:
- Summary: total budget, released, pending, held amounts
- DataTable of payment records: Milestone, Amount, Status (Badge), Release Mode, Date, Transaction ID
- Admin can see payment holds and override them via "Release Hold" button (calls POST endpoint)

Props: `{ projectId: string }`

**Step 7: ProjectTeamTab.**

`apps/admin-panel/src/components/projects/project-team-tab.tsx`:
'use client'

Fetches GET /api/admin/projects/:id/team. Shows:
- Grid of AnonymizedTeamCard from @glimmora/ui (grid-cols-1 sm:grid-cols-2 lg:grid-cols-3)
- Each card: anonymous seed, skill tags (max 4 + overflow), tier Badge, tasks assigned count
- NO real contributor names -- privacy preserved even for admin view
- Team stats summary: total members, skill coverage, average tier

Props: `{ projectId: string }`
  </action>
  <verify>
Run `pnpm turbo build --filter=@glimmora/admin-panel` -- builds without errors.
Verify all 7 tab files exist: `ls apps/admin-panel/src/components/projects/project-{overview,timeline,evidence,rework,escalation,payment,team}-tab.tsx` -- all 7 exist.
Verify TimelineBar: `grep "TimelineBar" apps/admin-panel/src/components/projects/project-timeline-tab.tsx` -- must match.
Verify EvidenceViewer: `grep "EvidenceViewer" apps/admin-panel/src/components/projects/project-evidence-tab.tsx` -- must match.
Verify AnonymizedTeamCard: `grep "AnonymizedTeamCard" apps/admin-panel/src/components/projects/project-team-tab.tsx` -- must match.
  </verify>
  <done>All 7 standard project tabs functional: Overview with summary stats, Timeline with milestones, Evidence with viewer, Rework requests, Escalations, Payments with hold override, Team with AnonymizedTeamCard grid. Combined with Task 1's 3 admin-only tabs, the full 10-tab admin project view is complete.</done>
</task>

</tasks>

<verification>
1. `pnpm turbo build --filter=@glimmora/admin-panel` builds without errors
2. Project list at /projects shows DataTable with status/health filtering
3. Project detail at /projects/[id] shows 10 tabs with hash-based URL state
4. All 10 tabs are functional (not placeholders)
5. APG Activity Log uses APGFeed from @glimmora/ui
6. Admin Interventions DataTable is read-only (no edit/delete actions)
7. Freeze/Unfreeze requires mandatory reason via confirmation dialog
8. Team Summary uses AnonymizedTeamCard (no real contributor names)
9. Evidence tab uses EvidenceViewer (contributor identity hidden)
10. MSW handlers serve all project admin endpoints
</verification>

<success_criteria>
- Admin can browse all projects with status and health filtering
- Admin can view any project's complete admin view across 10 tabs
- Admin can freeze a project (pausing all activity) with logged reason
- Admin can record immutable interventions with type, reason, and details
- Admin can view full APG activity log for any project
- Privacy preserved in Team Summary (anonymized contributors)
</success_criteria>

<output>
After completion, create `.planning/phases/06-admin-panel/06-03-SUMMARY.md`
</output>
