---
phase: 06-admin-panel
plan: 05
type: execute
wave: 4
depends_on: ["06-01", "06-02", "06-03", "06-04"]
files_modified:
  - apps/admin-panel/src/app/(app)/reports/page.tsx
  - apps/admin-panel/src/app/(app)/reports/builder/page.tsx
  - apps/admin-panel/src/components/reports/report-type-card.tsx
  - apps/admin-panel/src/components/reports/report-builder-form.tsx
  - apps/admin-panel/src/components/reports/report-pdf-document.tsx
  - apps/admin-panel/src/components/reports/index.ts
  - apps/admin-panel/src/lib/export-csv.ts
  - apps/admin-panel/src/app/(app)/content/skills/page.tsx
  - apps/admin-panel/src/app/(app)/content/announcements/page.tsx
  - apps/admin-panel/src/components/content/skill-taxonomy-tree.tsx
  - apps/admin-panel/src/components/content/announcements-form.tsx
  - apps/admin-panel/src/app/(app)/content/resources/page.tsx
  - apps/admin-panel/src/components/content/resource-library-table.tsx
  - apps/admin-panel/src/components/content/index.ts
  - apps/admin-panel/src/app/(app)/apg-config/page.tsx
  - apps/admin-panel/src/components/apg-config/config-card.tsx
  - apps/admin-panel/src/components/apg-config/super-admin-gate.tsx
  - apps/admin-panel/src/components/apg-config/index.ts
  - apps/admin-panel/src/app/(app)/audit-log/page.tsx
  - apps/admin-panel/src/components/audit-log/audit-log-table.tsx
  - apps/admin-panel/src/components/audit-log/index.ts
  - apps/admin-panel/src/app/(app)/settings/page.tsx
  - apps/admin-panel/src/components/settings/admin-role-management.tsx
  - apps/admin-panel/src/components/settings/index.ts
  - apps/admin-panel/src/lib/msw/factories/report.ts
  - apps/admin-panel/src/lib/msw/factories/audit-log.ts
  - apps/admin-panel/src/lib/msw/factories/skill-taxonomy.ts
  - apps/admin-panel/src/lib/msw/factories/apg-config.ts
  - apps/admin-panel/src/lib/msw/handlers/reports.ts
  - apps/admin-panel/src/lib/msw/handlers/content.ts
  - apps/admin-panel/src/lib/msw/handlers/apg-config.ts
  - apps/admin-panel/src/lib/msw/handlers/audit-log.ts
  - apps/admin-panel/src/lib/msw/handlers/index.ts
autonomous: true

must_haves:
  truths:
    - "Reports page shows 5 report types as cards: Platform Health, User Growth, Delivery Performance, Payment Flow, Dispute Analytics"
    - "Custom report builder has date range (DatePicker), report type (Select), filters, preview DataTable, and CSV/PDF export buttons"
    - "CSV export uses Blob + URL.createObjectURL pattern (no external library)"
    - "PDF export uses dynamic import('@react-pdf/renderer') inside event handler -- NEVER top-level import"
    - "Skill taxonomy management shows tree of skills organized by category with add/edit/merge actions"
    - "Platform announcements form supports create, edit, publish with audience targeting"
    - "Resource library at /content/resources shows a table of guides/policy docs with add/edit/delete actions"
    - "APG Configuration page is gated by SuperAdminGate component -- standard admins see locked message"
    - "APG Config shows inline-editable cards organized by 3 domains: Thresholds, Auto-Approval Rules, Escalation Triggers"
    - "Platform audit log at /audit-log is read-only DataTable with timestamp, actor, action, entity, reason columns -- NO edit/delete"
    - "Admin role management (Settings page) is Super Admin only, shows admin list with role assignments"
    - "SuperAdminGate renders Shield icon + 'Super Admin Access Required' message for standard admins"
  artifacts:
    - path: "apps/admin-panel/src/app/(app)/reports/page.tsx"
      provides: "5 report types (AP-15)"
    - path: "apps/admin-panel/src/app/(app)/reports/builder/page.tsx"
      provides: "Custom report builder (AP-16, AP-17)"
    - path: "apps/admin-panel/src/lib/export-csv.ts"
      provides: "CSV export utility"
      contains: "exportCSV"
    - path: "apps/admin-panel/src/components/reports/report-pdf-document.tsx"
      provides: "PDF report document (dynamic import only)"
      contains: "ReportPDF"
    - path: "apps/admin-panel/src/app/(app)/content/skills/page.tsx"
      provides: "Skill taxonomy management (AP-18)"
    - path: "apps/admin-panel/src/app/(app)/content/announcements/page.tsx"
      provides: "Platform announcements (AP-20)"
    - path: "apps/admin-panel/src/app/(app)/content/resources/page.tsx"
      provides: "Resource library management (AP-19)"
    - path: "apps/admin-panel/src/app/(app)/apg-config/page.tsx"
      provides: "APG configuration, Super Admin only (AP-21)"
    - path: "apps/admin-panel/src/app/(app)/audit-log/page.tsx"
      provides: "Platform audit log (AP-23)"
    - path: "apps/admin-panel/src/app/(app)/settings/page.tsx"
      provides: "Admin role management, Super Admin only (AP-22)"
    - path: "apps/admin-panel/src/components/apg-config/super-admin-gate.tsx"
      provides: "Reusable Super Admin access gate component"
      contains: "SuperAdminGate"
  key_links:
    - from: "apps/admin-panel/src/components/reports/report-builder-form.tsx"
      to: "@glimmora/ui DatePicker"
      via: "Date range selection for reports"
      pattern: "import.*DatePicker.*from.*@glimmora/ui"
    - from: "apps/admin-panel/src/components/reports/report-builder-form.tsx"
      to: "apps/admin-panel/src/lib/export-csv.ts"
      via: "CSV export for report data"
      pattern: "import.*exportCSV.*from.*@/lib/export-csv"
    - from: "apps/admin-panel/src/components/apg-config/super-admin-gate.tsx"
      to: "apps/admin-panel/src/store/auth-store.ts"
      via: "adminRole check for Super Admin gating"
      pattern: "import.*useAuthStore.*from.*@/store/auth-store"
    - from: "apps/admin-panel/src/components/content/skill-taxonomy-tree.tsx"
      to: "@glimmora/ui Tag"
      via: "Skill tags display in taxonomy"
      pattern: "import.*Tag.*from.*@glimmora/ui"
    - from: "apps/admin-panel/src/components/content/resource-library-table.tsx"
      to: "@glimmora/ui DataTable"
      via: "Resource library listing table"
      pattern: "import.*DataTable.*from.*@glimmora/ui"
    - from: "apps/admin-panel/src/components/audit-log/audit-log-table.tsx"
      to: "@glimmora/ui DataTable"
      via: "Read-only audit log -- no actions column"
      pattern: "import.*DataTable.*from.*@glimmora/ui"
---

<objective>
Build reports and analytics (5 report types + custom builder + CSV/PDF export), content management (skill taxonomy + resource library + announcements), APG configuration (Super Admin only with inline-editable cards), platform audit log, and admin role management -- fulfilling AP-15 through AP-23.

Purpose: This is the final plan, aggregating the remaining admin capabilities: reports for data-driven oversight, content management for the platform's knowledge base, APG configuration for tuning the AI governor, and the immutable audit log that underpins compliance. The Super Admin gating pattern (visible-but-locked) is introduced here for APG config and role management.

Output: Reports section with 5 types + custom builder + CSV/PDF export, skill taxonomy management, resource library management, announcements with audience targeting, APG config with SuperAdminGate, platform-wide audit log, admin role management, and MSW handlers for all remaining endpoints.
</objective>

<execution_context>
@/Users/kavi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kavi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-admin-panel/06-RESEARCH.md
@.planning/phases/06-admin-panel/06-01-SUMMARY.md
@.planning/phases/06-admin-panel/06-02-SUMMARY.md
@.planning/phases/06-admin-panel/06-03-SUMMARY.md
@.planning/phases/06-admin-panel/06-04-SUMMARY.md

Key codebase references:
@packages/types/src/admin.ts (ReportType, ReportConfig, PlatformReportData, SkillTaxonomyTag, APGConfigEntry, APGConfigDomain, PlatformAuditEntry, AuditActionCategory, PlatformAnnouncement, AnnouncementAudience, AnnouncementStatus)
@packages/ui/src/components/data-table/data-table.tsx
@packages/ui/src/components/date-picker/date-picker.tsx
@packages/ui/src/components/tag/tag.tsx
@packages/ui/src/components/switch/switch.tsx
@apps/enterprise-portal/src/components/compliance/esg-export-form.tsx (PDF dynamic import pattern)
@apps/enterprise-portal/src/components/compliance/esg-report-pdf.tsx (PDF document pattern)
@apps/admin-panel/src/store/auth-store.ts (adminRole for Super Admin gating)
@apps/admin-panel/src/components/dev/role-switcher.tsx (DevTools for testing)

CRITICAL constraints:
- PDF: NEVER import @react-pdf/renderer at top level -- ALWAYS `await import()` inside event handler
- PDF document component (report-pdf-document.tsx) is NOT barrel-exported -- only dynamically imported
- CSV export: use Blob + URL.createObjectURL (no external library needed)
- Audit log DataTable: NO action column, NO edit/delete -- read-only with search/filter only
- SuperAdminGate: visible-but-locked pattern from CONTEXT.md (Shield icon + message, NOT hidden)
- APG Config: inline-editable cards, one card per domain (Thresholds / Auto-Approval / Escalation)
- @glimmora/ui Label is <p> not <label> -- use native <label> for form fields
- Button variant="outline" does not exist -- use variant="secondary"
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build reports section with 5 report types, custom builder, and CSV/PDF export</name>
  <files>
    apps/admin-panel/src/app/(app)/reports/page.tsx
    apps/admin-panel/src/app/(app)/reports/builder/page.tsx
    apps/admin-panel/src/components/reports/report-type-card.tsx
    apps/admin-panel/src/components/reports/report-builder-form.tsx
    apps/admin-panel/src/components/reports/report-pdf-document.tsx
    apps/admin-panel/src/components/reports/index.ts
    apps/admin-panel/src/lib/export-csv.ts
    apps/admin-panel/src/lib/msw/factories/report.ts
    apps/admin-panel/src/lib/msw/handlers/reports.ts
    apps/admin-panel/src/lib/msw/handlers/index.ts
  </files>
  <action>
**Step 1: Create CSV export utility.**

`apps/admin-panel/src/lib/export-csv.ts`:
```typescript
export function exportCSV(filename: string, headers: string[], rows: (string | number)[][]): void {
  const csvContent = [
    headers.join(','),
    ...rows.map(row => row.map(cell =>
      typeof cell === 'string' && cell.includes(',') ? `"${cell}"` : String(cell)
    ).join(','))
  ].join('\n')

  const blob = new Blob([csvContent], { type: 'text/csv;charset=utf-8;' })
  const url = URL.createObjectURL(blob)
  const a = document.createElement('a')
  a.href = url
  a.download = filename
  a.click()
  URL.revokeObjectURL(url)
}
```

**Step 2: Create reports list page.**

`apps/admin-panel/src/app/(app)/reports/page.tsx`:
'use client'

Renders:
1. PageHeader "Reports & Analytics" with "Custom Report" button -> /reports/builder
2. Grid of 5 ReportTypeCard components (grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4):

```typescript
const REPORT_TYPES = [
  { type: 'platform_overview', title: 'Platform Health', description: 'Active users, project health, system performance', icon: Activity },
  { type: 'user_activity', title: 'User Growth', description: 'Registration trends, retention, type distribution', icon: TrendingUp },
  { type: 'project_delivery', title: 'Delivery Performance', description: 'Completion rates, timelines, quality metrics', icon: CheckCircle },
  { type: 'financial', title: 'Payment Flow', description: 'Revenue, payouts, disputes, fee analysis', icon: DollarSign },
  { type: 'skill_growth', title: 'Dispute Analytics', description: 'Dispute types, resolution times, outcomes', icon: Scale },
]
```

Each card click navigates to /reports/builder?type={reportType}.

`apps/admin-panel/src/components/reports/report-type-card.tsx`:
Card with icon, title, description, and "Generate" button. Warm styling with hover state.

**Step 3: Create custom report builder page.**

`apps/admin-panel/src/app/(app)/reports/builder/page.tsx`:
'use client'

Reads optional `type` from URL search params to pre-select report type.

Renders ReportBuilderForm component.

`apps/admin-panel/src/components/reports/report-builder-form.tsx`:
'use client'

Form-driven report builder:

1. Report Type: Select dropdown (5 report types). Pre-selected if type came from URL.
2. Date Range: Two DatePicker components (from @glimmora/ui) for start and end dates
3. Filters section (conditional on report type):
   - User Activity: user type filter (Select)
   - Project Delivery: project status filter (Select)
   - Financial: payment status filter (Select)
   - Dispute Analytics: dispute type filter (Select)
4. "Generate Report" button -> fetches GET /api/admin/reports/:type?from=...&to=...&filters=...

5. Results area (shown after generate):
   - Summary metrics: 4-6 key numbers in a grid of Cards
   - Data table: DataTable showing report rows
   - Chart: Simple recharts BarChart visualizing key metrics (import recharts as direct dep)
   - Export buttons:
     - "Export CSV" -> calls exportCSV() from @/lib/export-csv with table data
     - "Export PDF" -> dynamic import of @react-pdf/renderer and ReportPDF document:
       ```typescript
       async function handleExportPDF() {
         setExporting(true)
         try {
           const { pdf } = await import('@react-pdf/renderer')
           const { ReportPDF } = await import('./report-pdf-document')
           const blob = await pdf(<ReportPDF data={reportData} config={reportConfig} />).toBlob()
           const url = URL.createObjectURL(blob)
           const a = document.createElement('a')
           a.href = url
           a.download = `report-${reportConfig.type}-${Date.now()}.pdf`
           a.click()
           URL.revokeObjectURL(url)
         } finally {
           setExporting(false)
         }
       }
       ```

**Step 4: Create PDF document (NOT barrel-exported).**

`apps/admin-panel/src/components/reports/report-pdf-document.tsx`:
```typescript
// This file is ONLY imported via dynamic import() inside event handlers.
// DO NOT add to barrel export or import at top level.
import { Document, Page, Text, View, StyleSheet } from '@react-pdf/renderer'
```

Simple PDF layout:
- Header: "GlimmoraTeam Report" + report type title + date range
- Summary section: key metrics as label/value pairs
- Data section: table rows
- Footer: generated timestamp + "Confidential"

Export: `export function ReportPDF({ data, config }: { data: PlatformReportData; config: ReportConfig })`

`apps/admin-panel/src/components/reports/index.ts`:
Export ReportTypeCard, ReportBuilderForm. Do NOT export ReportPDF.

**Step 5: Create MSW report factories and handlers.**

`apps/admin-panel/src/lib/msw/factories/report.ts`:
- `createMockReportData(type: ReportType)` -- returns PlatformReportData with type-appropriate metrics:
  - platform_overview: totalUsers, activeProjects, avgResponseTime, uptimePercentage
  - user_activity: newRegistrations, activeUsers, retentionRate, usersByType
  - project_delivery: completionRate, avgDeliveryDays, reworkRate, onTimeDelivery
  - financial: totalRevenue, totalPayouts, avgTransactionValue, disputeRate
  - skill_growth: skillsVerified, podlsIssued, avgSkillGrowthRate, topSkills

`apps/admin-panel/src/lib/msw/handlers/reports.ts`:
- GET /api/admin/reports/:type -> accepts query params (from, to), returns createMockReportData(type)

Update handlers/index.ts to spread reportHandlers.
  </action>
  <verify>
Run `pnpm turbo build --filter=@glimmora/admin-panel` -- builds without errors.
Verify reports page: `ls apps/admin-panel/src/app/\(app\)/reports/page.tsx` -- exists.
Verify builder page: `ls apps/admin-panel/src/app/\(app\)/reports/builder/page.tsx` -- exists.
Verify CSV utility: `grep "exportCSV" apps/admin-panel/src/lib/export-csv.ts` -- must match.
Verify CSV usage: `grep "exportCSV" apps/admin-panel/src/components/reports/report-builder-form.tsx` -- must match.
Verify PDF dynamic import: `grep "await import.*@react-pdf/renderer" apps/admin-panel/src/components/reports/report-builder-form.tsx` -- must match.
Verify PDF NOT barrel-exported: `grep -c "ReportPDF\|report-pdf" apps/admin-panel/src/components/reports/index.ts` -- must be 0.
Verify DatePicker: `grep "DatePicker" apps/admin-panel/src/components/reports/report-builder-form.tsx` -- must match.
Verify recharts: `grep "recharts\|BarChart" apps/admin-panel/src/components/reports/report-builder-form.tsx` -- must match.
  </verify>
  <done>Reports section: 5 report type cards, custom report builder with date range + type + filters, DataTable preview, BarChart visualization, CSV export via Blob utility, PDF export via dynamic @react-pdf/renderer import. ReportPDF document NOT barrel-exported.</done>
</task>

<task type="auto">
  <name>Task 2: Build skill taxonomy, announcements, APG config (Super Admin), audit log, and admin role management</name>
  <files>
    apps/admin-panel/src/app/(app)/content/skills/page.tsx
    apps/admin-panel/src/app/(app)/content/announcements/page.tsx
    apps/admin-panel/src/components/content/skill-taxonomy-tree.tsx
    apps/admin-panel/src/components/content/announcements-form.tsx
    apps/admin-panel/src/app/(app)/content/resources/page.tsx
    apps/admin-panel/src/components/content/resource-library-table.tsx
    apps/admin-panel/src/components/content/index.ts
    apps/admin-panel/src/app/(app)/apg-config/page.tsx
    apps/admin-panel/src/components/apg-config/config-card.tsx
    apps/admin-panel/src/components/apg-config/super-admin-gate.tsx
    apps/admin-panel/src/components/apg-config/index.ts
    apps/admin-panel/src/app/(app)/audit-log/page.tsx
    apps/admin-panel/src/components/audit-log/audit-log-table.tsx
    apps/admin-panel/src/components/audit-log/index.ts
    apps/admin-panel/src/app/(app)/settings/page.tsx
    apps/admin-panel/src/components/settings/admin-role-management.tsx
    apps/admin-panel/src/components/settings/index.ts
    apps/admin-panel/src/lib/msw/factories/audit-log.ts
    apps/admin-panel/src/lib/msw/factories/skill-taxonomy.ts
    apps/admin-panel/src/lib/msw/factories/apg-config.ts
    apps/admin-panel/src/lib/msw/handlers/content.ts
    apps/admin-panel/src/lib/msw/handlers/apg-config.ts
    apps/admin-panel/src/lib/msw/handlers/audit-log.ts
    apps/admin-panel/src/lib/msw/handlers/index.ts
  </files>
  <action>
**Step 1: Create SuperAdminGate component.**

`apps/admin-panel/src/components/apg-config/super-admin-gate.tsx`:
'use client'

Reusable gating component. Follow the EXACT pattern from 06-RESEARCH.md:
```typescript
function SuperAdminGate({ children }: { children: React.ReactNode }) {
  const { adminRole } = useAuthStore()

  if (adminRole !== 'super_admin') {
    return (
      <div className="flex flex-col items-center justify-center py-16 space-y-4">
        <Shield className="h-12 w-12 text-text-caption" />
        <Heading level="h3">Super Admin Access Required</Heading>
        <Body className="text-text-caption text-center max-w-md">
          This section is restricted to Super Admin users. Contact your platform
          administrator if you need access.
        </Body>
      </div>
    )
  }

  return <>{children}</>
}
```

**Step 2: Create skill taxonomy management page.**

`apps/admin-panel/src/app/(app)/content/skills/page.tsx`:
'use client'

Fetches GET /api/admin/content/skills via TanStack useQuery. Renders:
1. PageHeader "Skill Taxonomy" with "Add Skill" button
2. SkillTaxonomyTree component

`apps/admin-panel/src/components/content/skill-taxonomy-tree.tsx`:
'use client'

Displays skills organized by category:
- Category sections (accordion or Card groups): "Programming", "Design", "Data", "Communication", "Project Management", etc.
- Each skill as a Tag from @glimmora/ui with:
  - Skill name
  - Usage count badge
  - Active/inactive toggle (Switch from @glimmora/ui)
  - Edit button (opens inline edit: rename)
  - Merge button (opens dialog: select target skill to merge into)

Add Skill form: Dialog with name input, category Select, parent skill Combobox (optional).

MSW endpoints:
- POST /api/admin/content/skills -> create skill
- PATCH /api/admin/content/skills/:id -> update name, category, isActive
- POST /api/admin/content/skills/:id/merge -> merge into target skill

Props: `{ skills: SkillTaxonomyTag[] }`

**Step 3: Create platform announcements, resource library, and content barrel export.**

`apps/admin-panel/src/app/(app)/content/announcements/page.tsx`:
'use client'

Fetches GET /api/admin/content/announcements via TanStack useQuery. Renders:
1. PageHeader "Platform Announcements" with "New Announcement" button
2. DataTable of announcements: Title, Audience (Badge), Status (Badge: draft/published/archived), Published Date, Actions (Edit, Publish, Archive)
3. Below or via dialog: AnnouncementsForm component

`apps/admin-panel/src/components/content/announcements-form.tsx`:
'use client'

Form for creating/editing announcements:
- Title (TextInput)
- Content (Textarea, multiline)
- Audience (Select: All / Contributors / Enterprise / Mentors / Admins)
- Status: Draft (save) or Published (publish)
- Save Draft button (variant="secondary") + Publish button (variant="primary")

Calls POST /api/admin/content/announcements (create) or PATCH /api/admin/content/announcements/:id (update).

`apps/admin-panel/src/app/(app)/content/resources/page.tsx`:
'use client'

Fetches GET /api/admin/content/resources via TanStack useQuery. Renders:
1. PageHeader "Resource Library" with "Add Resource" button that opens a Dialog
2. ResourceLibraryTable component with fetched data
3. Add Resource Dialog form with:
   - Title (TextInput)
   - Type (Select: guide / policy / template)
   - File URL (TextInput for S3/CDN URL)
   - Save button -> POST /api/admin/content/resources

`apps/admin-panel/src/components/content/resource-library-table.tsx`:
'use client'

DataTable<ResourceItem> with columns:
- Title (text)
- Type (Badge: guide=normal, policy=inprogress, template=atrisk)
- Updated At (formatted date)
- Actions: Edit button (opens Dialog pre-filled with resource data, PATCH /api/admin/content/resources/:id), Delete button (confirmation Dialog, DELETE /api/admin/content/resources/:id)

Props: `{ resources: ResourceItem[] }`

Note: The ResourceItem type must be defined in `packages/types/src/admin.ts` (this file is created in 06-01). The executor should add the ResourceItem interface to that file:
```typescript
interface ResourceItem {
  id: string
  title: string
  type: 'guide' | 'policy' | 'template'
  fileUrl: string
  updatedAt: string
  createdBy: string
}
```

`apps/admin-panel/src/components/content/index.ts`:
Barrel export SkillTaxonomyTree, AnnouncementsForm, ResourceLibraryTable.

**Step 4: Create APG Configuration page (Super Admin only).**

`apps/admin-panel/src/app/(app)/apg-config/page.tsx`:
'use client'

Wraps content in SuperAdminGate:
```typescript
<SuperAdminGate>
  {/* APG config content */}
</SuperAdminGate>
```

Inside the gate, fetches GET /api/admin/apg-config via TanStack useQuery. Renders:
1. PageHeader "APG Configuration" with warning caption: "Changes affect platform-wide AI governance behavior"
2. Three sections (one per APGConfigDomain):
   - "Thresholds" -- auto-approval confidence threshold, minimum evidence score, SLA warning hours
   - "Auto-Approval Rules" -- max auto-approval amount, allowed task types, require mentor verification
   - "Escalation Triggers" -- consecutive failures threshold, payment dispute auto-escalate, safety keyword detection

Each section renders ConfigCard components.

`apps/admin-panel/src/components/apg-config/config-card.tsx`:
'use client'

Inline-editable card for a single APG config entry:
- View mode: label, current value, description, last updated by + date
- Edit button -> switches to form mode:
  - Input for value (type depends on value type: TextInput for string, number input for number, Switch for boolean)
  - Save button + Cancel button
  - On save: PATCH /api/admin/apg-config/:id -> refetches

Props: `{ entry: APGConfigEntry; onSave: (id: string, value: string | number | boolean) => Promise<void> }`

`apps/admin-panel/src/components/apg-config/index.ts`:
Barrel export ConfigCard, SuperAdminGate.

**Step 5: Create platform audit log page.**

`apps/admin-panel/src/app/(app)/audit-log/page.tsx`:
'use client'

Fetches GET /api/admin/audit-log via TanStack useQuery. Renders:
1. PageHeader "Platform Audit Log"
2. Filter bar: TextInput search (by actor, action, entity) + Select for actionCategory (All / User Management / Project Intervention / Dispute Resolution / Content Management / APG Configuration / System) + DatePicker for date range
3. AuditLogTable with filtered data (useMemo pattern)

`apps/admin-panel/src/components/audit-log/audit-log-table.tsx`:
'use client'

DataTable with columns:
- Timestamp (formatted with date-fns format: 'yyyy-MM-dd HH:mm:ss')
- Actor (admin name)
- Category (Badge with actionCategory)
- Action (descriptive text)
- Affected Entity (type + link to entity page)
- Reason (text, truncated with tooltip for full text)

CRITICAL: NO action column. NO edit/delete buttons. This table is READ-ONLY. Audit entries are immutable (isImmutable: true).

Props: `{ entries: PlatformAuditEntry[] }`

`apps/admin-panel/src/components/audit-log/index.ts`:
Barrel export AuditLogTable.

**Step 6: Create settings page with admin role management (Super Admin only).**

`apps/admin-panel/src/app/(app)/settings/page.tsx`:
'use client'

Renders:
1. PageHeader "Admin Settings"
2. SuperAdminGate wrapping AdminRoleManagement component

`apps/admin-panel/src/components/settings/admin-role-management.tsx`:
'use client'

Fetches GET /api/admin/settings/admins via TanStack useQuery. Shows:
- DataTable of admin users: Name, Email, Role (Badge: standard_admin/super_admin), Last Login, Status
- "Change Role" action per admin: Select dropdown to switch between standard_admin and super_admin
- Calls PATCH /api/admin/settings/admins/:id with { adminRole: newRole }
- "Invite Admin" button -> Dialog with email input + role Select

Props: none (fetches its own data)

`apps/admin-panel/src/components/settings/index.ts`:
Barrel export AdminRoleManagement.

**Step 7: Create MSW factories and handlers.**

`apps/admin-panel/src/lib/msw/factories/audit-log.ts`:
- `createMockAuditEntries()` -- returns 25-30 PlatformAuditEntry objects across all categories:
  - user_management: suspend, approve_verification, reject_verification
  - project_intervention: freeze, unfreeze, payment_hold
  - dispute_resolution: decision_made, escalated, safety_case_created
  - content_management: skill_added, announcement_published
  - apg_configuration: threshold_updated, rule_modified
  - system: maintenance, backup

`apps/admin-panel/src/lib/msw/factories/skill-taxonomy.ts`:
- `createMockTaxonomy()` -- returns 35-40 SkillTaxonomyTag objects organized in categories:
  - Programming: TypeScript, Python, React, Node.js, SQL, Go
  - Design: UI Design, UX Research, Figma, Prototyping
  - Data: Data Analysis, Machine Learning, ETL, Visualization
  - Communication: Technical Writing, Documentation, Presentation
  - Project Management: Agile, Scrum, Risk Management

`apps/admin-panel/src/lib/msw/factories/apg-config.ts`:
- `createMockAPGConfig()` -- returns 10-12 APGConfigEntry objects across 3 domains:
  - thresholds: auto_approval_confidence (0.85), min_evidence_score (70), sla_warning_hours (4)
  - auto_approval_rules: max_auto_amount (5000), allowed_types ("code,document"), require_mentor (true)
  - escalation_triggers: consecutive_failures (3), payment_auto_escalate (true), safety_keywords_enabled (true)

`apps/admin-panel/src/lib/msw/handlers/content.ts`:
- GET /api/admin/content/skills -> returns createMockTaxonomy()
- POST /api/admin/content/skills -> returns 201
- PATCH /api/admin/content/skills/:id -> returns 200
- POST /api/admin/content/skills/:id/merge -> returns 200
- GET /api/admin/content/announcements -> returns 5-6 mock announcements
- POST /api/admin/content/announcements -> returns 201
- PATCH /api/admin/content/announcements/:id -> returns 200
- GET /api/admin/content/resources -> returns array of 5-6 ResourceItem objects (mix of guides, policies, templates with titles like "Contributor Onboarding Guide", "Platform Privacy Policy", "Deliverable Template")
- POST /api/admin/content/resources -> returns 201
- PATCH /api/admin/content/resources/:id -> returns 200
- DELETE /api/admin/content/resources/:id -> returns 204

`apps/admin-panel/src/lib/msw/handlers/apg-config.ts`:
- GET /api/admin/apg-config -> returns createMockAPGConfig()
- PATCH /api/admin/apg-config/:id -> returns 200

`apps/admin-panel/src/lib/msw/handlers/audit-log.ts`:
- GET /api/admin/audit-log -> returns createMockAuditEntries() with category/search query param filtering
- GET /api/admin/settings/admins -> returns 4-5 mock admin users with mixed roles
- PATCH /api/admin/settings/admins/:id -> returns 200

Update handlers/index.ts to spread reportHandlers, contentHandlers, apgConfigHandlers, auditLogHandlers.
  </action>
  <verify>
Run `pnpm turbo build --filter=@glimmora/admin-panel` -- builds without errors.
Verify reports: `ls apps/admin-panel/src/app/\(app\)/reports/page.tsx apps/admin-panel/src/app/\(app\)/reports/builder/page.tsx` -- both exist.
Verify CSV export: `grep "exportCSV" apps/admin-panel/src/lib/export-csv.ts` -- must match.
Verify PDF dynamic import: `grep "await import.*@react-pdf/renderer" apps/admin-panel/src/components/reports/report-builder-form.tsx` -- must match.
Verify PDF NOT exported: `grep -c "ReportPDF\|report-pdf" apps/admin-panel/src/components/reports/index.ts` -- must be 0.
Verify skill taxonomy: `ls apps/admin-panel/src/app/\(app\)/content/skills/page.tsx` -- exists.
Verify announcements: `ls apps/admin-panel/src/app/\(app\)/content/announcements/page.tsx` -- exists.
Verify resource library: `ls apps/admin-panel/src/app/\(app\)/content/resources/page.tsx` -- exists.
Verify resource table: `grep "ResourceLibraryTable" apps/admin-panel/src/components/content/resource-library-table.tsx` -- must match.
Verify resource barrel export: `grep "ResourceLibraryTable" apps/admin-panel/src/components/content/index.ts` -- must match.
Verify APG config: `ls apps/admin-panel/src/app/\(app\)/apg-config/page.tsx` -- exists.
Verify SuperAdminGate: `grep "SuperAdminGate" apps/admin-panel/src/app/\(app\)/apg-config/page.tsx` -- must match.
Verify audit log read-only: `grep -c "delete\|edit" apps/admin-panel/src/components/audit-log/audit-log-table.tsx` -- should be 0 (no edit/delete actions).
Verify settings: `ls apps/admin-panel/src/app/\(app\)/settings/page.tsx` -- exists.
Verify admin role management: `grep "SuperAdminGate" apps/admin-panel/src/app/\(app\)/settings/page.tsx` -- must match.
Verify all handlers aggregated: `grep "reportHandlers\|contentHandlers\|apgConfigHandlers\|auditLogHandlers" apps/admin-panel/src/lib/msw/handlers/index.ts` -- must match all 4.
  </verify>
  <done>Reports with 5 types + custom builder + CSV/PDF export. Skill taxonomy with add/edit/merge. Resource library with DataTable of guides/policies/templates and add/edit/delete. Announcements with audience targeting. APG Configuration gated by SuperAdminGate with inline-editable cards. Platform audit log (read-only, immutable). Admin role management (Super Admin only). All MSW handlers aggregated.</done>
</task>

</tasks>

<verification>
1. `pnpm turbo build --filter=@glimmora/admin-panel` builds without errors
2. Reports page shows 5 report type cards
3. Custom report builder has DatePicker range, type Select, filters, preview DataTable, recharts BarChart
4. CSV export works via Blob + URL.createObjectURL (no external library)
5. PDF export uses dynamic import of @react-pdf/renderer (NOT top-level import)
6. ReportPDF document is NOT barrel-exported
7. Skill taxonomy page shows skills by category with add/edit/merge/toggle actions
8. Announcements page has form with audience targeting and publish/draft status
9. Resource library at /content/resources shows DataTable of guides/policies/templates with add/edit/delete
10. APG Config page shows SuperAdminGate for standard admins (Shield + message)
11. APG Config shows 3 domain cards with inline editing for Super Admin
12. Platform audit log is read-only DataTable with NO edit/delete buttons
13. Admin settings with role management wrapped in SuperAdminGate
14. DevTools role-switcher (from 06-01) allows testing both Standard and Super Admin views
15. All MSW handlers aggregated in handlers/index.ts
</verification>

<success_criteria>
- Admin can generate 5 report types with date range filtering and export as CSV or PDF
- Admin can manage the skill taxonomy (add, edit, merge, toggle skills)
- Admin can manage the resource library (add, edit, delete guides/policies/templates)
- Admin can create and publish platform announcements with audience targeting
- Super Admin can view and modify APG configuration (standard admin sees locked message)
- Admin can view the full immutable platform audit log
- Super Admin can manage admin roles (standard admin sees locked message)
- All admin capabilities from AP-15 through AP-23 are functional
</success_criteria>

<output>
After completion, create `.planning/phases/06-admin-panel/06-05-SUMMARY.md`
</output>
