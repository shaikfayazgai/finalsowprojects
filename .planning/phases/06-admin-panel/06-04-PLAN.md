---
phase: 06-admin-panel
plan: 04
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - apps/admin-panel/src/app/(app)/disputes/page.tsx
  - apps/admin-panel/src/app/(app)/disputes/[disputeId]/page.tsx
  - apps/admin-panel/src/app/(app)/disputes/safety/page.tsx
  - apps/admin-panel/src/app/(app)/disputes/safety/[caseId]/page.tsx
  - apps/admin-panel/src/components/disputes/dispute-queue-table.tsx
  - apps/admin-panel/src/components/disputes/dispute-detail-layout.tsx
  - apps/admin-panel/src/components/disputes/case-context-panel.tsx
  - apps/admin-panel/src/components/disputes/evidence-messages-panel.tsx
  - apps/admin-panel/src/components/disputes/decision-form-panel.tsx
  - apps/admin-panel/src/components/disputes/dispute-audit-trail.tsx
  - apps/admin-panel/src/components/disputes/safety-case-view.tsx
  - apps/admin-panel/src/components/disputes/safety-case-list.tsx
  - apps/admin-panel/src/components/disputes/index.ts
  - apps/admin-panel/src/lib/msw/factories/dispute.ts
  - apps/admin-panel/src/lib/msw/handlers/disputes.ts
  - apps/admin-panel/src/lib/msw/handlers/index.ts
autonomous: true

must_haves:
  truths:
    - "Dispute queue shows DataTable filterable by 5 types (Payment, Quality, Conduct, Technical, Safety) and severity"
    - "Dispute detail uses 3-panel ResizablePanelGroup layout: left (case context 25%), center (evidence/messages 45%), right (decision form 30%)"
    - "3-panel layout mirrors mentor-portal review layout pattern (ResizablePanelGroup from @glimmora/ui)"
    - "Decision form supports 5 decision types: resolved_favor_requester, resolved_favor_contributor, partial_resolution, escalated_to_safety, dismissed"
    - "Safety Case at /admin/disputes/safety is a dedicated section with noticeably more serious visual treatment (more whitespace, privacy indicators)"
    - "Safety Case shows Privacy Restricted Badge with Shield icon and Evidence Preserved indicator"
    - "Per-dispute Audit Log tab shows case-specific history of all actions"
    - "Safety Case decision form does NOT include 'dismissed' option -- only resolved and escalated"
  artifacts:
    - path: "apps/admin-panel/src/app/(app)/disputes/page.tsx"
      provides: "Dispute queue (AP-11)"
    - path: "apps/admin-panel/src/app/(app)/disputes/[disputeId]/page.tsx"
      provides: "Dispute detail with 3-panel layout (AP-12)"
    - path: "apps/admin-panel/src/app/(app)/disputes/safety/page.tsx"
      provides: "Safety Case protocol list (AP-13)"
    - path: "apps/admin-panel/src/app/(app)/disputes/safety/[caseId]/page.tsx"
      provides: "Safety Case detail (AP-13)"
    - path: "apps/admin-panel/src/components/disputes/dispute-detail-layout.tsx"
      provides: "3-panel dispute review layout"
      contains: "DisputeDetailLayout"
    - path: "apps/admin-panel/src/components/disputes/decision-form-panel.tsx"
      provides: "5 decision types for disputes"
      contains: "DecisionFormPanel"
    - path: "apps/admin-panel/src/components/disputes/dispute-audit-trail.tsx"
      provides: "Per-dispute audit history (AP-14)"
      contains: "DisputeAuditTrail"
  key_links:
    - from: "apps/admin-panel/src/components/disputes/dispute-detail-layout.tsx"
      to: "@glimmora/ui ResizablePanelGroup"
      via: "3-panel layout reusing mentor review pattern"
      pattern: "import.*ResizablePanelGroup.*ResizablePanel.*ResizableHandle.*from.*@glimmora/ui"
    - from: "apps/admin-panel/src/components/disputes/evidence-messages-panel.tsx"
      to: "@glimmora/ui EvidenceViewer"
      via: "Evidence display within dispute context"
      pattern: "import.*EvidenceViewer.*from.*@glimmora/ui"
    - from: "apps/admin-panel/src/components/disputes/safety-case-view.tsx"
      to: "lucide-react Shield"
      via: "Privacy Restricted indicator"
      pattern: "import.*Shield.*from.*lucide-react"
---

<objective>
Build the complete dispute resolution section: dispute queue with type/severity filtering, 3-panel dispute detail layout (mirroring mentor review pattern), 5 decision types, Safety Case protocol with dedicated section and enhanced visual treatment, and per-dispute audit trail -- fulfilling AP-11, AP-12, AP-13, and AP-14.

Purpose: Dispute resolution is the most governance-critical admin function. It requires a 3-panel layout that mirrors what mentors already use (giving admins familiar patterns), plus a dedicated Safety Case workflow for the highest-severity situations. The Safety Case section needs noticeably more serious visual treatment -- not alarming, but gravity-appropriate -- with visible privacy indicators and evidence preservation notices.

Output: Dispute queue page with type/severity filters, dispute detail with 3-panel ResizablePanelGroup layout, decision form with 5 types, Safety Case at /disputes/safety with privacy indicators, per-dispute audit trail, and MSW handlers for all dispute endpoints.
</objective>

<execution_context>
@/Users/kavi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kavi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-admin-panel/06-RESEARCH.md
@.planning/phases/06-admin-panel/06-01-SUMMARY.md

Key codebase references:
@packages/types/src/admin.ts (Dispute, DisputeType, DisputeSeverity, DisputeStatus, DisputeDecisionType, DisputeEvidence, DisputeMessage, DisputeDecision, SafetyCase)
@packages/ui/src/components/resizable-panel/resizable-panel.tsx (ResizablePanelGroup, ResizablePanel, ResizableHandle)
@packages/ui/src/components/evidence-viewer/evidence-viewer.tsx
@packages/ui/src/components/badge/badge.tsx
@apps/mentor-portal/src/components/review-detail/review-layout.tsx (3-panel layout to mirror)
@apps/mentor-portal/src/components/review-detail/review-form-panel.tsx (decision form pattern)
@apps/admin-panel/src/components/users/user-action-dialog.tsx (confirmation dialog pattern)
@apps/admin-panel/src/store/auth-store.ts (adminRole for Super Admin gating)

CRITICAL constraints:
- 3-panel layout: ResizablePanelGroup with orientation="horizontal", left 25% / center 45% / right 30%
- autoSaveId prop does NOT exist on ResizablePanelGroup v4 -- do NOT pass it (per decision [04-04])
- Safety Case visual treatment: more whitespace, bg-status-urgent/5 subtle background on header, Shield icon, "Privacy Restricted" Badge
- Safety Case decision form omits 'dismissed' option (only resolved, escalated)
- Dispute audit trail is per-dispute (case-specific) -- platform-wide audit log is in Plan 06-05
- Evidence viewer hides contributor identity (same constraint as everywhere)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build dispute queue page and 3-panel dispute detail layout with decision form</name>
  <files>
    apps/admin-panel/src/app/(app)/disputes/page.tsx
    apps/admin-panel/src/app/(app)/disputes/[disputeId]/page.tsx
    apps/admin-panel/src/components/disputes/dispute-queue-table.tsx
    apps/admin-panel/src/components/disputes/dispute-detail-layout.tsx
    apps/admin-panel/src/components/disputes/case-context-panel.tsx
    apps/admin-panel/src/components/disputes/evidence-messages-panel.tsx
    apps/admin-panel/src/components/disputes/decision-form-panel.tsx
    apps/admin-panel/src/components/disputes/dispute-audit-trail.tsx
    apps/admin-panel/src/components/disputes/index.ts
    apps/admin-panel/src/lib/msw/factories/dispute.ts
    apps/admin-panel/src/lib/msw/handlers/disputes.ts
    apps/admin-panel/src/lib/msw/handlers/index.ts
  </files>
  <action>
**Step 1: Create dispute queue page.**

`apps/admin-panel/src/app/(app)/disputes/page.tsx`:
'use client'

Fetches GET /api/admin/disputes via TanStack useQuery. Renders:
1. PageHeader "Dispute Resolution" with right action: Link to "/disputes/safety" (Safety Cases badge showing count of safety cases)
2. Filter bar: Select for type (All / Payment / Quality / Conduct / Technical / Safety) + Select for severity (All / Low / Medium / High / Critical) + Select for status (All / Open / Under Review / Awaiting Evidence / Resolved / Escalated)
3. DisputeQueueTable with filtered data (useMemo pattern)

`apps/admin-panel/src/components/disputes/dispute-queue-table.tsx`:
'use client'

DataTable (ticket-style list) with columns:
- ID (short dispute ID, clickable -> /disputes/:id)
- Title (text, truncated)
- Type (Badge: payment=normal, quality=inprogress, conduct=atrisk, technical=normal, safety=urgent)
- Severity (Badge: low=normal, medium=inprogress, high=atrisk, critical=urgent)
- Status (Badge)
- Project (link to /projects/:projectId)
- Created (relative time)
- Assigned To (admin name or "Unassigned")

Sort by: severity descending (critical first), then created ascending (oldest first).

Props: `{ disputes: Dispute[] }`

**Step 2: Create dispute detail page and 3-panel layout.**

`apps/admin-panel/src/app/(app)/disputes/[disputeId]/page.tsx`:
'use client'

Reads disputeId from URL params. Fetches GET /api/admin/disputes/:id via TanStack useQuery. Renders:
1. PageHeader with dispute title + type Badge + severity Badge + status Badge
2. Tabs for main content vs audit trail:
   - "Case Review" tab -> DisputeDetailLayout (3-panel)
   - "Audit Trail" tab -> DisputeAuditTrail
3. If dispute is resolved, show resolution summary banner at top

`apps/admin-panel/src/components/disputes/dispute-detail-layout.tsx`:
'use client'

3-panel layout using ResizablePanelGroup from @glimmora/ui:

```typescript
<ResizablePanelGroup direction="horizontal">
  <ResizablePanel defaultSize={25} minSize={20} maxSize={35}>
    <CaseContextPanel dispute={dispute} />
  </ResizablePanel>
  <ResizableHandle />
  <ResizablePanel defaultSize={45} minSize={30}>
    <EvidenceMessagesPanel disputeId={dispute.id} />
  </ResizablePanel>
  <ResizableHandle />
  <ResizablePanel defaultSize={30} minSize={25} maxSize={40}>
    <DecisionFormPanel disputeId={dispute.id} disputeType={dispute.type} />
  </ResizablePanel>
</ResizablePanelGroup>
```

DO NOT pass autoSaveId prop (v4 does not support it).

Props: `{ dispute: Dispute }`

**Step 3: Create the 3 panel components.**

`apps/admin-panel/src/components/disputes/case-context-panel.tsx`:
'use client'

Left panel (25%) showing:
- Dispute type + severity badges
- Project info: project name (link), enterprise organization
- Parties involved:
  - Requester: name + role
  - Contributor: seed (anonymized) -- NO real name
- Key dates: filed, last updated, SLA deadline
- Related entities: evidence pack link, payment record link (if payment dispute)

Props: `{ dispute: Dispute }`

`apps/admin-panel/src/components/disputes/evidence-messages-panel.tsx`:
'use client'

Center panel (45%) with two sub-tabs (Tabs component):

1. "Evidence" tab:
   - Fetches GET /api/admin/disputes/:id/evidence
   - Lists DisputeEvidence items chronologically
   - Each item shows: submitter role badge (requester/contributor/admin/system), type badge, content
   - For file/screenshot type: shows file preview or image
   - Uses EvidenceViewer from @glimmora/ui for document/code evidence

2. "Messages" tab:
   - Fetches GET /api/admin/disputes/:id/messages
   - Chat-style message list with senderRole badges (requester/contributor/admin)
   - Admin can send a message: Textarea + Send button -> POST /api/admin/disputes/:id/messages
   - Messages sorted chronologically

Props: `{ disputeId: string }`

`apps/admin-panel/src/components/disputes/decision-form-panel.tsx`:
'use client'

Right panel (30%) with the decision form:

1. Status: shows current dispute status
2. Decision type Select with 5 options:
   - "Resolve: Favor Requester" (resolved_favor_requester)
   - "Resolve: Favor Contributor" (resolved_favor_contributor)
   - "Partial Resolution" (partial_resolution)
   - "Escalate to Safety Case" (escalated_to_safety)
   - "Dismiss" (dismissed)
3. Summary Textarea (required, 2-3 sentences)
4. Detailed Reasoning Textarea (required, full explanation)
5. Financial Resolution (conditional, shown for payment disputes):
   - Refund Amount (number input)
   - Additional Payment (number input)
   - Currency (text, pre-filled from dispute)
6. Submit Decision button -> POST /api/admin/disputes/:id/decision

If dispute.type === 'safety', hide the "Dismiss" option (safety cases cannot be dismissed).
If dispute is already resolved, show the existing decision read-only.

Props: `{ disputeId: string; disputeType: DisputeType }`

**Step 4: Create dispute audit trail.**

`apps/admin-panel/src/components/disputes/dispute-audit-trail.tsx`:
'use client'

Fetches GET /api/admin/disputes/:id/audit via TanStack useQuery. Shows:
- Vertical timeline (border-l-2 pattern) of all actions on this dispute
- Each entry: timestamp, actor name, action description, details
- Actions include: created, assigned, evidence_submitted, message_sent, decision_made, escalated, resolved
- Read-only -- no editing of audit entries

Props: `{ disputeId: string }`

**Step 5: Create MSW dispute factories and handlers.**

`apps/admin-panel/src/lib/msw/factories/dispute.ts`:
- `createMockDisputeList()` -- returns 12-15 disputes across all 5 types with mixed severities and statuses:
  - 3 payment disputes (1 open, 1 under_review, 1 resolved)
  - 3 quality disputes (2 open, 1 resolved)
  - 2 conduct disputes (1 open, 1 escalated)
  - 2 technical disputes (1 awaiting_evidence, 1 resolved)
  - 2 safety disputes (1 open, 1 under_review -- marked isSafetyCase: true)
- `createMockDisputeDetail(id)` -- returns full dispute detail
- `createMockSafetyCaseList()` -- returns 3-4 SafetyCase objects with isSafetyCase: true, severity: 'critical'
- `createMockSafetyCaseDetail(id)` -- returns SafetyCase with privacyRestricted, evidencePreserved, accessRestrictions
- `createMockDisputeEvidence(disputeId)` -- returns 4-6 evidence items from different submitters
- `createMockDisputeMessages(disputeId)` -- returns 5-8 messages between requester/contributor/admin
- `createMockDisputeAudit(disputeId)` -- returns 8-12 audit entries covering the dispute lifecycle

`apps/admin-panel/src/lib/msw/handlers/disputes.ts`:
- GET /api/admin/disputes -> returns createMockDisputeList() with type/severity/status query param filtering
- GET /api/admin/disputes/:id -> returns createMockDisputeDetail(id)
- GET /api/admin/disputes/:id/evidence -> returns createMockDisputeEvidence(id)
- GET /api/admin/disputes/:id/messages -> returns createMockDisputeMessages(id)
- POST /api/admin/disputes/:id/messages -> accepts { content }, returns 201
- POST /api/admin/disputes/:id/decision -> accepts DisputeDecision body, returns 200
- GET /api/admin/disputes/:id/audit -> returns createMockDisputeAudit(id)
- GET /api/admin/disputes/safety -> returns createMockSafetyCaseList()
- GET /api/admin/disputes/safety/:id -> returns createMockSafetyCaseDetail(id)

IMPORTANT: Define the `/api/admin/disputes/safety` route BEFORE `/api/admin/disputes/:id` in the handler array to prevent path shadowing (same pattern as enterprise /projects/completed route).

Update handlers/index.ts to spread disputeHandlers.

`apps/admin-panel/src/components/disputes/index.ts`:
Barrel export all dispute components.
  </action>
  <verify>
Run `pnpm turbo build --filter=@glimmora/admin-panel` -- builds without errors.
Verify dispute queue: `ls apps/admin-panel/src/app/\(app\)/disputes/page.tsx` -- exists.
Verify dispute detail: `ls apps/admin-panel/src/app/\(app\)/disputes/\[disputeId\]/page.tsx` -- exists.
Verify 3-panel layout: `grep "ResizablePanelGroup" apps/admin-panel/src/components/disputes/dispute-detail-layout.tsx` -- must match.
Verify NO autoSaveId: `grep -c "autoSaveId" apps/admin-panel/src/components/disputes/dispute-detail-layout.tsx` -- must be 0.
Verify 5 decision types: `grep "resolved_favor_requester\|resolved_favor_contributor\|partial_resolution\|escalated_to_safety\|dismissed" apps/admin-panel/src/components/disputes/decision-form-panel.tsx` -- must match all 5.
Verify EvidenceViewer: `grep "EvidenceViewer" apps/admin-panel/src/components/disputes/evidence-messages-panel.tsx` -- must match.
Verify safety route: `ls apps/admin-panel/src/app/\(app\)/disputes/safety/page.tsx` -- exists.
Verify route order: `grep -n "safety\|:id" apps/admin-panel/src/lib/msw/handlers/disputes.ts` -- safety route should appear before :id route.
  </verify>
  <done>Dispute queue with type/severity/status filtering. 3-panel dispute detail (ResizablePanelGroup mirroring mentor review pattern). Decision form with 5 decision types. Per-dispute audit trail. Safety route registered before parameterized route to prevent shadowing.</done>
</task>

<task type="auto">
  <name>Task 2: Build Safety Case protocol with dedicated section and enhanced visual treatment</name>
  <files>
    apps/admin-panel/src/app/(app)/disputes/safety/page.tsx
    apps/admin-panel/src/app/(app)/disputes/safety/[caseId]/page.tsx
    apps/admin-panel/src/components/disputes/safety-case-list.tsx
    apps/admin-panel/src/components/disputes/safety-case-view.tsx
  </files>
  <action>
**Step 1: Create Safety Case list page.**

`apps/admin-panel/src/app/(app)/disputes/safety/page.tsx`:
'use client'

Fetches GET /api/admin/disputes/safety via TanStack useQuery. Renders:
1. PageHeader "Safety Case Protocol" with back link to /disputes
2. Brief description: "Safety cases represent the highest severity incidents requiring full privacy protection and evidence preservation."
3. SafetyCaseList component

`apps/admin-panel/src/components/disputes/safety-case-list.tsx`:
'use client'

Enhanced visual treatment compared to regular dispute queue:
- More whitespace (p-6 instead of p-4)
- Each case rendered as a Card (not a dense table row) with:
  - Subtle urgent background: `bg-status-urgent/5` on the card header area
  - Privacy Restricted Badge with Shield icon from lucide-react
  - Evidence Preserved indicator (CheckCircle icon + "Evidence Preserved" text)
  - Case title, severity badge (always "critical"), status badge
  - Project name, filed date
  - Click navigates to /disputes/safety/:id

Props: `{ cases: SafetyCase[] }`

**Step 2: Create Safety Case detail page.**

`apps/admin-panel/src/app/(app)/disputes/safety/[caseId]/page.tsx`:
'use client'

Reads caseId from URL params. Fetches GET /api/admin/disputes/safety/:id via TanStack useQuery. Renders:
1. Header area with enhanced visual treatment:
   - Subtle `bg-status-urgent/5` background on the header section
   - Shield icon + "Safety Case" label
   - "Privacy Restricted" Badge (status="urgent")
   - "Evidence Preserved" Badge with CheckCircle icon
   - Access restriction notice: "Access to this case is restricted and all actions are logged"
2. SafetyCaseView component (reuses 3-panel layout with safety modifications)

`apps/admin-panel/src/components/disputes/safety-case-view.tsx`:
'use client'

Uses the same 3-panel ResizablePanelGroup layout as regular dispute detail, BUT with modifications:

Left Panel (CaseContextPanel) -- enhanced:
- Shows privacy restriction indicators
- Access restriction list from SafetyCase.accessRestrictions
- "Evidence Preserved" timestamp
- More whitespace between sections

Center Panel (EvidenceMessagesPanel) -- same component reused:
- Same evidence and messages sub-tabs
- Evidence viewer hides contributor identity as always

Right Panel (Decision Form) -- modified:
- Same DecisionFormPanel BUT with `isSafetyCase={true}` prop
- When isSafetyCase is true:
  - Remove "Dismiss" option from decision types (safety cases CANNOT be dismissed)
  - Available decisions: resolved_favor_requester, resolved_favor_contributor, partial_resolution, escalated_to_safety
  - Add "Privacy Impact Assessment" section: Textarea asking admin to document any privacy implications of the decision
  - Warning text: "Safety Case decisions are permanent and fully audited"

Below the 3-panel layout:
- Audit Trail section (DisputeAuditTrail component) always visible for safety cases (not behind a tab)
- Enhanced audit trail includes access tracking (who viewed the case and when)

Props: `{ safetyCase: SafetyCase }`
  </action>
  <verify>
Run `pnpm turbo build --filter=@glimmora/admin-panel` -- builds without errors.
Verify safety list: `ls apps/admin-panel/src/app/\(app\)/disputes/safety/page.tsx` -- exists.
Verify safety detail: `ls apps/admin-panel/src/app/\(app\)/disputes/safety/\[caseId\]/page.tsx` -- exists.
Verify Shield icon: `grep "Shield" apps/admin-panel/src/components/disputes/safety-case-view.tsx` -- must match.
Verify privacy indicators: `grep "Privacy Restricted\|Evidence Preserved" apps/admin-panel/src/components/disputes/safety-case-view.tsx` -- must match both.
Verify no dismiss for safety: `grep "isSafetyCase" apps/admin-panel/src/components/disputes/decision-form-panel.tsx` -- must match (safety flag filters out dismiss).
Verify enhanced visual: `grep "status-urgent" apps/admin-panel/src/components/disputes/safety-case-list.tsx` -- must match subtle urgent background.
  </verify>
  <done>Safety Case protocol: dedicated /disputes/safety section with enhanced visual treatment (more whitespace, subtle urgent background), Privacy Restricted Badge with Shield icon, Evidence Preserved indicator, access restriction notices. Safety Case decision form removes 'dismissed' option and adds privacy impact assessment. Audit trail always visible for safety cases.</done>
</task>

</tasks>

<verification>
1. `pnpm turbo build --filter=@glimmora/admin-panel` builds without errors
2. Dispute queue at /disputes shows DataTable with type/severity/status filtering
3. Safety Case link prominently visible in dispute queue header
4. Dispute detail uses 3-panel ResizablePanelGroup (left 25%, center 45%, right 30%)
5. 3-panel layout mirrors mentor review pattern (ResizablePanelGroup, same panel structure)
6. Decision form has 5 decision types (resolved_favor_requester, resolved_favor_contributor, partial_resolution, escalated_to_safety, dismissed)
7. Safety Case at /disputes/safety has dedicated list with enhanced visual treatment
8. Safety Case detail shows Privacy Restricted Badge + Shield icon + Evidence Preserved indicator
9. Safety Case decision form excludes 'dismissed' option
10. Per-dispute audit trail shows case-specific history
11. MSW safety routes defined BEFORE parameterized :id routes to prevent path shadowing
</verification>

<success_criteria>
- Admin can process disputes across 5 types with a familiar 3-panel review layout
- Admin can submit decisions with 5 decision types including financial resolution for payment disputes
- Admin can initiate Safety Case protocol with full privacy protection indicators
- Safety Case section feels noticeably more serious (visual treatment) without being alarming
- Per-dispute audit trail tracks all actions on a case
- Dispute evidence displays via EvidenceViewer with contributor identity hidden
</success_criteria>

<output>
After completion, create `.planning/phases/06-admin-panel/06-04-SUMMARY.md`
</output>
