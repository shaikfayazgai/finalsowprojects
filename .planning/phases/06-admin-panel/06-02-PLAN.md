---
phase: 06-admin-panel
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - apps/admin-panel/src/app/(app)/users/page.tsx
  - apps/admin-panel/src/app/(app)/users/[userId]/page.tsx
  - apps/admin-panel/src/app/(app)/users/verification-queue/page.tsx
  - apps/admin-panel/src/components/users/user-list-table.tsx
  - apps/admin-panel/src/components/users/user-detail-tabs.tsx
  - apps/admin-panel/src/components/users/user-profile-tab.tsx
  - apps/admin-panel/src/components/users/user-activity-tab.tsx
  - apps/admin-panel/src/components/users/user-projects-tab.tsx
  - apps/admin-panel/src/components/users/user-payments-tab.tsx
  - apps/admin-panel/src/components/users/user-skills-tab.tsx
  - apps/admin-panel/src/components/users/user-audit-tab.tsx
  - apps/admin-panel/src/components/users/verification-queue-table.tsx
  - apps/admin-panel/src/components/users/user-action-dialog.tsx
  - apps/admin-panel/src/components/users/index.ts
  - apps/admin-panel/src/lib/msw/factories/user.ts
  - apps/admin-panel/src/lib/msw/handlers/users.ts
  - apps/admin-panel/src/lib/msw/handlers/index.ts
autonomous: true

must_haves:
  truths:
    - "User list page shows unified DataTable with all 6 user types, filterable by type via Select dropdown above the table"
    - "User type filter uses useMemo to filter data array before passing to DataTable -- no getFilteredRowModel"
    - "User detail opens at /admin/users/[userId] as full page with 6 tabs: Profile, Activity, Projects, Earnings/Payments, Skill Genome, Audit Log"
    - "Suspend/reactivate actions use UserActionDialog with mandatory reason field written to audit log"
    - "Verification queue at /admin/users/verification-queue shows pending users with approve/reject actions"
    - "Approve/reject verification requires a reason (even for approval -- reason stored in audit trail)"
    - "User list DataTable columns include: name, email, type (Badge), status (Badge), created date, last active, project count"
    - "ID/credential verification submissions viewable from user detail Profile tab"
  artifacts:
    - path: "apps/admin-panel/src/app/(app)/users/page.tsx"
      provides: "User list with type filter (AP-03)"
    - path: "apps/admin-panel/src/app/(app)/users/[userId]/page.tsx"
      provides: "User detail with 6 tabs (AP-04, AP-06, AP-07)"
    - path: "apps/admin-panel/src/app/(app)/users/verification-queue/page.tsx"
      provides: "Verification queue (AP-05)"
    - path: "apps/admin-panel/src/components/users/user-action-dialog.tsx"
      provides: "Confirmation dialog with mandatory reason for admin actions"
      contains: "UserActionDialog"
    - path: "apps/admin-panel/src/components/users/user-detail-tabs.tsx"
      provides: "6-tab user detail component"
      contains: "UserDetailTabs"
  key_links:
    - from: "apps/admin-panel/src/components/users/user-list-table.tsx"
      to: "@glimmora/ui DataTable"
      via: "DataTable with ColumnDef from @tanstack/react-table"
      pattern: "import.*ColumnDef.*from.*@tanstack/react-table"
    - from: "apps/admin-panel/src/components/users/user-action-dialog.tsx"
      to: "@glimmora/ui Dialog"
      via: "Dialog with Textarea reason field and confirmation"
      pattern: "import.*Dialog.*DialogTrigger.*from.*@glimmora/ui"
    - from: "apps/admin-panel/src/components/users/user-skills-tab.tsx"
      to: "@glimmora/ui SkillGenomePanel"
      via: "Private Skill Genome display for admin view"
      pattern: "import.*SkillGenomePanel.*from.*@glimmora/ui"
    - from: "apps/admin-panel/src/lib/msw/handlers/users.ts"
      to: "packages/types/src/admin.ts"
      via: "AdminUserListItem, VerificationQueueItem types shape mock responses"
      pattern: "AdminUserListItem"
---

<objective>
Build the complete user management section: unified user list with type filtering, 6-tab user detail page, verification queue with approve/reject, suspend/reactivate actions with mandatory reason, and ID/credential verification -- fulfilling AP-03, AP-04, AP-05, AP-06, and AP-07.

Purpose: User management is the admin's most frequent task -- browsing users across all 6 types, investigating individual users (activity, projects, payments, skills, audit history), processing the verification queue, and taking state-changing actions (suspend/reactivate) with audit-logged reasons. This plan builds the complete user lifecycle management surface.

Output: User list page with type filter + DataTable, user detail page with 6 functioning tabs, verification queue page, UserActionDialog for all state-changing operations, and MSW handlers for user CRUD + verification.
</objective>

<execution_context>
@/Users/kavi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kavi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-admin-panel/06-RESEARCH.md
@.planning/phases/06-admin-panel/06-01-SUMMARY.md

Key codebase references:
@packages/types/src/admin.ts (AdminUserListItem, VerificationQueueItem, AdminAction, UserAccountStatus, AdminUserType)
@packages/ui/src/components/data-table/data-table.tsx (DataTable props, no filter model)
@packages/ui/src/components/tabs/tabs.tsx (Tabs, TabsList, TabsTrigger, TabsContent)
@packages/ui/src/components/skill-genome-panel/skill-genome-panel.tsx
@packages/ui/src/components/badge/badge.tsx
@apps/enterprise-portal/src/components/payments/payment-history-table.tsx (DataTable + ColumnDef pattern)
@apps/enterprise-portal/src/components/sow/sow-archive-table.tsx (DataTable with actions)
@apps/admin-panel/src/store/auth-store.ts (auth store for admin role)
@apps/admin-panel/src/lib/msw/handlers/index.ts (handler aggregation)

CRITICAL constraints:
- DataTable does NOT have built-in filtering -- build filter UI (TextInput search + Select type filter) ABOVE the table, filter data array with useMemo, pass filtered array to DataTable
- Import ColumnDef from @tanstack/react-table (direct dep, pnpm strict)
- User detail is FULL PAGE at /admin/users/[userId] (not slide-out -- 6-tab depth requires full page)
- State-changing actions REQUIRE confirmation dialog with mandatory reason field
- @glimmora/ui Label is <p> not <label> -- use native <label> for forms
- Button variant="outline" does not exist -- use variant="secondary"
- Skill Genome display uses SkillGenomePanel from @glimmora/ui -- private, NO peer comparison
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build user list page with type filter, DataTable, and UserActionDialog</name>
  <files>
    apps/admin-panel/src/app/(app)/users/page.tsx
    apps/admin-panel/src/components/users/user-list-table.tsx
    apps/admin-panel/src/components/users/user-action-dialog.tsx
    apps/admin-panel/src/components/users/index.ts
    apps/admin-panel/src/lib/msw/factories/user.ts
    apps/admin-panel/src/lib/msw/handlers/users.ts
    apps/admin-panel/src/lib/msw/handlers/index.ts
  </files>
  <action>
**Step 1: Create the user list page.**

`apps/admin-panel/src/app/(app)/users/page.tsx`:
'use client'

Fetches GET /api/admin/users via TanStack useQuery. Renders:
1. PageHeader "User Management" with right action: Link to "/users/verification-queue" showing Badge with count
2. Filter bar: TextInput for search (by name/email) + Select dropdown for userType (All / Woman Contributor / Community Support Lead / Student / Alumni / Enterprise Requester / Mentor) + Select for accountStatus (All / Active / Suspended / Pending / Deactivated)
3. UserListTable component with filtered data

Filter implementation:
```typescript
const [searchQuery, setSearchQuery] = useState('')
const [typeFilter, setTypeFilter] = useState<AdminUserType | 'all'>('all')
const [statusFilter, setStatusFilter] = useState<UserAccountStatus | 'all'>('all')

const filteredUsers = useMemo(() => {
  if (!data) return []
  return data.filter(u => {
    const matchesSearch = !searchQuery || u.displayName.toLowerCase().includes(searchQuery.toLowerCase()) || u.email.toLowerCase().includes(searchQuery.toLowerCase())
    const matchesType = typeFilter === 'all' || u.userType === typeFilter
    const matchesStatus = statusFilter === 'all' || u.accountStatus === statusFilter
    return matchesSearch && matchesType && matchesStatus
  })
}, [data, searchQuery, typeFilter, statusFilter])
```

**Step 2: Create UserListTable.**

`apps/admin-panel/src/components/users/user-list-table.tsx`:
'use client'

DataTable with columns:
- Name (text, clickable -> /users/:id via Link)
- Email (text)
- Type (Badge: map adminUserType to display labels and status variant)
- Status (Badge: active=done, suspended=urgent, pending_verification=inprogress, deactivated=normal)
- Joined (formatted date using date-fns format)
- Last Active (relative time using date-fns formatDistanceToNow)
- Projects (number)
- Actions (DropdownMenu: View Details, Suspend/Reactivate via UserActionDialog)

ColumnDef import: `import type { ColumnDef } from '@tanstack/react-table'`

The actions column includes:
- "View Details" link -> /users/:id
- "Suspend User" (if status is active) or "Reactivate User" (if suspended) -- both trigger UserActionDialog

Props: `{ users: AdminUserListItem[] }`

**Step 3: Create UserActionDialog.**

`apps/admin-panel/src/components/users/user-action-dialog.tsx`:
'use client'

Copy the EXACT pattern from 06-RESEARCH.md "Admin Action Confirmation Dialog" code example. This is a reusable dialog for all admin state-changing actions. It:
- Opens via DialogTrigger (trigger prop is the button/menu item)
- Shows action label and description
- Has a Textarea for mandatory reason (cannot submit if empty)
- Submit calls onConfirm(reason) which is a Promise
- Shows "Processing..." state while submitting
- Closes on success, clears reason
- Optional destructive prop for red button variant

Props: `{ actionLabel, actionDescription, onConfirm, trigger, destructive? }`

**Step 4: Create MSW user factory and handlers.**

`apps/admin-panel/src/lib/msw/factories/user.ts`:
- `createMockAdminUserList()` -- returns ~18-20 AdminUserListItem objects across all 6 types:
  - 4 woman-contributor (2 active, 1 suspended, 1 pending_verification)
  - 2 community-support-lead (both active)
  - 4 student-contributor (3 active, 1 pending_verification)
  - 2 alumni-contributor (1 active, 1 deactivated)
  - 3 enterprise-requester (2 active, 1 suspended)
  - 3 mentor-reviewer (2 active, 1 pending_verification)
- `createMockUserDetail(userId)` -- returns full user detail object with profile data, activity summary, project list, payment summary, skill genome data, and audit entries
- `createMockVerificationQueue()` -- returns 5-6 VerificationQueueItem objects with mixed types and verification types

`apps/admin-panel/src/lib/msw/handlers/users.ts`:
- GET /api/admin/users -> returns createMockAdminUserList() with type/status query param filtering
- GET /api/admin/users/:userId -> returns createMockUserDetail(userId)
- GET /api/admin/users/:userId/activity -> returns mock activity timeline (10-15 events)
- GET /api/admin/users/:userId/projects -> returns mock project list (3-4 projects)
- GET /api/admin/users/:userId/payments -> returns mock payment records (5-8 payments)
- GET /api/admin/users/:userId/skills -> returns mock SkillGenome data
- GET /api/admin/users/:userId/audit -> returns mock audit entries (5-10 entries)
- POST /api/admin/users/:userId/suspend -> accepts { reason }, returns 200
- POST /api/admin/users/:userId/reactivate -> accepts { reason }, returns 200
- GET /api/admin/users/verification-queue -> returns createMockVerificationQueue()
- POST /api/admin/users/verification/:itemId/approve -> accepts { reason }, returns 200
- POST /api/admin/users/verification/:itemId/reject -> accepts { reason }, returns 200

Update `apps/admin-panel/src/lib/msw/handlers/index.ts` to spread userHandlers.

`apps/admin-panel/src/components/users/index.ts`:
Barrel export all user components.
  </action>
  <verify>
Run `pnpm turbo build --filter=@glimmora/admin-panel` -- builds without errors.
Verify user list: `ls apps/admin-panel/src/app/\(app\)/users/page.tsx` -- exists.
Verify DataTable: `grep "DataTable" apps/admin-panel/src/components/users/user-list-table.tsx` -- must match.
Verify ColumnDef import: `grep "ColumnDef.*@tanstack/react-table" apps/admin-panel/src/components/users/user-list-table.tsx` -- must match.
Verify useMemo filter: `grep "useMemo" apps/admin-panel/src/app/\(app\)/users/page.tsx` -- must match.
Verify UserActionDialog: `grep "reason" apps/admin-panel/src/components/users/user-action-dialog.tsx` -- must match mandatory reason.
Verify MSW handlers: `grep "users" apps/admin-panel/src/lib/msw/handlers/index.ts` -- must match.
  </verify>
  <done>User list page with type/status/search filtering using useMemo (not DataTable built-in filter), UserActionDialog with mandatory reason for all state-changing actions, MSW factories generating 20 users across 6 types + verification queue items.</done>
</task>

<task type="auto">
  <name>Task 2: Build user detail 6-tab page and verification queue page</name>
  <files>
    apps/admin-panel/src/app/(app)/users/[userId]/page.tsx
    apps/admin-panel/src/app/(app)/users/verification-queue/page.tsx
    apps/admin-panel/src/components/users/user-detail-tabs.tsx
    apps/admin-panel/src/components/users/user-profile-tab.tsx
    apps/admin-panel/src/components/users/user-activity-tab.tsx
    apps/admin-panel/src/components/users/user-projects-tab.tsx
    apps/admin-panel/src/components/users/user-payments-tab.tsx
    apps/admin-panel/src/components/users/user-skills-tab.tsx
    apps/admin-panel/src/components/users/user-audit-tab.tsx
    apps/admin-panel/src/components/users/verification-queue-table.tsx
  </files>
  <action>
**Step 1: Create user detail page.**

`apps/admin-panel/src/app/(app)/users/[userId]/page.tsx`:
'use client'

Reads userId from URL params. Fetches GET /api/admin/users/:userId via TanStack useQuery. Renders:
1. PageHeader with user displayName + userType Badge + accountStatus Badge
2. Action buttons in PageHeader action area:
   - Suspend button (if active) -> UserActionDialog with destructive=true
   - Reactivate button (if suspended) -> UserActionDialog
   - Back button -> /users
3. UserDetailTabs component

**Step 2: Create UserDetailTabs.**

`apps/admin-panel/src/components/users/user-detail-tabs.tsx`:
'use client'

Uses Tabs from @glimmora/ui with 6 tabs:
```typescript
const TABS = [
  { value: 'profile', label: 'Profile' },
  { value: 'activity', label: 'Activity' },
  { value: 'projects', label: 'Projects' },
  { value: 'payments', label: 'Earnings/Payments' },
  { value: 'skills', label: 'Skill Genome' },
  { value: 'audit', label: 'Audit Log' },
]
```

Hash-based tab state (same pattern as enterprise project detail): read from window.location.hash, default to 'profile', update hash on tab change.

Props: `{ userId: string; userType: AdminUserType }`

**Step 3: Create each tab component.**

`user-profile-tab.tsx`:
Fetches GET /api/admin/users/:userId. Shows:
- Profile info: display name, email, type, status, join date, last active
- Type-specific fields: organization name for enterprise, university for student, tier for mentor
- ID/credential verification section: shows submitted documents (AP-07), verification status badge, approve/reject buttons if pending

`user-activity-tab.tsx`:
Fetches GET /api/admin/users/:userId/activity. Shows:
- Timeline of recent activities (last 30 days)
- Each entry: timestamp, action description, related entity link
- Uses Card + vertical timeline pattern (border-l-2 with dots)

`user-projects-tab.tsx`:
Fetches GET /api/admin/users/:userId/projects. Shows:
- DataTable with columns: Project Name (link to /projects/:id), Role, Status (Badge), Joined Date, Tasks Completed
- Empty state if no projects

`user-payments-tab.tsx`:
Fetches GET /api/admin/users/:userId/payments. Shows:
- Summary stats at top: total earned, pending amount, released amount (using Card)
- DataTable with columns: Payment ID, Project, Amount, Status (Badge), Date, Transaction ID
- Empty state if no payments

`user-skills-tab.tsx`:
Fetches GET /api/admin/users/:userId/skills. Shows:
- SkillGenomePanel from @glimmora/ui with the user's skill genome data
- Read-only view (admin can see but not edit)
- Privacy note: "This data is private to the contributor"

`user-audit-tab.tsx`:
Fetches GET /api/admin/users/:userId/audit. Shows:
- DataTable with columns: Timestamp, Action, Performed By, Reason
- Read-only -- no actions on audit entries
- Sorted by timestamp descending (most recent first)

**Step 4: Create verification queue page.**

`apps/admin-panel/src/app/(app)/users/verification-queue/page.tsx`:
'use client'

Fetches GET /api/admin/users/verification-queue via TanStack useQuery. Renders:
1. PageHeader "Verification Queue" with back link to /users
2. VerificationQueueTable component

`apps/admin-panel/src/components/users/verification-queue-table.tsx`:
'use client'

DataTable with columns:
- User Name (link to /users/:userId)
- User Type (Badge)
- Verification Type (text: "Identity", "Organization", "University", "Mentor Credentials")
- Documents (count)
- Submitted (formatted date)
- Actions: two buttons
  - "Approve" -> UserActionDialog (actionLabel: "Approve Verification", non-destructive)
  - "Reject" -> UserActionDialog (actionLabel: "Reject Verification", destructive)

Both approve and reject require a reason via UserActionDialog. Approve calls POST /api/admin/users/verification/:itemId/approve, reject calls POST /api/admin/users/verification/:itemId/reject.

Props: `{ items: VerificationQueueItem[] }`
  </action>
  <verify>
Run `pnpm turbo build --filter=@glimmora/admin-panel` -- builds without errors.
Verify user detail: `ls apps/admin-panel/src/app/\(app\)/users/\[userId\]/page.tsx` -- exists.
Verify 6 tabs: `grep "profile.*activity.*projects.*payments.*skills.*audit" apps/admin-panel/src/components/users/user-detail-tabs.tsx` -- must match.
Verify SkillGenomePanel: `grep "SkillGenomePanel" apps/admin-panel/src/components/users/user-skills-tab.tsx` -- must match.
Verify verification queue: `ls apps/admin-panel/src/app/\(app\)/users/verification-queue/page.tsx` -- exists.
Verify approve/reject: `grep "approve\|reject" apps/admin-panel/src/components/users/verification-queue-table.tsx` -- must match.
Verify UserActionDialog usage: `grep "UserActionDialog" apps/admin-panel/src/components/users/verification-queue-table.tsx` -- must match.
Verify credential verification in profile: `grep "verification\|credential" apps/admin-panel/src/components/users/user-profile-tab.tsx` -- must match.
  </verify>
  <done>User detail page with 6 tabs (Profile with ID/credential verification, Activity timeline, Projects list, Earnings/Payments, Skill Genome via SkillGenomePanel, Audit Log). Verification queue page with approve/reject actions requiring mandatory reasons. All admin actions logged to audit trail via MSW handlers.</done>
</task>

</tasks>

<verification>
1. `pnpm turbo build --filter=@glimmora/admin-panel` builds without errors
2. User list at /users shows DataTable with all 6 user types
3. Type filter (Select dropdown) + search (TextInput) + status filter work via useMemo
4. Clicking user name navigates to /users/[userId] (full page, not slide-out)
5. User detail shows 6 tabs with hash-based URL state
6. Profile tab shows user info + ID/credential verification with status
7. Skill Genome tab uses SkillGenomePanel from @glimmora/ui (private, no comparison)
8. Suspend/reactivate buttons trigger UserActionDialog with mandatory reason
9. Verification queue at /users/verification-queue shows pending users
10. Approve and reject both require reason (even approval -- stored in audit trail)
11. MSW handlers serve user list, detail, activity, projects, payments, skills, audit, verification queue
</verification>

<success_criteria>
- Admin can browse all users across 6 types with filtering and search
- Admin can view any user's complete profile across 6 tabs
- Admin can suspend or reactivate users with logged reasons
- Admin can process verification queue (approve/reject with reason)
- Admin can view ID/credential verification submissions
- All admin actions produce audit trail entries
</success_criteria>

<output>
After completion, create `.planning/phases/06-admin-panel/06-02-SUMMARY.md`
</output>
