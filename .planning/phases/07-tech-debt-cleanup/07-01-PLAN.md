---
phase: 07-tech-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/types/src/podl.ts
  - apps/university-portal/src/lib/msw/factories/podl.ts
  - apps/women-portal/src/lib/msw/factories/podl.ts
  - packages/types/src/evidence.ts
  - apps/enterprise-portal/src/lib/msw/factories/evidence.ts
autonomous: true

must_haves:
  truths:
    - "PoDL TypeScript interface has no verifiedByMentorId field"
    - "Women's portal PoDL factory compiles without verifiedByMentorId"
    - "University portal PoDL factory compiles without verifiedByMentorId"
    - "Evidence type has contributorId as optional field"
    - "Enterprise evidence factory produces no contributorId in mock data"
    - "pnpm turbo build passes clean after all type changes"
  artifacts:
    - path: "packages/types/src/podl.ts"
      provides: "Clean PoDL interface without verifiedByMentorId"
      contains: "interface PoDL"
    - path: "packages/types/src/evidence.ts"
      provides: "Evidence interface with optional contributorId"
      contains: "contributorId?"
    - path: "apps/enterprise-portal/src/lib/msw/factories/evidence.ts"
      provides: "Evidence factory without contributorId field"
    - path: "apps/university-portal/src/lib/msw/factories/podl.ts"
      provides: "PoDL factory without verifiedByMentorId"
    - path: "apps/women-portal/src/lib/msw/factories/podl.ts"
      provides: "PoDL factory without verifiedByMentorId"
  key_links:
    - from: "apps/university-portal/src/lib/msw/factories/podl.ts"
      to: "packages/types/src/podl.ts"
      via: "import type { PoDLCredential }"
      pattern: "import type.*PoDLCredential.*@glimmora/types"
    - from: "apps/enterprise-portal/src/lib/msw/factories/evidence.ts"
      to: "packages/types/src/evidence.ts"
      via: "import type { Evidence }"
      pattern: "import type.*Evidence.*@glimmora/types"
---

<objective>
Fix PoDL and Evidence TypeScript interfaces and downstream MSW factories to match the correct API contract.

Purpose: The shared types package has two incorrect fields -- `verifiedByMentorId` on PoDL (not part of the API contract) and `contributorId` on Evidence (should be optional since enterprise portal should not see contributor identity). Fixing these types first ensures all downstream code in Wave 2 builds against correct contracts.

Output: Clean `packages/types/src/podl.ts` and `packages/types/src/evidence.ts`, plus updated factories in enterprise, women's, and university portals. `pnpm turbo build` passes clean.
</objective>

<execution_context>
@/Users/kavi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kavi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-tech-debt-cleanup/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove verifiedByMentorId from PoDL type and factories (POL-05)</name>
  <files>
    packages/types/src/podl.ts
    apps/university-portal/src/lib/msw/factories/podl.ts
    apps/women-portal/src/lib/msw/factories/podl.ts
  </files>
  <action>
1. In `packages/types/src/podl.ts` line 11: DELETE the entire line `verifiedByMentorId: string` from the PoDL interface. The interface should go from `evidenceHash` directly to `organizationName`.

2. In `apps/university-portal/src/lib/msw/factories/podl.ts`: Remove the `verifiedByMentorId` property from ALL 5 credential objects in the `createMockCredentials()` function. The objects are at approximately lines 16, 31, 46, 61, 76. Each one has `verifiedByMentorId: 'mentor-0XX'` -- delete that entire key-value pair from each object.

3. In `apps/women-portal/src/lib/msw/factories/podl.ts`: Remove the `verifiedByMentorId` property from ALL 3 credential objects in the `createMockPoDLCredentials()` function. Each one has `verifiedByMentorId: 'mentor-0XX'` -- delete that entire key-value pair from each object (approximately lines 11, 19, 27).

IMPORTANT: Do NOT remove any other fields. Only remove `verifiedByMentorId`. The `contributorId` field stays on PoDL (it is correct there -- contributor portals display their own credentials).
  </action>
  <verify>
Run `pnpm turbo build --filter=@glimmora/types --filter=women-portal --filter=university-portal` and confirm zero errors. Then grep: `grep -r "verifiedByMentorId" packages/types/ apps/university-portal/src/lib/msw/factories/ apps/women-portal/src/lib/msw/factories/` should return zero results.
  </verify>
  <done>PoDL interface has no verifiedByMentorId. Both portal factories compile clean against the updated type. No other fields were removed.</done>
</task>

<task type="auto">
  <name>Task 2: Make Evidence.contributorId optional and remove from enterprise factory (POL-02)</name>
  <files>
    packages/types/src/evidence.ts
    apps/enterprise-portal/src/lib/msw/factories/evidence.ts
  </files>
  <action>
1. In `packages/types/src/evidence.ts` line 13: Change `contributorId: string` to `contributorId?: string` (add the `?` to make it optional). This is because contributor portals need it (they show the contributor their own evidence) but the enterprise portal API contract should NOT include it (privacy -- enterprise should not see contributor identity in evidence responses).

2. In `apps/enterprise-portal/src/lib/msw/factories/evidence.ts` line 12: DELETE the entire line `contributorId: \`contributor-${randomId('c')}\`,` from the `base` object inside `createMockEvidence()`. Since `contributorId` is now optional on the `Evidence` type, omitting it from the enterprise factory simulates the correct API behavior where enterprise does not receive contributor identity.

IMPORTANT: Do NOT touch evidence factories in other portals (women-portal, university-portal, mentor-portal). Those portals correctly include contributorId because contributors and mentors see their own/their mentees' evidence.
  </action>
  <verify>
Run `pnpm turbo build` (full monorepo build) and confirm zero errors. Then verify: `grep "contributorId" apps/enterprise-portal/src/lib/msw/factories/evidence.ts` should return zero results.
  </verify>
  <done>Evidence.contributorId is optional in the shared type. Enterprise factory produces evidence without contributorId. Full `pnpm turbo build` passes clean. Other portal factories are untouched and still compile (they provide contributorId which satisfies the optional field).</done>
</task>

</tasks>

<verification>
1. `pnpm turbo build` passes with zero errors across all workspaces
2. `grep -r "verifiedByMentorId" packages/types/src/podl.ts` returns nothing
3. `grep -r "verifiedByMentorId" apps/university-portal/src/lib/msw/factories/podl.ts apps/women-portal/src/lib/msw/factories/podl.ts` returns nothing
4. `grep "contributorId" apps/enterprise-portal/src/lib/msw/factories/evidence.ts` returns nothing
5. `grep "contributorId?" packages/types/src/evidence.ts` returns the optional field declaration
</verification>

<success_criteria>
- PoDL interface has exactly 12 fields (id, contributorId, taskId, projectId, issuedAt, title, description, skillsDemonstrated, evidenceHash, organizationName, isRevoked) -- verifiedByMentorId is gone
- Evidence interface has contributorId as optional (?)
- Enterprise evidence factory has no contributorId in its mock data
- `pnpm turbo build` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/07-tech-debt-cleanup/07-01-SUMMARY.md`
</output>
