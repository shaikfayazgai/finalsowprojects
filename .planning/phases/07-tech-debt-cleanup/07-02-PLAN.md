---
phase: 07-tech-debt-cleanup
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - apps/women-portal/src/app/layout.tsx
  - apps/university-portal/src/app/layout.tsx
  - apps/enterprise-portal/src/app/layout.tsx
  - apps/mentor-portal/src/app/layout.tsx
  - apps/admin-panel/src/app/layout.tsx
  - apps/enterprise-portal/src/components/projects/bulk-payment-release.tsx
  - apps/enterprise-portal/src/components/payments/pending-approvals-table.tsx
  - apps/enterprise-portal/src/components/projects/payment-release-card.tsx
autonomous: true

must_haves:
  truths:
    - "All 5 portals render Playfair Display for headings and DM Sans for body text in next dev"
    - "Font CSS variables are named --font-miller-display and --font-avenir (bridge names)"
    - "All enterprise portal OTP imports use the barrel pattern from @/components/shared"
    - "pnpm turbo build passes clean after font and import changes"
  artifacts:
    - path: "apps/women-portal/src/app/layout.tsx"
      provides: "Playfair Display + DM Sans via next/font/google with bridge variable names"
      contains: "Playfair_Display"
    - path: "apps/university-portal/src/app/layout.tsx"
      provides: "Playfair Display + DM Sans via next/font/google with bridge variable names"
      contains: "Playfair_Display"
    - path: "apps/enterprise-portal/src/app/layout.tsx"
      provides: "Playfair Display + DM Sans via next/font/google with bridge variable names"
      contains: "Playfair_Display"
    - path: "apps/mentor-portal/src/app/layout.tsx"
      provides: "Playfair Display + DM Sans via next/font/google with bridge variable names"
      contains: "Playfair_Display"
    - path: "apps/admin-panel/src/app/layout.tsx"
      provides: "Playfair Display + DM Sans via next/font/google with bridge variable names"
      contains: "Playfair_Display"
    - path: "apps/enterprise-portal/src/components/projects/bulk-payment-release.tsx"
      provides: "OTP import via barrel"
      contains: "from '@/components/shared'"
    - path: "apps/enterprise-portal/src/components/payments/pending-approvals-table.tsx"
      provides: "OTP import via barrel"
      contains: "from '@/components/shared'"
    - path: "apps/enterprise-portal/src/components/projects/payment-release-card.tsx"
      provides: "OTP import via barrel"
      contains: "from '@/components/shared'"
  key_links:
    - from: "apps/*/src/app/layout.tsx"
      to: "next/font/google"
      via: "font variable injection on html element"
      pattern: "className=.*font-miller-display.*font-avenir"
---

<objective>
Integrate production fonts via next/font/google across all 5 portals and fix OTP import paths to use barrel pattern.

Purpose: All 5 portals currently have commented-out local font loading (waiting for WOFF2 files). Replace with next/font/google using Playfair Display (heading substitute for Miller Display) and DM Sans (body substitute for Avenir LT Std). Use bridge CSS variable names (`--font-miller-display`, `--font-avenir`) so future licensed font switch is a single-file change per portal. Also standardize 3 OTP dialog imports in enterprise portal to use the barrel pattern.

Output: All 5 layout.tsx files load Google Fonts with correct variables applied to the html element. Three enterprise OTP imports use `@/components/shared`. `pnpm turbo build` passes.
</objective>

<execution_context>
@/Users/kavi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kavi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-tech-debt-cleanup/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Playfair Display + DM Sans via next/font/google to all 5 portal layouts (POL-03)</name>
  <files>
    apps/women-portal/src/app/layout.tsx
    apps/university-portal/src/app/layout.tsx
    apps/enterprise-portal/src/app/layout.tsx
    apps/mentor-portal/src/app/layout.tsx
    apps/admin-panel/src/app/layout.tsx
  </files>
  <action>
For each of the 5 layout.tsx files, apply the following transformation:

1. DELETE all the commented-out localFont code block (the entire block from `// Font loading -- uncomment when WOFF2...` through the closing `// })`). This is approximately lines 7-26 in each file.

2. ADD these imports at the top of the file (after existing imports, before `export const metadata`):
```typescript
import { Playfair_Display, DM_Sans } from 'next/font/google'

const playfairDisplay = Playfair_Display({
  subsets: ['latin'],
  weight: ['400', '500', '700', '900'],
  variable: '--font-miller-display',
  display: 'swap',
})

const dmSans = DM_Sans({
  subsets: ['latin'],
  weight: ['300', '400', '500', '700'],
  variable: '--font-avenir',
  display: 'swap',
})
```

3. Apply font variables to the `<html>` element by adding `className={`${playfairDisplay.variable} ${dmSans.variable}`}` to the `<html>` tag.

PORTAL-SPECIFIC DIFFERENCES:

**women-portal** (async layout, has next-intl + RTL):
- `<html>` tag currently: `<html lang={locale} dir={dir}>`
- Change to: `<html lang={locale} dir={dir} className={\`${playfairDisplay.variable} ${dmSans.variable}\`}>`

**university-portal** (async layout, has next-intl + RTL):
- Same as women-portal: `<html lang={locale} dir={dir} className={\`${playfairDisplay.variable} ${dmSans.variable}\`}>`

**enterprise-portal** (sync layout):
- `<html>` tag currently: `<html lang="en">`
- Change to: `<html lang="en" className={\`${playfairDisplay.variable} ${dmSans.variable}\`}>`
- Delete the comment `// When fonts are ready, add className=...` from above the return

**mentor-portal** (sync layout):
- Same as enterprise-portal pattern
- Delete the comment about fonts above the return

**admin-panel** (sync layout):
- Same as enterprise-portal pattern
- Delete the comment about fonts above the return

IMPORTANT: The CSS variable names MUST be `--font-miller-display` and `--font-avenir` (NOT `--font-playfair` or `--font-dm-sans`). These are bridge names -- the Tailwind config already maps `font-display` to `var(--font-miller-display)` and `font-body` to `var(--font-avenir)`. Using bridge names means future licensed font swap only changes the font loader, not Tailwind config.
  </action>
  <verify>
Run `pnpm turbo build --filter=women-portal --filter=university-portal --filter=enterprise-portal --filter=mentor-portal --filter=admin-panel` and confirm zero errors. Then verify each file: `grep "Playfair_Display" apps/*/src/app/layout.tsx` should match 5 files. `grep "font-miller-display" apps/*/src/app/layout.tsx` should match 5 files.
  </verify>
  <done>All 5 portals import Playfair Display and DM Sans from next/font/google, inject CSS variables --font-miller-display and --font-avenir on the html element, and the commented-out local font code is removed. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Fix OTP import paths to use barrel pattern (POL-04)</name>
  <files>
    apps/enterprise-portal/src/components/projects/bulk-payment-release.tsx
    apps/enterprise-portal/src/components/payments/pending-approvals-table.tsx
    apps/enterprise-portal/src/components/projects/payment-release-card.tsx
  </files>
  <action>
In each of these 3 files, change the OTP import from the direct path to the barrel pattern:

1. `apps/enterprise-portal/src/components/projects/bulk-payment-release.tsx` line 28:
   - FROM: `import { OTPConfirmationDialog } from '../shared/otp-confirmation-dialog'`
   - TO:   `import { OTPConfirmationDialog } from '@/components/shared'`

2. `apps/enterprise-portal/src/components/payments/pending-approvals-table.tsx` line 6:
   - FROM: `import { OTPConfirmationDialog } from '../shared/otp-confirmation-dialog'`
   - TO:   `import { OTPConfirmationDialog } from '@/components/shared'`

3. `apps/enterprise-portal/src/components/projects/payment-release-card.tsx` line 6:
   - FROM: `import { OTPConfirmationDialog } from '../shared/otp-confirmation-dialog'`
   - TO:   `import { OTPConfirmationDialog } from '@/components/shared'`

The barrel export at `apps/enterprise-portal/src/components/shared/index.ts` already exists and exports `OTPConfirmationDialog`. No changes needed to the barrel file.
  </action>
  <verify>
Run `pnpm turbo build --filter=enterprise-portal` and confirm zero errors. Then verify: `grep -r "from '../shared/otp-confirmation-dialog'" apps/enterprise-portal/src/components/` should return zero results. `grep -r "from '@/components/shared'" apps/enterprise-portal/src/components/` should return at least 3 results.
  </verify>
  <done>All 3 enterprise portal OTP imports use the barrel pattern `@/components/shared`. No direct path imports to otp-confirmation-dialog remain. Build passes.</done>
</task>

</tasks>

<verification>
1. `pnpm turbo build` passes with zero errors across all workspaces
2. All 5 portals have `Playfair_Display` and `DM_Sans` imports
3. All 5 portals have `--font-miller-display` and `--font-avenir` CSS variables on html
4. No commented-out local font code remains in any layout.tsx
5. Zero direct-path OTP imports in enterprise portal (all use barrel)
</verification>

<success_criteria>
- `grep "Playfair_Display" apps/*/src/app/layout.tsx` matches 5 files
- `grep "font-miller-display" apps/*/src/app/layout.tsx` matches 5 files
- `grep -r "from '../shared/otp-confirmation-dialog'" apps/enterprise-portal/` returns nothing
- `pnpm turbo build` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/07-tech-debt-cleanup/07-02-SUMMARY.md`
</output>
