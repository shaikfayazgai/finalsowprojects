---
phase: 07-tech-debt-cleanup
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - apps/admin-panel/src/lib/msw/handlers/audit-log.ts
  - apps/admin-panel/src/lib/msw/handlers/settings.ts
  - apps/admin-panel/src/lib/msw/handlers/index.ts
  - packages/types/src/admin.ts
  - apps/admin-panel/src/app/(app)/reports/page.tsx
  - apps/admin-panel/src/components/reports/report-builder-form.tsx
  - apps/admin-panel/src/lib/msw/factories/report.ts
  - apps/admin-panel/src/components/reports/report-pdf-document.tsx
autonomous: true

must_haves:
  truths:
    - "Admin panel settings MSW routes live in their own settings.ts file, not audit-log.ts"
    - "Admin panel has 6 report types including PoDL Ledger"
    - "PoDL Ledger report shows per-credential rows with credential ID, anonymized contributor, user type, project, task, skills, issue date, verification status"
    - "PoDL Ledger has date range picker and user type filter (Women/Student/Alumni tabs)"
    - "PoDL Ledger supports CSV and PDF export like the other 5 reports"
    - "pnpm turbo build passes clean"
  artifacts:
    - path: "apps/admin-panel/src/lib/msw/handlers/settings.ts"
      provides: "Extracted settings MSW handlers (admin CRUD + mockAdmins array)"
    - path: "apps/admin-panel/src/lib/msw/handlers/audit-log.ts"
      provides: "Audit log handler only (settings routes removed)"
    - path: "apps/admin-panel/src/lib/msw/handlers/index.ts"
      provides: "Barrel importing both auditLogHandlers and settingsHandlers"
      contains: "settingsHandlers"
    - path: "packages/types/src/admin.ts"
      provides: "ReportType union with podl_ledger added"
      contains: "podl_ledger"
    - path: "apps/admin-panel/src/app/(app)/reports/page.tsx"
      provides: "6 report type cards including PoDL Ledger"
      contains: "podl_ledger"
    - path: "apps/admin-panel/src/components/reports/report-builder-form.tsx"
      provides: "PoDL Ledger in report type options with user type filter"
      contains: "podl_ledger"
    - path: "apps/admin-panel/src/lib/msw/factories/report.ts"
      provides: "podlLedgerData factory with per-credential chartData rows"
      contains: "podl_ledger"
    - path: "apps/admin-panel/src/components/reports/report-pdf-document.tsx"
      provides: "PoDL Ledger in PDF title map"
      contains: "podl_ledger"
  key_links:
    - from: "apps/admin-panel/src/lib/msw/handlers/index.ts"
      to: "apps/admin-panel/src/lib/msw/handlers/settings.ts"
      via: "import { settingsHandlers }"
      pattern: "import.*settingsHandlers.*from.*settings"
    - from: "apps/admin-panel/src/components/reports/report-builder-form.tsx"
      to: "packages/types/src/admin.ts"
      via: "ReportType union includes podl_ledger"
      pattern: "podl_ledger"
    - from: "apps/admin-panel/src/lib/msw/factories/report.ts"
      to: "packages/types/src/admin.ts"
      via: "createMockReportData switch case for podl_ledger"
      pattern: "case.*podl_ledger"
---

<objective>
Extract settings MSW handlers from audit-log.ts into their own file, and add PoDL Ledger as the 6th admin report type with a filterable credential audit table.

Purpose: The audit-log.ts handler file currently contains unrelated settings routes (admin user CRUD) that belong in their own file. Additionally, the admin panel needs a PoDL Ledger report for credential auditing -- the 6th and final report type. This plan cleans up handler organization and completes the report suite.

Output: New `settings.ts` handler file, cleaned `audit-log.ts`, PoDL Ledger report type added across the full 7-step report pipeline (type union, page card, builder form, factory, PDF export). `pnpm turbo build` passes.
</objective>

<execution_context>
@/Users/kavi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kavi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-tech-debt-cleanup/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract settings handlers from audit-log.ts into settings.ts (POL-06)</name>
  <files>
    apps/admin-panel/src/lib/msw/handlers/audit-log.ts
    apps/admin-panel/src/lib/msw/handlers/settings.ts
    apps/admin-panel/src/lib/msw/handlers/index.ts
  </files>
  <action>
1. CREATE new file `apps/admin-panel/src/lib/msw/handlers/settings.ts` containing:
   - The `http`, `HttpResponse` imports from `'msw'`
   - The `isoPast`, `isoNow`, `randomId` imports from `'../factories/common'`
   - The `AdminUser`, `AdminRole` type imports from `'@glimmora/types'`
   - The entire `mockAdmins` array (currently lines 8-54 of audit-log.ts) -- all 5 admin user objects
   - The 3 settings-related route handlers (currently lines 61-86 of audit-log.ts):
     - `GET /api/admin/settings/admins`
     - `PATCH /api/admin/settings/admins/:id`
     - `POST /api/admin/settings/admins`
   - Export as `settingsHandlers` array

2. MODIFY `apps/admin-panel/src/lib/msw/handlers/audit-log.ts`:
   - Remove the `mockAdmins` array entirely (lines 8-54)
   - Remove the 3 settings routes from the `auditLogHandlers` array (keep only the `GET /api/admin/audit-log` handler)
   - Remove unused imports: `isoPast`, `isoNow`, `randomId` from `'../factories/common'` and `AdminUser`, `AdminRole` from `'@glimmora/types'`
   - The file should only have: `import { http, HttpResponse } from 'msw'`, the audit entries factory import, the mockAuditEntries const, and the single audit-log GET handler exported as `auditLogHandlers`

3. MODIFY `apps/admin-panel/src/lib/msw/handlers/index.ts`:
   - Add: `import { settingsHandlers } from './settings'`
   - Add `...settingsHandlers` to the `handlers` array (after `...auditLogHandlers`)
  </action>
  <verify>
Run `pnpm turbo build --filter=admin-panel` and confirm zero errors. Verify: `grep "settings" apps/admin-panel/src/lib/msw/handlers/index.ts` shows the import and spread. Verify: `grep "mockAdmins\|settings" apps/admin-panel/src/lib/msw/handlers/audit-log.ts` returns nothing (all settings code removed).
  </verify>
  <done>Settings handlers (mockAdmins array + 3 settings routes) live in their own settings.ts file. audit-log.ts contains only the audit log GET handler. index.ts imports and spreads both handler arrays. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add PoDL Ledger as 6th report type across the full report pipeline (POL-01)</name>
  <files>
    packages/types/src/admin.ts
    apps/admin-panel/src/app/(app)/reports/page.tsx
    apps/admin-panel/src/components/reports/report-builder-form.tsx
    apps/admin-panel/src/lib/msw/factories/report.ts
    apps/admin-panel/src/components/reports/report-pdf-document.tsx
  </files>
  <action>
Follow the same pattern as the existing 5 report types. Add `podl_ledger` at each point in the pipeline:

**Step 1: Type union** -- `packages/types/src/admin.ts` line 184:
- Change: `export type ReportType = 'platform_overview' | 'user_activity' | 'project_delivery' | 'financial' | 'skill_growth'`
- To: `export type ReportType = 'platform_overview' | 'user_activity' | 'project_delivery' | 'financial' | 'skill_growth' | 'podl_ledger'`

**Step 2: Report page card** -- `apps/admin-panel/src/app/(app)/reports/page.tsx`:
- Add `import { Award } from 'lucide-react'` (or add `Award` to existing lucide import)
- Add 6th entry to `REPORT_TYPES` array:
```typescript
{
  type: 'podl_ledger',
  title: 'PoDL Ledger',
  description: 'Credential audit trail, verification status, skill attestation',
  icon: Award,
},
```

**Step 3: Builder form options + filter** -- `apps/admin-panel/src/components/reports/report-builder-form.tsx`:
- Add to `REPORT_TYPE_OPTIONS` array: `{ value: 'podl_ledger', label: 'PoDL Ledger' }`
- Add a new constant for the PoDL user type filter:
```typescript
const PODL_USER_TYPE_FILTERS = [
  { value: 'all', label: 'All Contributors' },
  { value: 'women', label: 'Women Contributors' },
  { value: 'student', label: 'Students' },
  { value: 'alumni', label: 'Alumni' },
]
```
- Add `case 'podl_ledger': return PODL_USER_TYPE_FILTERS` to the `filterOptions` switch/useMemo (before the `default` case)

**Step 4: MSW factory** -- `apps/admin-panel/src/lib/msw/factories/report.ts`:
- Add a new `podlLedgerData()` function that returns `PlatformReportData` with:
  - `reportType: 'podl_ledger'`
  - `metrics`: `{ total_credentials: 342, verified: 298, pending: 31, revoked: 13, women_contributors: 128, student_contributors: 214 }`
  - `chartData`: An array of 8-10 per-credential row objects (NOT monthly aggregates). Each row:
    ```typescript
    {
      credential_id: 'PODL-001',
      contributor: 'W-7A3F',       // anonymized
      user_type: 'Women',          // or 'Student' or 'Alumni'
      project: 'E-Commerce Platform',
      task: 'API Integration',
      skills: 'React, TypeScript, API Design',
      issued: '2026-02-15',
      status: 'Verified',          // or 'Pending' or 'Revoked'
    }
    ```
  - Create 8 varied rows mixing Women/Student/Alumni user types and Verified/Pending/Revoked statuses. Use realistic project and task names.
- Add `case 'podl_ledger': return podlLedgerData()` to the `createMockReportData` switch statement

**Step 5: PDF export title** -- `apps/admin-panel/src/components/reports/report-pdf-document.tsx`:
- Add to `REPORT_TYPE_TITLES` object: `podl_ledger: 'PoDL Credential Ledger Report'`

IMPORTANT: The PoDL Ledger chartData is per-credential rows (flat table data), NOT monthly aggregates like the other 5 reports. The existing report builder's DataTable and chart rendering already handle arbitrary chartData keys dynamically, so no changes to the rendering logic are needed -- it will automatically create table columns from the row keys.
  </action>
  <verify>
Run `pnpm turbo build` (full monorepo) and confirm zero errors. Then verify:
- `grep "podl_ledger" packages/types/src/admin.ts` returns the type union line
- `grep "podl_ledger" apps/admin-panel/src/app/\(app\)/reports/page.tsx` returns the card entry
- `grep "podl_ledger" apps/admin-panel/src/components/reports/report-builder-form.tsx` returns the option + filter case
- `grep "podl_ledger" apps/admin-panel/src/lib/msw/factories/report.ts` returns the factory function + switch case
- `grep "podl_ledger" apps/admin-panel/src/components/reports/report-pdf-document.tsx` returns the PDF title
  </verify>
  <done>Admin panel has 6 report types. PoDL Ledger appears in the report type cards page, can be selected in the builder form with Women/Student/Alumni user type filter tabs, generates per-credential audit table data from the MSW factory, and exports to CSV and PDF. Full pnpm turbo build passes.</done>
</task>

</tasks>

<verification>
1. `pnpm turbo build` passes with zero errors
2. Admin panel handlers/index.ts imports and spreads settingsHandlers
3. audit-log.ts has no settings routes or mockAdmins
4. settings.ts exists with mockAdmins + 3 settings route handlers
5. ReportType union includes 'podl_ledger' (6 types total)
6. All 5 pipeline files contain 'podl_ledger'
7. PoDL Ledger factory returns per-credential rows (not monthly aggregates)
</verification>

<success_criteria>
- `wc -l apps/admin-panel/src/lib/msw/handlers/settings.ts` shows a non-empty file
- `grep -c "http\." apps/admin-panel/src/lib/msw/handlers/audit-log.ts` returns 1 (only audit-log route)
- `grep -c "podl_ledger" packages/types/src/admin.ts` returns 1
- Reports page has 6 ReportTypeCard entries
- PoDL Ledger factory chartData has per-credential rows with credential_id, contributor, user_type, project, task, skills, issued, status
- `pnpm turbo build` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/07-tech-debt-cleanup/07-03-SUMMARY.md`
</output>
